// E5-T06d: the browser build consumes the checked-in JavaScript projection directly. Keep the
// TypeScript source entrypoint aligned for editors and future bundlers.
export { PresentationController } from "./presentation.js";
