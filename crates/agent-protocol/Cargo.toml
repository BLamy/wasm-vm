[package]
name = "wasm-vm-agent-protocol"
description = "Shared no_std framing and negotiation protocol for the wasm-vm guest agent channel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true

[lib]
name = "wasm_vm_agent_protocol"

[dependencies]
