[package]
name = "wasm-vm-file-agent-storage"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
libc = "0.2"
sha2 = { version = "0.10", default-features = false }
unicode-normalization = "0.1"

[dev-dependencies]
tempfile = "3"
