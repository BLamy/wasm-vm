[package]
name = "wasm-vm-file-agent"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
sha2 = { version = "0.10", default-features = false }
wasm-vm-file-agent-storage = { path = "../file-agent-storage" }

[dev-dependencies]
tempfile = "3"
