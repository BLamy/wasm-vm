---
id: E3.5-T04h
epic: 3.5
title: Layer cache browser E2E — reload survival, dedupe, tamper refetch (Playwright)
priority: 312.43
status: pending
depends_on: [E3.5-T04g]
estimate: S
risk: high
capstone: false
---

## Goal
The browser proof (Playwright, E3-T05 double-boot pattern): pull a small riscv64 image, reload, pull
again is 100% cache hits with ZERO blob bytes over the network (request interception proves it); two
images sharing base layers store each shared layer once; a blob tampered via IDB is detected on read,
evicted, and refetched.

## Acceptance criteria
- [ ] Pull, reload, re-pull: 0 blob bytes over the network (intercepts prove), manifest re-resolution
  allowed.
- [ ] Shared base layers stored once (store-size accounting).
- [ ] IDB-tampered blob detected on read, evicted, refetched, never unpacked.

## Verification log
(empty)
