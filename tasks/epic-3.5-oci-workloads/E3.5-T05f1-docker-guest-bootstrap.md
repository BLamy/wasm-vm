---
id: E3.5-T05f1
epic: 3.5
title: Docker-tab guest bootstrap and honest capability state
priority: 312.4951
status: verified
depends_on: [E3.5-T05c, E3.5-T05e3]
estimate: S
risk: high
capstone: false
---

## Goal
Give the Docker tab one honest guest session boundary: boot the container-capable Alpine guest in-tab,
detect the runtime from guest files, and make provider/degraded states explicit before any lifecycle
action is offered.

## Context
T05e3 proves the Alpine bridge and T05c proves the guest lifecycle primitives, but the Docker tab still
needs to own boot readiness and capability gating. This slice does not implement image rows or container
actions. It prevents the public busybox build, a missing provider, or a failed Alpine boot from looking
like a partially working Docker daemon.

## Deliverables
- An in-tab Alpine boot affordance wired to `wvmDemo.bootAlpine()` with real progress and typed failure.
- Runtime gating based on the real `wvmDemo.hasContainerRuntime()` result, with no manifest-only guess.
- Honest no-provider state explaining that live pull requires a relay/tailscale provider and that the
  baked set is available; no fake spinner, layers, or progress.
- Public busybox-only degraded state that leaves the catalog surface usable but disables lifecycle work.

## Acceptance criteria
- [ ] A fresh local Chromium visit to the Docker tab can start the real Alpine boot in-tab; success and
      failure are rendered from the bridge result, and a failed boot exposes a typed error without
      pretending a container runtime is available.
- [ ] Runtime controls are enabled only after the guest probe returns true; the public busybox build
      reports runtime absent and shows an honest degraded banner rather than a broken lifecycle UI.
- [ ] With no network provider configured, Pull renders the baked-set/provider explanation and does not
      emit fabricated download progress or layer state.

## Adversarial verification
Use a fresh Chromium context with busybox assets and a context with Alpine assets. Remove or corrupt the
runtime metadata before probing: the UI must fail closed. Trigger boot failure and reload while the boot
is pending: no stale enabled controls or unhandled console error may remain.

## Verification log

### 2026-09-02 — worker — submission (commit `2838bc7`)

- Commands: `node --check web/ide.js`; `node --check web/dist/ide.js`; `node --check web/tests/e3-t05f1-docker-bootstrap.spec.js`; `node --check tools/verify/e3-t05f1-browser-proof.mjs`; `python3 tools/check_task_policy.py`; `make tasks-json`; `make web-dist`; `git diff --check`.
- Recorded acceptance: `node tools/verify/e3-t05f1-browser-proof.mjs` passed in Chromium `152.0.7977.75` against `http://127.0.0.1:8123`.
- Evidence: `evidence/e3-t05f1/docker-bootstrap-browser.json` (SHA-256 `ba74ad2d3424479a072cd02a07018fca601755d2fec21596b32559f9d4f1bfd1`) and `evidence/e3-t05f1/docker-bootstrap-browser.png` (SHA-256 `2a9a1697b0d461d368317eb845185128c488ed2aac82f0c17408780c39615a47`).
- The recording proves a real in-tab Alpine boot reaches `ready: true`, `guestUp: true`, and `runtime: available`; a real busybox boot remains `runtime: unavailable` with `RUNTIME_ABSENT`, disabled Run controls, and locked Containers; a busybox-to-Alpine switch returns typed `GUEST_CONFLICT`; the offline Pull path reports the baked guest set without progress; the provider re-render path was exercised; and a mocked missing Alpine manifest leaves the public build catalog-only with the Alpine button disabled. The public mock's two expected manifest 404 messages are recorded separately; all other scenarios have zero console errors and failed requests.

### 2026-09-02 — verifier — VERDICT: verified

- P1 in-tab Alpine bootstrap — HELD. Predicted a fresh Docker-tab click would drive the real bridge to a ready guest and then enable controls only after the guest probe; observed `alpineInTab.state = { ready: true, guestUp: true, runtime: "available" }` and the enabled Run control in `evidence/e3-t05f1/docker-bootstrap-browser.json`.
- P2 fail-closed capability boundary — HELD. Predicted a ready busybox guest would not count as a container runtime; observed `RUNTIME_ABSENT`, `runtime: "unavailable"`, disabled Run controls, and the locked Containers message. The attempted switch returned typed `GUEST_CONFLICT` (`busybox boot already owns the VM`) in the same evidence.
- P3 provider/degraded behavior — HELD. Predicted offline Pull would expose only the baked-set explanation with no fabricated progress; observed the offline notice and `provider: "offline"`. Switching the visible provider popover to `relay` re-rendered the explicit no-pull message, then returned to offline. The missing-manifest attack produced `alpineStatus: "absent"`, `alpineAssets: false`, a disabled Alpine button, and only the two expected mocked 404 messages; unexpected console errors and failed requests were empty.
- COVERAGE — HELD. The final replay exercised the new runtime CSS/DOM, Alpine present/absent probes, busybox and Alpine ready/booting transitions, available/unavailable/error gating, Pull/provider re-render, `startPsPoll`, and the guest-backed runtime probe. Defensive bridge-unavailable and probe-exception branches are typed fail-closed plumbing and were waived as unexecuted error-only branches; `web/ide.js` and `web/dist/ide.js` were byte-identical after `make web-dist`.
- SUITE: retained `web/tests/e3-t05f1-docker-bootstrap.spec.js` and `tools/verify/e3-t05f1-browser-proof.mjs`; the evidence screenshot is `evidence/e3-t05f1/docker-bootstrap-browser.png` (SHA-256 `2a9a1697b0d461d368317eb845185128c488ed2aac82f0c17408780c39615a47`). Independent machines and WebKit were waived by the user.
- Commands: `node tools/verify/e3-t05f1-browser-proof.mjs`; `node --check web/ide.js`; `node --check web/dist/ide.js`; `node --check web/tests/e3-t05f1-docker-bootstrap.spec.js`; `node --check tools/verify/e3-t05f1-browser-proof.mjs`; `git diff --check`; `make web-dist`.

## Acceptance command

`node tools/verify/e3-t05f1-browser-proof.mjs`
