---
id: E3.5-T05e1
epic: 3.5
title: Structured guest RPC framing and parser core
priority: 312.491
status: implemented
depends_on: [E3.5-T05b]
estimate: S
risk: medium
capstone: false
---

## Goal
Make the guest/UI request fence deterministic and independently testable before it is wired to a
slow browser boot: unique request IDs, guest-computed end markers, echo stripping, byte-stream
parsing, and structured exit results.

## Context
The bridge runs over one ttyS0 stream, so a parser bug can cross-contaminate requests or hang every
later command. `web/guest-rpc.js` is the pure seam extracted from `main.js`; it must stay independent
of WasmMachine, DOM state, and browser timing.

## Deliverables
- `web/guest-rpc.js` framing/parse helpers (`stripConsole`, `endMarkerRegex`, `formatRpcCommand`,
  `createFencedRpc`).
- `web/tests/guest-rpc.test.mjs` deterministic tests for the parser and command formatter.

## Acceptance criteria
- [ ] The exact-id marker is required; a wrong-id marker and the echoed `%s` formatter cannot settle
      a request.
- [ ] Markers split across feeds, ANSI/CR noise, a 100,000-line burst, and non-zero guest exits are
      handled without dropped output or a hang.
- [ ] `cd web && node --test tests/guest-rpc.test.mjs` passes with the adversarial cases above.

## Adversarial verification
Feed wrong-id, truncated, split, regex-special, and command-echo lines in arbitrary chunks. Mutate
the parser to accept any `__WVEND_` line or to use a host-computed exit code; the deterministic suite
must fail. Confirm the module has no DOM or WasmMachine dependency.

## Verification log

### 2026-09-02 — planning split

Separated from E3.5-T05e so the byte-stream trust boundary can be verified with a bounded node test
without waiting for Alpine boot. Existing parent evidence is context only; this child remains pending
until an exact-head worker run and a fresh verifier.

### 2026-09-02 — worker — implemented

Exact-head commit `4cbf42c` contains the extracted pure parser and its deterministic adversarial
suite. `cd web && node --test tests/guest-rpc.test.mjs` passed **10/10**: formatter fencing,
basic stdout/exit parsing, non-zero exit, split markers, wrong-ID marker spoofing, echoed `%s`
rejection, a 100,000-line burst, regex-special request IDs, and ANSI-wrapped markers. `node --check
guest-rpc.js` and `git diff --check` also passed. This child changes no guest or browser runtime
behavior, so no guest trace or Playwright capture is applicable; the node transcript is the direct
deterministic evidence. A fresh verifier must inspect the diff and rerun the exact command before
granting `verified`.

### 2026-09-02 — fresh adversarial verifier — VERDICT: refuted (commit `2a22162`)

- **HELD:** exact-id binding, wrong-id spoof resistance, truncation handling, regex-special IDs,
  echoed `%s`, ordinary non-zero exits, burst handling, and module independence all survived the
  verifier's probes.
- **FAILED:** a valid marker split between the digits of exit `17` settled early as exit `1` at
  `web/guest-rpc.js:26-47` because the matcher did not require the terminating newline. The verifier
  also found that the committed “100k-line” test fed only 1,000 lines of 100 characters. The required
  rework is to require a complete newline-terminated marker, add a multi-digit split regression, and
  make the burst test exercise exactly 100,000 lines.
- **Commands:** `cd web && node --test tests/guest-rpc.test.mjs`, `node --check guest-rpc.js`, the
  verifier-only split-marker probe, `git diff --check`, and source-independence inspection. The task
  returned to worker rework; no browser or guest trace is applicable to this pure module.

### 2026-09-02 — worker — rework complete

Fix commit `885bab2` makes `endMarkerRegex` require the newline terminating the complete guest
record, so a marker split between `_1` and `7\n` remains pending and returns exit 17 only after the
second feed. The regression suite now feeds exactly 100,000 output lines in bounded chunks. The
focused node gate and syntax check passed at that exact head; a fresh verifier must re-run the exact
head before granting `verified`.
