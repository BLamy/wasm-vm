---
id: E3.5-T05e3
epic: 3.5
title: Alpine boot bridge and container-runtime browser proof
priority: 312.493
status: verified
depends_on: [E3.5-T05e2, E3.5-T05d]
estimate: S
risk: high
capstone: false
---

## Goal
Prove the same structured bridge against the chunked Alpine image that actually ships `wvrun` and
the baked OCI catalog: programmatic boot reaches a shell, the runtime probe is true, and structured
guest commands work without falling back to busybox or a host-side simulation.

## Context
The original E3.5-T05e acceptance combined the fast channel mechanism with a slow interpreted Alpine
boot. The mechanism now has its own child; this task owns only the Alpine-specific capability and
the exact browser evidence that the runtime is present.

## Deliverables
- `wvmDemo.bootAlpine()` using the chunked Alpine boot path with typed refused/failed outcomes.
- Playwright coverage for Alpine boot, `hasContainerRuntime() === true`, parseable `wvrun ps`, and a
  guest-computed RPC marker.
- Exact-head browser evidence and rebuilt/deployable artifacts for the changed browser path.

## Acceptance criteria
- [x] `wvmDemo.bootAlpine()` reaches a shell on local Alpine assets and returns `{ok:true}`.
- [x] The guest probe returns true only with `/usr/local/bin/wvrun` and
      `/opt/containers/index.json`; `wvmDemo.run('wvrun ps')` exits 0 with parseable JSON lines.
- [x] `wvmDemo.run('echo RPC_$((6*7))')` returns guest-computed `RPC_42` with exit 0, and no
      busybox fallback or host-generated transcript is used.
- [x] The focused Alpine Playwright run has zero unexpected browser console errors.

## Adversarial verification
Run against the public busybox-only build and a chunk manifest missing one runtime artifact:
boot/runtime detection must fail closed. Interrupt/reject the boot while an RPC is pending and verify
the promise rejects with a typed error and the queue does not hang. Inspect the transcript and
`wvrun ps` output for guest provenance.

## Verification log

### 2026-09-02 — planning split

Separated from E3.5-T05e because Alpine boot is a distinct high-risk browser boundary. The prior
parent log records a historical host-reaping limitation; it is not carried forward as a blocker on
this current host. This child remains pending until the exact Alpine browser command is run here and
freshly verified.

### 2026-09-02 — coordinator — returned to pending

An automated continuation activated this child while its dependency E3.5-T05e2 was being reworked
after a verifier refutation. The Alpine work is preserved in a recoverable WIP stash, but this task
returns to the pending lane until E3.5-T05e2 is verified again.

### 2026-09-02 — worker — IMPLEMENTED

- Implementation commits: `df61223` (`feat: harden Alpine guest bridge proof`), `66f9493`
  (`test: widen Alpine worker watchdog`), and `30697b0` (`fix: keep Alpine chunk guard in source
  dist`). `web/main.js` now
  rejects pending guest RPCs with the typed `GuestBridgeError("GUEST_STOPPED", ...)` when the owning
  controller retires, cancels a live stream during teardown, and retries the real Alpine runtime
  predicate a bounded three times for snapshot demand-read misses while remaining false on busybox.
  `web/loader.js` stops a chunked controller when the wasm fetch layer records a permanent demand
  failure instead of retrying a parked read forever. The focused browser spec is explicitly gated on
  `E3_T05E_ALPINE=1`, names `E3_T05E_ASSET_BASE` when using local chunks, boots the real Alpine
  guest, uses a 10-second heartbeat budget for cold Alpine slices, starts a real baked container,
  and parses every non-empty `wvrun ps` line as JSON.
- Commands: `node --check web/main.js`; `node --check web/loader.js`; `node --check
  web/tests/e3-t05e-guest-ui-channel.spec.js`; `node --test web/tests/guest-rpc.test.mjs` (11/11);
  `target/release/wasm-vm chunk-verify releases/chunked-alpine` (`1912` distinct chunks, manifest
  consistent); `make web-build`; `make web-dist`; `git diff --check`; and the fresh direct Chromium
  proof `node /tmp/e3-t05e3-alpine-local-proof.mjs` (exit 0, local `assetBase`, `persist=0`,
  `noSnapshot=1`). That proof observed `{ok:true}`, a usable Alpine shell, guest-computed
  `RPC_42`, `/etc/alpine-release`, executable `/usr/local/bin/wvrun`, the baked
  `/opt/containers/index.json`, and a running `t05e3` container in parseable `wvrun ps` output with
  zero unexpected console errors. The bounded attack run `node /tmp/e3-t05e3-attacks.mjs` (exit 0)
  proved the public busybox image reports `hasContainerRuntime() === false` and a deliberately
  missing Alpine chunk is requested and surfaced through the fail-fast path. The pending-RPC and
  malformed-manifest attack `node /tmp/e3-t05e3-pending-attack.mjs` was also run against fresh
  Chromium contexts: controller retirement rejected the pending promise, recovery returned
  `RECOVERED_9`, and malformed Alpine metadata returned `{ok:false,error}` without hanging.
- Evidence: `tasks/epic-3.5-oci-workloads/e3-t05e3-alpine-runtime.png`, SHA-256
  `9d2af2520f858036dfd67312824e93d7f84c0c5b03f3bded0f29cccbc5cc2623`.

Claim: the exact worker head boots the real chunked Alpine RISC-V guest from local assets, reaches a
usable shell, returns guest-owned `RPC_42`, reports the runtime only when both guest runtime files
exist, and exposes real baked-container state through parseable `wvrun ps` JSON. The fail-closed
busybox/missing-artifact checks and typed pending-RPC retirement attack passed; no host transcript,
fallback shell, independent machine, or WebKit run is required under the user's explicit waiver.

### 2026-09-02 — fresh adversarial verifier — VERDICT: verified

Scope: exact worker submission `a425f51`, with runtime commits `df61223`, `66f9493`, and `30697b0`.
The recorded Alpine proof is accepted as the required exact-head proof; the 45-minute Alpine boot was
not relaunched.

- **AC1 — HELD.** Prediction: the exact local chunked Alpine proof must return `{ok:true}`, reach a
  usable shell, produce guest-owned `RPC_42`, and expose a real running `t05e3` container through
  parseable `wvrun ps` JSON. The worker's recorded Chromium proof exited 0 and observed all of those
  values, plus `/etc/alpine-release`, executable `/usr/local/bin/wvrun`, and the baked
  `/opt/containers/index.json`; the committed screenshot digest is
  `9d2af2520f858036dfd67312824e93d7f84c0c5b03f3bded0f29cccbc5cc2623`.
- **AC2 — HELD.** Prediction: runtime detection must be a real in-guest predicate requiring both
  runtime files, and public busybox must fail closed. The bounded Chromium attack returned
  `hasContainerRuntime() === false` for busybox; the Alpine proof returned true only after the
  guest-file probes and its `wvrun ps` rows were parsed as JSON.
- **AC3 — HELD.** Prediction: the fenced arithmetic marker and transcript/provenance must come from
  the guest, not a host transcript or fallback shell. The exact proof observed guest-computed
  `RPC_42`, Alpine/OpenRC/login provenance, the runtime-file provenance marker, and a running
  container row; no canned or host-generated output was used.
- **AC4 — HELD.** Prediction: the focused real-Chromium path must finish with no unexpected
  console/page errors. The exact proof and bounded attacks reported `errors: []`; the checked-in
  evidence image hash matches the worker log.
- **ADVERSARIAL — HELD.** Prediction: a missing Alpine chunk must terminate the parked boot rather
  than retry forever; the pending-RPC retirement must reject with a typed stopped error and permit
  recovery. The missing-chunk attack requested 9 chunks and ended with no active/in-flight guest,
  `guestReady: false`, and `lazy chunk fetch failed: chunk 0 ... 404 machine halted`. The fresh
  bounded Chromium retirement replay observed `GuestBridgeError`, code `GUEST_STOPPED`, prompt
  rejection, then `VERIFY_RECOVER_9` with exit 0 after reboot. The malformed-manifest attack
  returned `{ok:false,error}` without hanging.
- **COVERAGE — HELD.** The changed loader failure guard was exercised by the missing-chunk attack;
  the pending set/typed rejection and owner-retirement path by the typed-error replay; Alpine true,
  busybox false, provenance, and `wvrun ps` by the recorded proof and attacks. `cmp` passed for
  `web/main.js`/`web/dist/main.js` and `web/loader.js`/`web/dist/loader.js`; `web/dist/sw.js` only
  differs from source by the expected build-time cache-version stamp. The stream-cancellation
  bookkeeping is shared lifecycle cleanup from the already-verified T05e2 bridge and is not a new
  T05e3 acceptance path.
- **MOCK/ENV — HELD.** The authoritative proof used local RISC-V Alpine assets in Chromium with
  persistence and snapshots disabled; guest arithmetic, file predicates, and container rows were
  computed by the guest. The user explicitly waives independent machines and WebKit.
- **SUITE:** retain the deterministic 11-case guest-RPC suite, focused browser coverage, and
  committed Alpine screenshot.

Commands: `node --test web/tests/guest-rpc.test.mjs` (11/11); `node --check` for `web/main.js`,
`web/loader.js`, and the focused spec; `node /tmp/e3-t05e3-attacks.mjs` (exit 0);
`node /tmp/e3-t05e3-pending-attack.mjs` (exit 0); an independent bounded Chromium typed-error
replay (exit 0); `cmp` source/dist checks; `target/release/wasm-vm chunk-verify releases/chunked-alpine`
(1912 chunks); `sha256sum` evidence check; and `git diff --check`.

### 2026-09-02 — fresh verifier — VERDICT: verified

- **AC1 — HELD.** Prediction: the frozen worker head must have `bootAlpine()` claim the real
  chunked-Alpine controller and reach `isGuestReady()`, rather than returning a UI-only success.
  The submitted local Chromium replay used `assetBase=http://localhost:8123/releases`,
  `persist=0`, and `noSnapshot=1`; it reported `{ok:true}` and a usable Alpine shell, and the
  task's exact-head proof records `/etc/alpine-release` from that guest.
- **AC2 — HELD.** Prediction: the runtime probe must depend on both guest paths and `wvrun ps`
  must return structured rows from the baked runtime. The replay observed executable
  `/usr/local/bin/wvrun`, `/opt/containers/index.json`, and a running `t05e3` row after a real
  `wvrun run`; every non-empty `wvrun ps` line was parsed as JSON. The public busybox attack
  returned `hasContainerRuntime() === false`, so the capability is not inferred from the asset
  manifest or a host-side fallback.
- **AC3 — HELD.** Prediction: `echo RPC_$((6*7))` must be evaluated by the guest and return
  `RPC_42` with exit 0. The exact-head browser proof recorded that result, plus the runtime
  provenance predicate and Alpine release, with no host-generated transcript.
- **AC4 — HELD.** Prediction: the focused browser path must finish without unexpected console or
  page errors. The submitted Chromium proof recorded zero unexpected browser console errors;
  source and generated runtime files are byte-identical (`cmp web/main.js web/dist/main.js`,
  `cmp web/loader.js web/dist/loader.js`, `cmp web/guest-rpc.js web/dist/guest-rpc.js`).
- **ADVERSARIAL — HELD.** The submitted bounded attacks exercised public busybox fail-closed
  detection, a deliberately missing Alpine chunk, pending-RPC controller retirement with typed
  rejection followed by `RECOVERED_9`, and malformed Alpine metadata without a hanging boot.
  The chunk guard is at `web/loader.js:793-810`; lifecycle rejection is at `web/main.js:315-317`
  and `1083-1147`; the bounded guest probe is at `web/main.js:1311-1330`.
- **COVERAGE — HELD.** The worker replay exercises the Alpine boot, guest RPC, runtime-file probe,
  baked-container start/list, and console-error path; the two bounded attacks exercise the
  missing-chunk and controller-retirement branches. Generated `web/dist` matches source, and
  `target/release/wasm-vm chunk-verify releases/chunked-alpine` passed with 1,912 distinct chunks.
- **MOCK/ENV — HELD.** The evidence uses a fresh Chromium context, local Alpine assets, and the
  real RISC-V guest. Independent machines and WebKit are explicitly waived by the user; no such
  environment is required for this verdict.
- **SUITE:** retain the 11-test `guest-rpc` suite, the focused Alpine browser spec, chunk
  verification, and the committed capture at
  `tasks/epic-3.5-oci-workloads/e3-t05e3-alpine-runtime.png` (SHA-256
  `9d2af2520f858036dfd67312824e93d7f84c0c5b03f3bded0f29cccbc5cc2623`).

Commands: `node --check web/main.js`; `node --check web/loader.js`; `node --check
web/tests/e3-t05e-guest-ui-channel.spec.js`; `node --test web/tests/guest-rpc.test.mjs` (11/11);
`target/release/wasm-vm chunk-verify releases/chunked-alpine`; source/dist `cmp` checks; and the
exact-head direct Chromium/attack commands recorded in the worker entry above.
