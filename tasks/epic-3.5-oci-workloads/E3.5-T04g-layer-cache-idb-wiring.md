---
id: E3.5-T04g
epic: 3.5
title: Layer cache IndexedDB backing + importer pull-through + cacheStats surface
priority: 312.42
status: blocked
blocked_on: [E3.6-T02]  # no in-browser registry pull exists yet (native sideload only); needs the E3.6 live-pull path
depends_on: [E3.5-T04f, E3-T05, E3-T10]
estimate: S
risk: medium
capstone: false
---

## Goal
Back the E3.5-T04f cache with real IndexedDB (versioned stores oci-blobs + oci-meta, the E3-T05
IdbStore pattern), wire the browser OCI importer to check the cache BEFORE any network fetch (hits
logged + counted), integrate storage pressure with the E3-T10 quota story, and expose a cacheStats JS
surface + boot-panel readout.

## Acceptance criteria
- [ ] The importer resolves a needed layer from the cache with zero network when present; a miss
  fetches, verifies, and populates the cache.
- [ ] Cache size + hit/miss counters surfaced to JS and visible in the demo boot panel.
- [ ] Under a tiny budget, unpinned LRU blobs evict; running an image pins its layers.

## Verification log
(empty)
