---
id: E3.5-T05e2
epic: 3.5
title: Busybox browser bridge — serialized RPC, streaming, and fail-closed runtime
priority: 312.492
status: verified
depends_on: [E3.5-T05e1, E3.5-T05b]
estimate: S
risk: medium
capstone: false
---

## Goal
Wire the tested framing core to the real busybox browser console so `wvmDemo.run`, `wvmDemo.stream`,
and `wvmDemo.hasContainerRuntime` are honest over the existing ttyS0/input path, including
re-entrancy-safe stream stopping and a truthful runtime-absent result.

## Context
Busybox boots quickly and exercises the mechanism without conflating it with Alpine's long boot or
the presence of `wvrun`. This is the bounded browser proof for serialization, marker-spoof
resistance, streaming, non-zero exits, and fail-closed behavior.

## Deliverables
- `window.wvmDemo.run(cmd)`, `.stream(cmd, onLine)`, and `.hasContainerRuntime()` wired through the
  real console callback and `sendInput` bridge.
- A Playwright spec covering busybox RPC, back-to-back ownership, streaming stop, non-zero exit,
  marker spoof, and runtime absence.
- Rebuilt browser artifacts when `web/` sources change.

## Acceptance criteria
- [x] The real busybox guest returns guest-computed `RPC_42`, separate `A_2`/`B_4` results, and
      exit 1 for `false`.
- [x] A live stream delivers successive guest lines; stopping it leaves the guest usable for a fresh
      RPC, with no re-entrant WasmMachine error.
- [x] A wrong-id `__WVEND_` line cannot end a request, and `hasContainerRuntime()` is false on the
      public busybox build.
- [x] The focused Playwright run passes with zero unexpected browser console errors.

## Adversarial verification
Issue two requests in the same microtask, print a forged marker, stop from inside `onLine`, emit a
large burst, and call the runtime probe on a guest without `wvrun`. Any interleave, dropped fence,
re-entrancy error, optimistic runtime result, or unhandled console error refutes the bridge.

## Verification log

### 2026-09-02 — planning split

Separated from E3.5-T05e so the fast real-browser mechanism proof can be completed independently of
the Alpine runtime proof. Existing parent evidence is context only; this child remains pending until
an exact-head worker run and a fresh verifier.

### 2026-09-02 — worker — IMPLEMENTED

- Implementation commit: `c33a12b` (`feat: wire busybox guest RPC bridge`). `web/main.js` now consumes
  the tested `guest-rpc.js` formatter/parser, so the live tty bridge enforces the complete newline-
  terminated request marker and preserves the shared serialized executor. The focused spec uses the
  public guest-readiness hook so both cold boots and silent coherent snapshot restores prove the same
  usable shell.
- Commands: `node --test tests/guest-rpc.test.mjs` (11 passed); `node --check main.js`; `make web-build`;
  `make web-dist`; and `node /tmp/e3-t05e2-browser-proof.mjs` (Chromium Playwright browser proof,
  PASS). The browser run exercised guest-computed `RPC_42`, same-microtask `A_2`/`B_4` serialization,
  guest exit 1, forged-marker output, successive streaming lines with stop from `onLine`, a post-stop
  `ALIVE_10` RPC, and zero unexpected console errors.
- Evidence: `tasks/epic-3.5-oci-workloads/e3-t05e-guest-ui-channel.png`.

Claim: at the frozen implementation head, the real busybox RISC-V guest owns every value asserted by
the browser proof. The parser cannot settle on a wrong request id or on a partial multi-digit marker;
the serialized live bridge returns each command's own guest output and exit status, deferred stream
stop avoids WasmMachine re-entrancy, and the real busybox probe correctly reports no container runtime.

### 2026-09-02 — verifier — VERDICT: verified

- AC1 — HELD. Prediction: after a real busybox boot, `run()` must return guest-computed `RPC_42`,
  separate `A_2`/`B_4` results for same-microtask calls, and `exit: 1` for `false`. The independent
  Chromium replay observed `{stdout: "A_2\\n\\n", exit: 0}`, `{stdout: "B_4\\n\\n", exit: 0}`,
  and `{stdout: "\\n", exit: 1}`; the worker replay also observed `RPC_42`. The live entrypoint is
  `web/main.js:1263`, and its serialized chain is `web/main.js:1069-1103`.
- AC2 — HELD. Prediction: a stream stopped from inside its line callback must deliver ordered guest
  lines, defer Ctrl-C out of the callback, and leave the shell usable. The independent replay observed
  `["VSTREAM_0", "VSTREAM_1", "VSTREAM_2"]`, then `AFTER_10` with exit 0 and no browser errors;
  the stop/send deferral is at `web/main.js:1309-1321`.
- AC3 — HELD. Prediction: a wrong-id marker and a partial multi-digit marker cannot settle the live
  request, while the real busybox probe returns false. The independent parser attack fed a wrong-id
  marker followed by a split `_1`/`2\\n` and observed `{stdout: "__WVEND_other_99\\n", exit: 12}`;
  byte-at-a-time framing of exit 37 also held. The load-bearing matcher is `web/guest-rpc.js:26-27`,
  parser state is `web/guest-rpc.js:39-57`, and the real runtime probe is
  `web/main.js:1267-1276`; the independent browser observed `hasContainerRuntime false`.
- AC4 — HELD. Prediction: every focused assertion must run against the real page with no unexpected
  console error. The worker and independent Chromium replays both completed the RPC, spoof, stream,
  and recovery assertions with `errors: []`; the checked-in assertions are
  `web/tests/e3-t05e-guest-ui-channel.spec.js:46-119`, and the committed capture is
  `e3-t05e-guest-ui-channel.png` (SHA-256
  `9d2af2520f858036dfd67312824e93d7f84c0c5b03f3bded0f29cccbc5cc2623`).
- COVERAGE — HELD. `web/main.js:27` imports the pure module, `guestExec` calls both
  `createFencedRpc` and `formatRpcCommand` at `web/main.js:1079-1094`, and the independent browser
  replay exercised those paths through the real worker-backed tty. The remaining changed lines are
  comments, the readiness assertion in the checked-in spec, and generated `web/dist`/task metadata;
  source/dist parity was verified with `cmp`, so no changed runtime hunk is unexercised or dead.
- MOCK/ENV — HELD. The browser replay boots the local RISC-V busybox artifacts and derives all
  asserted arithmetic and exit values in the guest; no canned output, `cfg(test)` path, independent
  machine, or WebKit dependency was used. The 11-test pure suite passed, and the novel split-byte
  attack passed.
- SUITE: retain the 11-test deterministic parser suite, the focused browser spec, and the committed
  screenshot as permanent evidence.

Commands: `cd web && node --test tests/guest-rpc.test.mjs` (11 passed); `cd web && node --check
main.js && node --check guest-rpc.js`; `node --input-type=module -e '…'` (independent browser replay,
PASS); and `node --input-type=module -e '…'` (independent split-byte/wrong-id parser attack, PASS).

### 2026-09-02 — fresh adversarial verifier — VERDICT: refuted

- **AC1 — HELD.** Prediction: the real busybox guest must return guest-computed `RPC_42`, isolate
  same-microtask `A_2`/`B_4`, and surface `false` as exit 1. Fresh Chromium observed
  `{"stdout":"RPC_42\\n\\n","exit":0}`, pair results
  `{"stdout":"A_2\\n\\n","exit":0}` and `{"stdout":"B_4\\n\\n","exit":0}`, and
  `{"stdout":"\\n","exit":1}`. The live path is `web/main.js:1260-1263`, serialized at
  `web/main.js:1069-1103`.
- **AC2 — FAILED.** Prediction: stopping from inside `onLine` must release the console cleanly so the
  next fenced RPC returns only its own output. The fresh Chromium run did call `stop()` from the
  callback (`stream.lines=["VSTREAM_0","VSTREAM_1","VSTREAM_2"]`, `stoppedFromOnLine=true`),
  but the subsequent structured result was
  `after.stdout="VSTREAM_5\\nVSTREAM_6\\nVSTREAM_7\\n^Cecho ALIVE_$((5+5)); ... ALIVE_10\\n\\n"`,
  `exit:0`. The stale stream tail, interrupt, and echoed next command are an interleave into the
  fresh RPC. `web/main.js:1314-1321` defers Ctrl-C while `web/main.js:1091-1100` admits the next
  request; `web/guest-rpc.js:53-57` only strips the first newline-delimited echo, so it cannot cleanly
  separate this tail. Serialize/drain stream termination before admitting the next RPC, then
  re-record the browser proof.
- **AC3 — HELD.** Prediction: a forged wrong-id marker cannot settle the live request and the busybox
  runtime probe must fail closed. Fresh Chromium observed spoof stdout containing both
  `__WVEND_deadbeef_0` and guest-computed `REAL_7` with exit 0, and `hasContainerRuntime=false`.
  The matcher is `web/guest-rpc.js:23-28`; the probe is `web/main.js:1267-1277`.
- **AC4 — HELD.** The required deterministic suite passed 11/11 with zero failures, and the fresh
  real Chromium context reported `errors=[]`; the bounded guest burst also preserved exactly 2,000
  `BURST_` lines through `BURST_1999`. The fresh capture is `/tmp/e3-t05e2-fresh-chromium.png`
  (SHA-256 `9d2af2520f858036dfd67312824e93d7f84c0c5b03f3bded0f29cccbc5cc2623`).
- **COVERAGE — HELD except AC2.** `web/main.js:27` imports `guest-rpc.js`; fresh Chromium exercised
  `createFencedRpc`/`formatRpcCommand` through `web/main.js:1079-1094`, the stream path at
  `web/main.js:1285-1321`, and the real worker-backed busybox tty. Source/dist parity was checked
  with `cmp`. No runtime source was edited by this verifier.
- **MOCK/ENV — HELD.** The run used a fresh headless Chromium context against the local real
  RISC-V busybox page with service workers blocked, no independent machine, and no WebKit. Arithmetic,
  exit status, marker output, stream lines, and burst data came from the guest; the failure is a
  bridge ordering issue, not a canned transcript.
- **SUITE:** retain the 11-test parser suite and browser capture, but do not promote this task until
  stream termination and the next RPC are serialized without stale output interleaving.

Commands: `node --test web/tests/guest-rpc.test.mjs` (11 passed); the independently authored bounded
`node --input-type=module -` Chromium replay against `http://127.0.0.1:8123` (fresh context,
`serviceWorkers: "block"`); `git diff --exit-code c33a12b -- web/main.js web/guest-rpc.js web/dist/main.js
web/dist/guest-rpc.js`; and `python3 tools/check_task_policy.py` (before metadata regeneration).

### 2026-09-02 — worker — rework IMPLEMENTED

- Rework commit: `78da226` (`fix: serialize stream shutdown before guest RPC`). A live stream now
  owns the tty until Ctrl-C is followed by a private guest-computed no-op fence; queued `run()` calls
  await that barrier, so late foreground output cannot be consumed as the next RPC's stdout. The
  barrier is bounded and reports a typed error to a caller if shutdown cannot complete.
- Commands: `node --test web/tests/guest-rpc.test.mjs` (11 passed); `node --check web/main.js`;
  `node --check web/guest-rpc.js`; `make web-build`; `make web-dist`; and
  `node /tmp/e3-t05e2-browser-proof.mjs` (fresh Chromium, PASS). The replay observed guest-computed
  `RPC_42`, serialized `A_2`/`B_4`, exit 1 for `false`, wrong-id marker resistance, three successive
  stream lines, and a clean post-stop `{stdout: "ALIVE_10\\n\\n", exit: 0}` with no `STREAM_` or `^C`
  residue. Unexpected browser errors were empty.
- Evidence: `tasks/epic-3.5-oci-workloads/e3-t05e-guest-ui-channel.png`, SHA-256
  `9d2af2520f858036dfd67312824e93d7f84c0c5b03f3bded0f29cccbc5cc2623`.

Claim: at the frozen rework head, stream shutdown and the next fenced RPC are ordered by guest
execution rather than JavaScript timing. The private fence is accepted only after the interrupt and
all preceding stream output, and the next RPC starts after that fence, eliminating the refuted stale
tail interleave while retaining the existing fencing, serialization, spoof resistance, and
fail-closed behavior.

### 2026-09-02 — fresh adversarial verifier — VERDICT: verified

- **AC1 — HELD.** Prediction: a real busybox boot and fenced RPC must return the guest-computed
  `RPC_42` with exit 0. The fresh Chromium replay observed `{"stdout":"RPC_42\\n\\n","exit":0}`.
  Same-microtask ownership and non-zero exit remain covered by the unchanged rework evidence and
  deterministic parser suite.
- **AC2 — HELD.** Prediction: stopping from inside `onLine` must deliver ordered successive guest
  lines, complete the Ctrl-C/private-fence barrier before the next RPC, and keep that RPC's stdout
  free of stream residue. The fresh replay observed
  `{"lines":["VSTREAM_0","VSTREAM_1","VSTREAM_2"],"stoppedFromOnLine":true}` followed by
  `{"stdout":"ALIVE_10\\n\\n","exit":0}`; `ALIVE_10` contained no `VSTREAM_` and no `^C`.
  This exercises the changed shutdown/barrier paths in `web/main.js` (the `streamBarrier` wait and
  private guest fence) against the real busybox tty.
- **AC3 — HELD.** Prediction: the exact-id fence parser must reject forged markers and the real
  busybox runtime probe must report false. The existing fresh adversarial evidence observed wrong-id
  marker resistance and `hasContainerRuntime() === false`; the deterministic parser suite passed
  all 11 cases at this exact head.
- **AC4 — HELD.** Prediction: the focused browser path must complete without unexpected console
  errors and preserve a large output burst. The fresh Chromium context reported `errors: []` and
  preserved exactly 2,000 `BURST_` lines through `BURST_1999`.
- **COVERAGE — HELD.** The replay executed the real busybox boot, `guestExec` fencing, the stream
  callback/stop path, the private stop fence, the post-stop RPC, and the large-output parser path;
  `cmp web/main.js web/dist/main.js` and `cmp web/guest-rpc.js web/dist/guest-rpc.js` both passed.
  The focused spec assertions for the fixed regression are at
  `web/tests/e3-t05e-guest-ui-channel.spec.js:116-121`; generated dist files are from the same
  exact rework head.
- **MOCK/ENV — HELD.** This was a fresh headless Chromium context with service workers blocked,
  using the local real RISC-V busybox guest and guest-computed arithmetic. No independent machine or
  WebKit was used. The deterministic suite passed 11/11 and `node --check` passed for the browser
  sources and focused spec.
- **SUITE:** retain the 11-test parser suite, the focused browser spec, and the committed final
  stream screenshot as permanent regression evidence.

Evidence: `tasks/epic-3.5-oci-workloads/e3-t05e2-stream-final.png`, SHA-256
`d46296e729fa1eaa53e1af80f845f61b96cdf1eda317bf13c8f0dffd5ade3267`.

Commands: `node /tmp/e3-t05e2-stream-proof.mjs` (fresh Chromium, real busybox, PASS);
`node --test web/tests/guest-rpc.test.mjs` (11 passed); `node --check web/main.js`,
`node --check web/loader.js`, `node --check web/tests/e3-t05e-guest-ui-channel.spec.js`;
`cmp web/main.js web/dist/main.js`; `cmp web/guest-rpc.js web/dist/guest-rpc.js`; and
`git diff --check`.

### 2026-09-02 — independent fresh adversarial verifier — VERDICT: verified

Scope: the E3.5-T05e2 runtime under test is the `78da226` stream-shutdown rework submitted by
`76fc42c`. The shared worktree advanced through metadata-only commits while this verification was
running; `git diff --exit-code 78da226 HEAD -- web/main.js web/dist/main.js web/guest-rpc.js
web/dist/guest-rpc.js` passed, so the T05e2 runtime is unchanged. The unrelated dirty T05e3
`web/loader.js` chunk-fetch hunk and T05e3 spec/image were not staged or used as T05e2 evidence.

- **AC1 — HELD.** Prediction: the real busybox guest must own the arithmetic and exit values, and
  same-microtask calls must not exchange stdout. A fresh Chromium context observed
  `RPC_42`/exit 0, exact pair results `{stdout: "A_2\\n\\n", exit: 0}` and
  `{stdout: "B_4\\n\\n", exit: 0}`, and `false` as `{stdout: "\\n", exit: 1}`. The live
  serialized path is `web/main.js:1079-1112`; the guest-owned public entrypoints are
  `web/main.js:1206-1227` and `web/main.js:1267-1272`.
- **AC2 — HELD.** Prediction: if `stop()` is called from `onLine` and a fresh RPC is queued before
  the stop fence completes, the stream must deliver ordered successive lines and the fresh RPC must
  contain only its own output. The same-page Chromium attack observed
  `VSTREAM_0`, `VSTREAM_1`, `VSTREAM_2`, `stoppedFromOnLine: true`, then exactly
  `{stdout: "ALIVE_10\\n\\n", exit: 0}`; the stop promise completed with no error. It also observed
  duplicate stream creation rejected with `a guest stream is already active`, and a second stop
  returned cleanly. The barrier and fence are `web/main.js:1071-1081`, `web/main.js:1303-1317`,
  and `web/main.js:1341-1375`; the focused regression assertions are
  `web/tests/e3-t05e-guest-ui-channel.spec.js:114-121`.
- **AC3 — HELD.** Prediction: a forged marker for another request id cannot settle the live parser,
  a multi-digit marker split before its terminating newline stays pending, and the real busybox
  runtime probe fails closed. The real guest spoof command returned exit 0 with both the forged
  `__WVEND_deadbeef_0` and guest-computed `REAL_7`; an independent in-page parser fed the wrong-id
  marker plus `_1`, then `2`, then `\\n`, returning `null`, `null`, and `{stdout:
  "__WVEND_wrong-id_99\\n", exit: 12}` respectively. A real guest `sh -c 'exit 12'` also returned
  exit 12, and `hasContainerRuntime()` returned `false`. The exact-id/newline matcher and stateful
  parser are `web/guest-rpc.js:23-57`; the fail-closed probe is `web/main.js:1273-1287`.
- **AC4 — HELD.** Prediction: the bounded browser proof must retain a large burst without loss and
  report no unexpected browser errors. The fresh context retained exactly 2,000 ordered lines from
  `BURST_0` through `BURST_1999`, with `consoleErrors: []`, `pageErrors: []`, and no warnings. A
  fresh screenshot was captured at `/tmp/e3-t05e2-independent-verifier.png` (SHA-256
  `53973db53605e8a5847625cab49f2d6a80cfc1027bcfc545a08c5ae68403050d`).
- **COVERAGE — HELD.** Before the browser run, the rework diff was inspected line by line. In-page
  Chromium V8 coverage recorded the `guestExec` task, stream constructor, `stop`, `finishStop`,
  `drainOnc`, private fence, and post-stop RPC; the same-page attack additionally exercised the
  active-stream and idempotent-stop branches. The isolated no-op input attack bounded the new
  `guest stream stop timed out` path and showed both `stop()` and a queued RPC receiving that typed
  error, without browser errors. The live RPCs and the independent import of `guest-rpc.js` executed
  the formatter/parser path. `cmp` passed for `web/main.js`/`web/dist/main.js` and
  `web/guest-rpc.js`/`web/dist/guest-rpc.js`; generated task metadata and the stamped dist service
  worker are metadata-only. The transport-catch arms were not forced because they require a thrown
  page controller call rather than a guest behavior named by this task.
- **MOCK/ENV — HELD.** All guest values came from a fresh headless Chromium context loading the
  local RISC-V busybox artifacts, with `serviceWorkers: "block"`; no canned transcript, independent
  machine, or WebKit was used. The bounded real Chromium proof was independently authored via
  `node --input-type=module -`, and the deterministic parser suite passed 11/11.
- **SUITE:** retain the 11-test parser suite, focused Playwright assertions, and existing committed
  stream screenshot; the fresh screenshot and command transcripts above are supplemental verifier
  evidence and are intentionally not added to the implementation diff.

Commands: `node --test web/tests/guest-rpc.test.mjs`; `node --check web/main.js && node --check
web/guest-rpc.js`; `bash tools/serve-dev.sh 8132`; three fresh Chromium runs supplied on stdin to
`node --input-type=module -` covering the full busybox matrix, same-page queued-stop/duplicate/
idempotent-stop attack, and bounded no-op-input timeout attack; `python3 tools/check_task_policy.py`;
`cmp -s web/main.js web/dist/main.js`; `cmp -s web/guest-rpc.js web/dist/guest-rpc.js`; and
`git diff --check`.
