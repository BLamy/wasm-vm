---
id: E5.5-T03i
epic: 5.5
title: Test timer-rate sensitivity of the restored Omarchy desktop
priority: 525.02595
status: verified
depends_on: [E5.5-T03h]
estimate: S
risk: medium
capstone: false
---

## Boundary

One bounded diagnostic using the same warm R3 LP1 pair, built runtime,
headed browser, default JIT/cache settings and physical x procedure as T03h.
Only the existing phase-preserving ICount divider selection changes, from
64 to 1. No new runtime semantics, renderer changes, image edits, production
deployment, repeated divider exploration or Epic 6 work.

The separate Daybreak critic recommended this existing-setting experiment
before adding an admission probe. It tests whether timer rate is sufficient
to restore physical response, not whether llvmpipe CPU ticks prove a fence.

## Acceptance criteria

- Bind actual served runtime, kernel, manifest, restored R3 pair and machine
  state. Verify actual divider 1, JIT on and all other settings unchanged.
- Capture a baseline screenshot, exactly one trusted physical x, immediate
  stats/screenshot and final stats/screenshot after 120–180 host seconds.
  No explicit serial diagnostic command during the timed observation.
- Report actual guest-time, instruction and presentation deltas. Personally
  inspect screenshots for a physical x. Faster serial replies or startup
  dialog disappearance are not physical response.
- If time advances faster but input remains absent, end this clock experiment
  and move to the exact recurrent-block admission witness named by T03h.
  Do not repeat a divider sweep or call timer adjustment a fix.
- If physical x appears, the eventual remedy remains separately gated by the
  unchanged 120-second physical nonce/readback and real GUI acceptance.
- Fresh critic independently checks the matched comparison and limitations.

## Verification command

```sh
node tools/verify/omarchy-input-diagnostic.mjs \
  evidence/omarchy-profile/timer-sensitivity-r1 1 1 \
  --pair-directory target/omarchy-sdr-r3-snapshot \
  --chunk-dir target/omarchy-profile-chunks-sdr-r3-256k --headed
```

Use the unchanged frozen recorder. Record the command stream/timestamps and
file digests, then quit after the final screenshot. Reuse held helper tests;
no runtime rebuild for this configuration-only observation.

## Adversarial verification

Reject arguments substituted for actual state, mixed RAM/disk provenance,
serial-created response, input-queue drainage substituted for guest
consumption, different runtime/cache settings, or a longer input window
presented as passing the eventual 120-second acceptance. A negative result
applies only to this matched timer-rate trial, not all clock policies.

## Verification log

### 2026-09-14 UTC — worker — bounded negative result

Frozen head `26085068dd2b892f4c2ab585e3461c3f91aefe6a`, unchanged recorder
and R3 pair, completed one headed divider1 recording. Exact launch, command
stream, counters, limitations and digests are in
`evidence/omarchy-profile/timer-sensitivity-r1/RESULT.md`. Raw JSON and actual
screenshots are adjacent. The coordinator inspected the immediate and final
screenshots: no physical x, with the real waiting dialog still visible.

The 142.229-second immediate-to-endpoint interval retired 597,947,910
instructions and advanced 59.7947895 guest seconds, but received/presented
frames stayed 1/1. Actual trusted KeyX events and all four successful worker
acknowledgements are recorded; input queue had two pending events immediately
and zero at the end, without drops/rejections. This does not prove guest
consumption. No explicit serial diagnostic ran; app RPCs and opaque agent
messages retain the existing observer limitations.

`node evidence/omarchy-profile/timer-sensitivity-r1/audit.mjs` passed, binding
actual served bytes and unchanged helper hashes plus the recording's events.
No runtime/helper change requires a new build or guest rerun. Divider1 was
insufficient in this matched restored arm; stop divider exploration. Any
next remedy still needs separate 120-second physical nonce acceptance.
Fresh Daybreak review remains pending. No production changes or merge.

### 2026-09-14 UTC — worker — timer comparison activated

T03h is now independently verified as a diagnostic, not a fix. Start the
single matched divider1 comparison using the unchanged recorder and R3 pair.
The pre-result predictions below remain the falsification criteria.

### 2026-09-14 UTC — verifier — pre-result predictions

Read this pending task before any T03i result. T03h's diagnostic-only review
is now complete; these predictions do not activate this task or authorize a
production change. Reuse the frozen recorder and unchanged R3 boundaries;
the one intended difference is ICount divider1 rather than divider64.

- **I1 identity.** Actual restored-machine state will show ICount divider1,
  JIT enabled and unchanged cache/residency configuration, with the same
  served core/kernel/manifest/RAM-disk identities and headed browser mode
  as T03h. A URL argument alone is insufficient. This is a warm restore,
  not a cold boot or continuation of an already advanced R2 guest.
- **I2 physical path and timing.** One trusted, canvas-focused KeyX down/up
  will produce the exact evdev45 down/up calls and sync acknowledgements.
  Baseline, immediate and 120–180-host-second endpoint observations will
  retain actual counters/timestamps and screenshots. No explicit serial
  diagnostic will run in that window; existing app readiness/opaque-agent
  traffic retains the same limitations. An empty queue is not guest
  consumption, and a delayed observation cannot be relabelled a 120-second
  nonce acceptance pass.
- **I3 timer mechanism.** Busy retire-derived clock progress should be
  consistent with one tick per retirement at the unchanged 10 MHz timebase,
  rather than one per 64 retirements; idle fast-forward can add time.
  More guest time alone is the expected configuration effect, not a cause
  established for the desktop stall or a rendering-throughput improvement.
  Compare actual retirements and presentation progress as well as host time;
  a 64-fold host-speed improvement is not predicted.
- **I4 falsifiable outcome.** A visible physical x within the observation
  would support a timer-sensitive response in this matched restored arm,
  but still needs the separate unchanged 120-second physical nonce/readback
  acceptance before any fix claim. If time advances faster but x is absent,
  divider1 is insufficient under this bounded test; end divider exploration
  and return to the precise recurrent-block admission witness in T03h.
  Neither outcome by itself proves timer starvation, useful shader work,
  a renderer-fence owner or an admission fault. Faster serial replies,
  new frames or disappearance of the startup dialog alone are not a
  physical-input response.

### 2026-09-14 UTC — verifier — VERDICT: verified (bounded negative diagnostic)

Independently reviewed the completed raw recording at frozen head
`26085068dd2b892f4c2ab585e3461c3f91aefe6a` against I1–I4, then read the
worker's RESULT.md and one-run audit. No guest was launched, no runtime or
recorder was changed, and no new checker was required. This verifies the
diagnostic conclusion only: divider1 was insufficient in this restored R3
arm. It does not verify responsive input or refute all clock policies.

- **I1 — HELD.** Independently hashed all 83 served responses (69 unique
  paths) and bound all 81 recorded browser requests. Runtime/helper bodies
  match both the frozen trial head and T03h recorder head
  `ea77bfa1c3d50b9b08ecd381025bbfc393741b52`; generated manifest routes,
  fetched chunk membership/content hashes, kernel and exact R3 RAM/disk
  pins also match. Actual restore is true, stored-snapshot restore was not
  attempted, and ready plus all three stats records show ICount1/10 MHz,
  JIT enabled, decoded cache4096 and repack-off cap24. See
  `timer-sensitivity-r1/identities.json:1268` and
  `timer-sensitivity-r1/diagnostic.json:614`. Headed mode and the four
  helper digests match the held baseline; URL arguments were not treated
  as actual-state proof.
- **I2 — HELD.** Independently checked all raw worker methods/order and
  reparsed the serial fences. `wire.json:1` contains exactly two trusted,
  focused KeyX events at 03:47:21.568/03:47:21.681 UTC; calls 92/93/95/96
  are exact evdev45 down/sync/up/sync, each acknowledged true by
  03:47:21.792. The endpoint stats are 142,878 ms after keyup, inside the
  declared 120–180-second diagnostic window. Immediate pending events are
  two (`diagnostic.json:513`), endpoint zero (`:879`), without drops or
  rejections. This is not proof of Linux/Hyprland/Foot consumption.
  No explicit serial diagnostic or process sample occurred. Only existing
  app ls/readiness commands are on serial; the last readiness RPC remains
  incomplete at shutdown. The 190 opaque 18-byte agent messages retain the
  payload/observer limitation, not an asserted observer-free interval.
- **I3 — HELD.** Independently recomputed immediate-to-endpoint deltas:
  142,229 host ms, 597,947,910 retirements, and 597,947,895 clock ticks
  (59.7947895 guest seconds). Source points are `diagnostic.json:439`,
  `:612`, `:667`, `:805`, `:978` and `:1033`. Clock and scheduler reads
  are sequential; their 15-count difference does not contradict the busy
  one-tick-per-retirement prediction. More guest time than T03h's
  1.6749978-second window is observed, not a claim of 64-fold host speedup
  or a causal attribution for different instruction throughput.
- **I4 — HELD, negative branch.** Frames received and successful presents
  stay at 1/1, with no presentation backlog (`diagnostic.json:556`, `:922`).
  Independently viewed `after-input-window.png`: real waiting dialog over
  Foot/bar, no x. Its SHA matches the before-input screenshot. Thus nearly
  60 further guest seconds did not produce visible physical response within
  this bounded arm. End divider exploration and pursue the precise recurrent
  renderer-block admission witness named in T03h, without presuming that
  discovery saturation is causal. No crash, fence ownership, timer-starvation
  diagnosis, necessary GPU architecture change or input-fix claim follows.

Independently matched SHA-256 values under
`evidence/omarchy-profile/timer-sensitivity-r1/`:

- diagnostic.json: `710b3705e3daed7c82db5c09a1b5d17ac1f5f0d3706b7010535079091a5708f0`
- identities.json: `d75f6b6d63aeee69e4a7aaef6af2ed2a9442acded22dc190575a64012d8f2c33`
- wire.json: `f20726ca47d71c7936a9d1e31bad1a134119033fa0554282fa19b431ba0e97a8`
- before-input.png / after-input-window.png: `eb4b181dc1480935a2ef49f1480d082eeb8371d5229dbef13a473b1c05c3da3e`
- after-input-immediate.png: `e9e97d1afcbaf32fba419933c1ac580983d21901d6d195e9c710258776533ee9`

COVERAGE/SUITE: reuse unchanged T03h helper/observer tests and prior HELD
runtime boundaries. Independent read-only Node assertions checked actual
served bytes against both frozen Git versions, raw input/ACKs, allowable
serial commands, actual clock/configuration, timing and arithmetic; visual
inspection checked the endpoint. The worker's one-run audit output agrees.
Retain this negative recording and the existing tests; no new runtime build,
cold clone, boot, divider sweep or general checker expansion is necessary.
Only this task file was changed by the verifier. Queue regeneration and
Git operations remain with the coordinator as explicitly requested.
