---
id: E5.5-T03n
epic: 5.5
title: Recover compositor wait stacks from the pinned Omarchy checkpoint
priority: 525.025998
status: implemented
depends_on: [E5.5-T03m]
estimate: S
risk: medium
capstone: false
---

## Boundary

Inspect the existing R3 RAM checkpoint offline. T03h records a sleeping
compositor and an executing renderer, but cannot identify the saved wait or
its caller. The checkpoint includes their kernel stacks and saved userspace
registers. Decode those bytes using the exact matching kernel and its build
layout; do not start a guest, modify runtime/image defaults, or repeat the
failed physical-input experiments.

This is a checkpoint observation, earlier than T03m's physical-input attempt.
It cannot establish that the same wait persisted through the later failure,
that a runnable task was executing at the checkpoint, or that input works.
T03d remains a planning container until a measured cause supports a remedy.

## Deterministic acceptance

`node tools/verify/omarchy-wait-checkpoint.mjs evidence/omarchy-profile/checkpoint-wait-r1`

- Bind the exact compressed snapshot, kernel Image, System.map and kernel
  layout provenance. Validate the actual saved address-translation mode and
  kernel code bytes before interpreting task fields.
- Traverse the real task/thread lists with reciprocal-link, identity and
  traversal bounds. Report PID/TGID, name and saved state; a RAM string search
  alone is not a task identity proof.
- Recover bounded kernel frame chains and the saved trap registers for the
  compositor leader and its renderer. Cite physical byte addresses, raw
  values and enclosing kernel symbol ranges. Stop on an invalid chain; do
  not guess through it. Distinguish saved user context from live execution.
- Identify the concrete checkpoint wait where supported, and state the
  remaining probe needed to connect it to the observed input failure.
- Focused deterministic tests reject corrupted kernel/layout identity,
  invalid page-table entries, broken task links and invalid frame chains.
  No native/WASM rebuild or browser run is required for this offline tool.

## Adversarial verification

A fresh critic predicts before inspecting the final output, independently
checks the cited snapshot bytes and kernel/layout identity, and performs one
bounded corruption attack. Reject name-scanning as task-list membership,
heuristic offsets, stale stack frames called current execution, unsupported
user-symbol attribution, or this earlier checkpoint presented as proof of the
later failed input interval. Preserve all prior HELD negative browser results.

## Verification log

### 2026-09-15 — coordinator — offline diagnostic activated

The queue has no active task. Continue T03d's missing-causal-probe investigation
with one S diagnostic. Read-only exploration found the existing local kernel
build matches the released Image and contains generated task/pt_regs offsets.
Preliminary snapshot reads locate plausible compositor and renderer records;
these are exploratory claims pending list-based identity and recorded proof.
No guest execution, image mutation, deployment or Epic 6 work is in scope.

### 2026-09-15 — independent critic — pre-result predictions

Before inspecting the inspector or final output, the fresh critic predicted:

- P1: exact R3 snapshot/kernel/layout hashes match, saved SATP selects the
  address translation, and symbol-bound kernel instruction bytes match.
- P2: bounded reciprocal task/thread lists establish unique PID/TGID and
  group-leader identities; RAM name matches alone are insufficient.
- P3: raw state and saved RA/SP/S0 are physically cited. Sleeping and runnable
  saved contexts are distinguished from current CPU execution.
- P4: bounded aligned frame chains stay in the kernel stack and executable
  symbol ranges. Any invalid frame stops interpretation. A futex chain needs
  corroborating saved syscall context; user-library/owner claims need more proof.
- P5: exact-layout pt_regs retain raw EPC/status/cause/arguments/original a0,
  with saved user context explicitly identified through trap metadata.
- P6: wrong identity, invalid PTEs, broken lists and invalid frames fail closed;
  the critic adds one independent bounded corruption attack.
- P7: the checkpoint predates T03m. Connecting its wait to the failed input
  interval needs a later-interval stack/wait-object observation. No remedy or
  responsiveness follows from this inspection.

The critic additionally requires full container/sparse bounds, canonical
virtual addresses, reserved-PTE and superpage-alignment checks, and exact
offsetof provenance for fields absent from generated asm-offsets. The real
kernel uses signal.thread_head/task.thread_node for thread-list traversal.

### 2026-09-15 — worker — implemented, offline checkpoint observation only

Frozen head `f3e0bba9a84091e64eca56de5dc3b42dd604e2f8`. Commands, raw
physical-byte citations, limits and digests are recorded in
`evidence/omarchy-profile/checkpoint-wait-r1/RESULT.md`; the complete decoded
observation is adjacent `checkpoint.json`. Exact gate outputs and exit codes
are in `evidence/omarchy-profile/checkpoint-wait-gates/run.json` and1–4.log.
Node syntax, scoped Python compilation, all8 corruption/reader tests and the
deterministic acceptance command passed. No runtime or browser gate applies.

Claim: the pinned earlier R3 checkpoint contains a list-identified Hyprland417
saved futex system call on user word `0x55555efbb948` (physical `0x87e2d948`,
value0), and a list-identified runnable llvmpipe462 saved interrupt context at
user EPC `0x7fff6c08ac7e`. The current saved CPU belongs to Bash174. Ten actual
kernel code anchors and matching generated-layout provenance precede all task
interpretation. Kernel frames, trap registers and address translations retain
raw byte citations. No runtime/image edit, new boot, deployment or input-fix
claim occurred. This earlier snapshot cannot establish persistence into T03m's
failed input window or a specific userspace caller/wake dependency. T03d remains
unresolved; fresh independent review is required for this diagnostic claim.
