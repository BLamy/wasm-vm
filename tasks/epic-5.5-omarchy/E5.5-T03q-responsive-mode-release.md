---
id: E5.5-T03q
epic: 5.5
title: Publish the validated responsive Omarchy desktop
priority: 525.027
status: pending
depends_on: [E5.5-T03t]
estimate: S
risk: medium
capstone: false
---

## Boundary

Apply the smallest execution or startup remedy demonstrated by the current
measured implementation slices to the actual Omarchy page and paired artifacts. Start only after its
recording proves physical input and real visible application response at the
unchanged deadline. If that condition fails, replan from the measured result;
do not publish an unproven lower resolution as a responsiveness fix.

T03p did not demonstrate guest mode adoption. T03r therefore tests explicit
compositor selection first; its diagnostic verification alone is insufficient.
T03r adopted the mode but produced a black screen and failed input. T03s now
measured 13.425016% FP compute. T03t begins the narrow FP JIT hybrid authorized
by the existing measured policy. This publication task remains gated on real
input success; instruction support alone cannot start it.

## Deterministic acceptance

`node tools/verify/omarchy-responsive-mode.mjs evidence/omarchy-profile/responsive-mode-final`

The actual built and published Omarchy page must use the validated remedy,
accept physical keyboard input with independent nonce readback in 120 seconds,
and show the real typed application response. Preserve readable desktop layout,
CSS fitting and correct pointer coordinates. Record source/artifact hashes and
screenshots, update the roadmap honestly, run affected browser tests plus the
live riscv suite, commit web/dist and publish through the existing Cloudflare
Pages path. Record a pristine-clone build check for the deployment claim.

## Adversarial verification

A fresh critic verifies actual guest dimensions rather than advertised mode or
canvas size, independent nonce creation and visible response; tests one bounded
viewport/pointer case; and checks the final served artifact identity. Keep all
PRs open. No Epic 6 GPU/JIT expansion or GitHub Actions.
