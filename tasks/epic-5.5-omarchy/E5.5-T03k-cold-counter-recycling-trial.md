---
id: E5.5-T03k
epic: 5.5
title: Test bounded cold-counter recycling against actual desktop input
priority: 525.02599
status: in-progress
depends_on: [E5.5-T03j]
estimate: S
risk: high
capstone: false
---

## Boundary

One local-only, explicit-opt-in JIT admission trial. T03j measured real
full-table refusals but its first-seen sample did not identify the hot work
behind them. It does not establish the GUI stall's cause. The critic recommends
one controlled counter-recycling A/B, not another probe or parameter sweep.

When a new physical entry encounters a full cold-counter map, the candidate
recycles only that cold history and begins counting the current entry normally.
Do not clear queued/excluded decisions, queued-hit priorities, discovery FIFO,
compile queue, installed code, decoded blocks, guest memory or architectural
state. Do not bump the code generation. Keep the normal threshold and all
other policies. Record actual selection and bounded reset/discard accounting.

The initial implementation is default off so control and candidate are the
same binary with one explicit boot option different. This is an experiment,
not a permanent legacy-compatibility mode or production default. A useful
result must be promoted separately into a clean default and release proof;
a failed result is not deployed or called a fix.

## Deterministic acceptance

`cargo test -p wasm-vm-core --lib --features trace cold_counter_recycling`

- A finite cold flood at the real default threshold must no longer prevent
  a subsequently recurring block from being nominated at that threshold.
  Disabled behavior and admission-probe accounting remain unchanged.
- Recycling is bounded by the existing counter capacity; it does not reset
  nominated/excluded decisions, exact requests, compile priority or generation.
- Changed code and real invalidation retain the existing stale-install checks.
- Deterministic retired trace and architectural snapshots agree with control
  over varied cold-flood/hot-loop schedules. Nomination counts may differ;
  that intentional difference must not be mistaken for architectural drift.
- Selection is an explicit boot option after restore, before execution, with
  actual state returned in existing JIT statistics and no mutation controller RPC.

## Browser trial contract

Use a local-only input-trial path in the existing actual desktop verifier,
reusing its physical key mapping, focus checks and read-only nonce lookup.
Keep R3 LP1, divider64,
JIT on, decoded cache4096, repack-off cap24, profiling/timing off, and admission
observation off in both arms. Never trigger the candidate arm on saturation.

Run control then candidate in separate fresh Chrome processes/contexts, closing
each before the next. Pin the same built binary and exact R3 pair/base bytes;
the only selected runtime difference is the recycling boolean. Record actual
selection, restore, policies, source identities and screenshots in each arm.

Warm-up is the same condition in both arms, not a manually chosen elapsed delay:
real desktop-ready evidence, mapped Foot and its actual active-window identity,
plus a coherent shipped-snapshot restore. Impose an absolute 300,000 ms startup
deadline from navigation through these checks, including asset restore and
serial-query waits. Do not reset it between stages or wait for table saturation.
The unchanged R3 renderer/image proof is carried forward; this trial makes no
new renderer or image-construction claim and does not add renderer-log probes.

When those conditions are met, immediately use the existing physical typing
sequence, capped at60,000ms with the unchanged40ms key delay and focus checks.
Record typing duration separately. Start the unchanged 120,000 ms
readback deadline after Enter, as the existing verifier does; every queued
read-only lookup must fit its remaining deadline. Record a presentation baseline
before typing and require a fresh real presentation after it. Retain a final
post-input screenshot with a bounded capture allowance (20,000 ms); failure
capture/shutdown has a bounded allowance and must not keep the guest running
indefinitely. This trial ends after input proof rather than performing unrelated
reload, second-tab or renderer experiments.

The only meaningful positive outcome is actual physical-keyboard nonce creation
with independent read-only verification within the unchanged 120-second limit,
plus personally inspected real post-input desktop screenshots. Do not type via
serial, extend the deadline, paint fake output, remove the readiness criterion,
or infer success from more compilation/fewer refusals. Preserve failed arms and
report warm-up time separately from input latency and first-boot readiness.

One control and one candidate, not a tuning sweep. If the candidate fails,
retain the failure and stop this policy trial; choose any next scoped action
from the evidence without assuming permission for Epic 6/GPU architecture work.

## Verification

Fresh Daybreak predictions precede the run. Risk-tier gates are scoped to this
changed admission boundary; preserve the known broad macOS `make ci` failures
and do not claim they passed. Reuse unchanged HELD observer/queue findings.
No activation until T03j's fresh verdict closes the active lane.

## Verification log

### 2026-09-14 UTC — coordinator — frozen candidate, pre-browser gates

Runtime/browser adapter and built dist are frozen at
`d68be8425cae6f8a921b2ecc85d757314a6b09d4`. WASM SHA-256:
`9405d6c38be9a170ef5a2e5e0bacdcbec6deace7de48c8e002bc3caea76dfb2b`
(1,585,863 bytes). Core and wrapper targeted tests passed7+1. Main recorded
scoped clippy (13m55s), all352 core +33 WASM library tests (zero failures/ignores),
fmt, actual built-WASM API,71 sequential browser adapters,28 recorder tests and
the built browser ISA suite127/0 with zero application/HTTP errors. The real
ISA screenshot was opened by the coordinator; it is not an Omarchy input proof.
Logs are in `evidence/omarchy-profile/counter-recycling-gates/`.

Before either arm, Daybreak found and the coordinator fixed Enter-deadline and
owned-browser cleanup gaps. An additional recording-only guard records the
recorder's `exit` immediately because stdio `close` can arrive later; its
regression proves a later watchdog does not signal that exited process group.
All12 final input/watchdog guards pass (`watchdog-exit.log`). This changes no
core, WASM, boot adapter or dist bytes, so unchanged native proof is carried
forward. The exact runtime-head scratch clone has passed its7 core tests and
is still compiling the wrapper test. No A/B has run, no responsiveness claim
or deployment is made, and status remains in-progress.

### 2026-09-14 UTC — coordinator — activated after independent T03j verdict

T03j is independently verified at `ed619d58`, for instrumentation only.
Implement the single default-off recycling trial and fixed physical-input A/B
above. This does not change production or claim that admission causes the stall.

### 2026-09-14 UTC — coordinator — pending plan only

The T03j critic's provisional review recommends this one bounded experiment.
No runtime policy has been changed, the A/B contract still needs to be frozen,
and this pending file is not a desktop-fix or verification claim. T03d remains
the unresolved product-level planning container.

### 2026-09-14 UTC — critic — preflight and pre-result predictions K1–K8

Read the whole contract before either arm or its implementation evidence.
T03j's HELD instrumentation/queue checks carry forward only where unchanged;
its zero retained-identity contribution does not establish a recycling remedy.
No T03k verdict is made here.

The identical 300-second startup predicate is fair and historically possible,
not guaranteed. `softpipe-r1/baseline-built-r2/report.json` under
`evidence/omarchy-profile/` records actual desktop-ready at64,006.915ms and
physical typing beginning208.245 seconds after report startup, with mapped
Foot/active-window checks and additional renderer queries already completed.
That older R3 observation is feasibility evidence, not this trial's control.
Conversely, T03j R2's final screenshot still has the waiting overlay roughly
303 seconds after its recorded app request. The current same-binary control
may therefore legitimately exhaust the startup cap. Freeze that possibility
and its outcome classification now; do not lower readiness after seeing it.

- **K1 — explicit selection, default unchanged.** Unset/false retains legacy
  full-map refusal behavior. True arms only the selected recycling policy
  after restore and before execution. The actual control/candidate JIT stats
  report false/true respectively, with threshold512, divider64, cache4096,
  repack-off cap24, profiling/timing off and admission observation off in both.
  No controller mutation RPC or implicit JIT/profiling enablement appears.
- **K2 — finite-flood admission contrast.** At the browser's actual threshold
  512, a finite cold flood followed by one sufficiently recurring eligible
  block leaves control refusing that untracked block, whereas candidate
  counts it normally and nominates at the unchanged threshold. The test must
  set/check512 explicitly: bare core discovery defaults to64. Every triggering
  entry is counted once. Reset/discard accounting equals the cold history
  actually removed by the frozen algorithm, and retained counters never exceed
  existing capacity. This does not promise fairness under an endless flood.
- **K3 — preserved non-cold state and architectural semantics.** Recycling
  neither clears nor changes queued/excluded decisions, queued-hit priorities,
  FIFO/compile requests, resident/decoded code or code generation. An already
  decided entry cannot trigger recycling through the ordinary dedup path.
  Varied deterministic flood/loop and changed-code schedules retain identical
  retired traces/architectural snapshots; new nominations are the intended
  difference. Real invalidation must still reject stale generations/bytes.
- **K4 — matched arms, bounded experiment.** One control then one candidate
  use separate fresh Chrome processes/contexts, the same frozen built binary,
  exact R3 pair/base and recorder, with only the recycling selection different.
  Control failure does not silently prevent the one candidate observation.
  No saturation trigger, extra warm-up after qualification, hidden physical
  prewarm, renderer experiment or probe-policy sweep is introduced. Record
  warm-up separately; this fixed-order pair is not a precise throughput study.
- **K5 — real readiness inside one startup envelope.** Each arm records
  coherent shipped restore, the actual desktop-ready event, mapped nonhidden
  Foot and a matching active-window identity before the one absolute
  navigation+300,000ms deadline. Generic guest-ready/restored or visible old
  pixels alone are insufficient. The event's current source is
  `web/main.js:1936–1940`: successful compositor layers plus actual desktop
  pixels. Asset waits, implicit readiness RPC, explicit clients/active-window
  queries and focus preparation cannot each receive a fresh300-second budget.
  A startup timeout is retained as input-not-tested/inconclusive, not a
  120-second input negative; neither deadline nor predicate is relaxed.
- **K6 — physical causality.** After qualification, the existing trusted DOM
  key sequence reaches the focused Foot through ordered keyboard/sync calls.
  The nonce file's independently generated name does not contain the nonce;
  serial operations only read it and do not create, echo or inject success.
  Readback must come from the real framed guest response, not command echo,
  a stale file, requested environment or worker metadata. Record physical
  input and its acknowledgements in both arms, including failures.
- **K7 — unchanged input deadline and fresh presentation.** Record typing
  duration separately, bounded to60,000ms including per-key focus checks at
  the unchanged40ms key delay, and fix the120,000ms readback deadline at completion of
  Enter, before any post-Enter focus/diagnostic round trip. Every queue wait
  and read-only lookup consumes the remaining deadline; a late result fails.
  This matters because the existing path at
  `tools/verify/omarchy-desktop-live.mjs:1094–1099` checks focus before starting
  its relative readback timer. The trial must not gain that time silently.
  Require real presentation progress beyond the pre-typing baseline and the
  final actual screenshot within the declared20,000ms capture allowance;
  failure capture/cleanup cannot become another unbounded guest wait.
- **K8 — outcome does not choose the rules.** Candidate success requires the
  unchanged physical nonce/readback and real post-input presentation/screenshot
  evidence. Fewer refusals or more compilation alone cannot pass. If both
  arms qualify and control fails input while candidate passes, report a
  bounded local improvement, not proof of renderer ownership or the entire
  stall's cause. If control never qualifies, any candidate pass is qualified
  candidate acceptance, not a matched input-latency speedup. If both time out
  before input, input efficacy is inconclusive; if candidate fails after
  valid qualification, preserve that negative and stop this policy trial.
  Both passing does not establish a benefit over control. No outcome here
  automatically changes defaults, deploys, or authorizes Epic6 work.

Prelaunch contract assessment: no objection to the symmetric300-second bound.
The concrete safeguards to carry into the new harness are one end-to-end
startup deadline, the Enter-anchored input deadline, and explicit startup
timeout classification. These preserve the stated contract rather than
weakening it based on either result. Only this prediction log was edited;
status and implementation remain coordinator-owned.

### 2026-09-14 UTC — critic — fixed-bound refinement before either arm

Accept the proposed absolute300s startup,60s physical typing,120s readback
anchored immediately at Enter,20s final capture and30s cleanup. The sum is a
530-second maximum per arm from navigation through cleanup, not permission
to transfer unused time or restart a phase deadline. Post-Enter focus checks
consume readback time; timed-out work cannot later produce an accepted result.
Typing failure is distinguished from a completed physical sequence whose
readback fails, and the prior startup-timeout/input-not-tested classification
is unchanged. Each arm closes before the next; cleanup only owns its own
browser/process and cannot trigger another measurement or guest command.

For the proposed `{enabled, epochs, discardedCounters}` API, predict that
`epochs` counts cold-counter recycling operations independently of code
generation, and `discardedCounters` counts removed counter-map entries, not
their summed hit counts or globally distinct PCs. Keep these counters lossless
in JS/JSON and give their reset/lifetime scope explicitly. The adapter's
explicit local-only guard must reject non-loopback opt-in without changing
the ordinary disabled path. These are pre-result interpretation/boundary
checks, not a review verdict on the still-in-progress helper implementation.
