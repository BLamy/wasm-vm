---
id: E5.5-T03x
epic: 5.5
title: Keep measured integer-to-float conversions in compiled renderer blocks
priority: 525.02692
status: verified
depends_on: [E5.5-T03w]
estimate: S
risk: high
capstone: false
---

## Boundary

Compile FCVT.S.W/WU/L/LU through one pure call to the existing integer-only
`softfloat::f32_from_int` backend. Pass the integer source, width/signedness
and resolved rounding mode; return packed raw result bits and flags through
the existing verified FPR/FP-control handoff. Reuse the precise FS/rounding
guards established for arithmetic. Other conversion directions and arithmetic
families stay interpreted. No new arithmetic implementation or Epic 6 work.

T03s measured 985,870 from-integer conversion retirements in 99,998,678
instructions. The saved renderer page contains 18 dynamic-rounding instances
alongside the now-supported moves, memory, comparisons and arithmetic. T03w's
actual desktop trial still produced no nonce or typed response at 120 seconds.
These conversions remain unsupported by whole-block admission. Start only
after independent T03w verification.

## Deterministic acceptance

`make verify-E5_5-T03x`

- Actual native, private-WASM and shared-WASM generated modules agree with the
  interpreter and independent literal results/flags for all four integer widths,
  zero, signed extrema, unsigned high bits, exact integers and halfway cases.
  Exercise all five static rounding modes and dynamic frm selection, source
  and destination boundary registers, integer x0 versus writable f0, sticky
  flags, NaN-boxed results and unchanged integer source/register state.
- FS-Off and reserved static/dynamic modes preserve the exact original parcel,
  virtual PC and retired prefix, with zero helper calls. Prove same/cross-module
  successors, budget boundaries, interpreted CSR changes, later memory faults
  and actual browser memory growth. Audit optional import/function indices in
  mixed arithmetic/conversion/integer modules and preserve unsupported families.
- The helper receives no hart, bus, memory, device or scheduler reference.
  Keep integer-only module behavior unchanged. Record the high-risk prescribed
  local submission, existing platform limitations, a compiled conversion guest,
  all 127 live ISA tests, zero browser errors, exact source/artifact/guest hashes
  and one final pristine-clone acceptance. Refresh generated roadmap task data,
  surface the capability and deploy the committed bundle through Cloudflare.
- Repeat the actual physical-keyboard test once at its original 120-second
  deadline. Inspect independent nonce readback and the actual terminal image.
  Keep T03q gated unless both succeed; conversion support is not usability.

## Adversarial verification

A fresh critic predicts independent literal rounded bits/flags before viewing
evidence; vary signs, high integer bits, rounding, aliases and prior flags.
Count helper calls on legal versus illegal instructions, attack X-register
handoff before a direct conversion successor and stale flags after a CSR clear,
audit every changed import/ABI hunk, and sabotage one expected literal in an
isolated test copy. Carry unchanged HELD FP evidence forward. Inspect the final
pristine rebuild, public artifact bytes and actual physical trial separately.

## Verification log

### 2026-09-15 — coordinator — next measured renderer boundary

T03w's built guest executes 3,649/4,000 instructions via JIT, matches interpreter
registers and spilled FP results, and passes 127/127 live ISA tests. The actual
desktop at source `d64b037b362b36490d58b82663a448ac0532b025` and WASM
`e84c821a12fa5782e229614b9bd3f5a37713e7f3e4d1d38788ac194a7a039db4`
accepted 128 trusted input events but failed the unchanged deadline at
`2026-09-15T21:46:39.143Z`. Frames remained 2→2 and the real Foot image stayed
at an empty prompt. Receipt:
`evidence/omarchy-profile/fp-arithmetic-r1/physical-input/desktop/report.json`,
SHA-256 `c20fe7de2adb79c581367529f52d942b535d43f4d15e257dfa58cb4ee8cc8b85`.
Use the original T03s measured counts; no speedup is inferred from this trial.

### 2026-09-15 — worker — activated after independent arithmetic verification

T03w is independently verified at `51dd9864` and published as open draft
PR #391. Its actual desktop input result remains failed. This slice begins
the four measured FCVT.S.W/WU/L/LU operations through the existing software
backend, with a fresh critic session and the original physical-input deadline.

### 2026-09-15 — worker — frozen conversion submission

Runtime, promoted independent fixtures and built artifact are frozen at
`b587faf6f1bee5db45c5c9368d0a748f9badaff4`; final deployment/planning head and
pristine-clone source are `5f759ef96773b93e2b1faff27183bf6e66213fea`.
The latter changes no runtime or test source. WASM SHA-256:
`0ce4a5a304d82574b5f4118725bcffc2547121605e49304d280153eae4a5d312`.
Worker evidence is `evidence/omarchy-profile/fp-from-integer-r1/`, with all
46 files sealed by `sha256.txt` (index SHA-256
`4cc96e781b67301c1bf3713705818e28f0a168c47956147522834f310ba5731f`).

Commands: `python3 evidence/omarchy-profile/fp-from-integer-r1/record-submission.py
affected`, then the same recorder with `ci`; `node
tools/verify/omarchy-desktop-services.mjs
evidence/omarchy-profile/fp-from-integer-r1/physical-input`;
`bash tools/deploy-cloudflare.sh`; `python3
evidence/omarchy-profile/fp-from-integer-r1/check-public.py`; and `python3
evidence/omarchy-profile/fp-from-integer-r1/run-cold.py`. All use
`DEVELOPER_DIR=/Library/Developer/CommandLineTools`. Exact subprocess commands,
exits and timestamps are in the adjacent JSON receipts and logs.

Focused Clippy, handoff, existing FP/fault regressions, all 23 native F/D ISA
ELFs, 33 native WASM-library tests, no-host-float and `make verify-E5_5-T03x`
passed normally. The worker corpus has 21,504 cases and register/flags FNV
`55175ee7c3836290`; actual CSR changes and faults add 384 cases plus three
machine-run-loop traps. Independent promoted fixtures cover 12,672 literal
states, 3,584 seeded states including 1,736 illegal exits, 3,072 instrumented
helper cases with zero illegal calls, mixed import/export indices, budgeted
same/cross-module successors and actual browser memory growth. Their predictions
and sabotage/restoration receipts are in the sibling verifier directory.

The built guest and final pristine clone both execute 3,611/4,000 instructions
via JIT, match interpreter and literal spilled results/flags, pass 127/127 live
ISA tests with zero errors and show the live conversion pip. Root inspected all
six built/cold screenshots. The clone began pristine with the documented
environment scrub, rebuilt the exact committed WASM and passed its complete
acceptance at `23:01:04Z`. Both Cloudflare origins (`d3e13537` and the production
alias) returned exact WASM/glue/roadmap/app bytes in all eight TLS checks.

Full `make -k ci` remains exit 2: inherited macOS wvseccomp/all-feature Clippy,
default-WASM resume, zicsr-stub test build and determinism-scanner failures are
preserved verbatim. Native ISA regression passes 128/128 and uncontended
performance smoke passes at 25.3 MIPS against 15. The critic independently
matched all five failed targets to unchanged prior boundaries; this is not a
claim that full CI is green.

The real physical-input trial **failed**: 128 trusted events, no nonce by
`22:40:14.941Z` (unchanged 120,000 ms), 13 completed reads returning 75 and one
pending, frames 2→2 and byte-identical empty Foot images. Raw report SHA-256:
`29b15c67cd97e1d8daac22b4bf449ba7bd35f3ec5fd6703e8a1e8054fdd7f1fa`.
No task-owned heavy process competed with it. T03q remains gated and T03y is
only a pending measured follow-up. This submission claims precise compiled
integer-to-float execution; it does not claim a responsive desktop or speedup.


### 2026-09-15 — verifier — VERDICT: verified

Independent review: `evidence/omarchy-profile/fp-from-integer-verifier/final-review.md`.
Submission `e0f2e712050182d956cba83bff23b5a1c104e8cc`; runtime `b587faf6`,
pristine/deployed source `5f759ef9`. `final-audit.json` SHA-256
`94322a0132953da3da9e39573f76ccc4f5072d45580a038a62c63a1cfdea7543`
checks all 46 sealed worker files against index SHA-256
`4cc96e781b67301c1bf3713705818e28f0a168c47956147522834f310ba5731f`.
No implementation edits were made by the verifier.

- P1–P2 — HELD. Independent literal/flags corpus: 12,672 states per actual
  native/private/shared path, receipt `11332523716468034169`. Seeded constructed
  midpoint/alias corpus: 3,584 states including 1,736 illegal exits, receipt
  `8412752937936467894`. Full integer bits, width/sign, x0/f0, rounding,
  NaN boxing, unchanged registers, FS/SD and sticky flags match predictions.
- P3–P5 — HELD. 3,072 instrumented generated executions make exactly 1,620
  legal conversion calls and zero illegal calls. Twelve CSR/fault cases retain
  receipt `5904181122689446553`. Actual mixed helper/function indices,
  integer predecessor handoff, sixteen same-/cross-module budget/fault cases
  and six genuine 65,536-byte private/shared memory-growth cases pass. Exact
  original parcels, virtual PCs and retired prefixes hold, including real
  Machine run-loop integration. See `cold/acceptance.log:117–140,235–275`.
- P6 sensitivity — HELD. One wrong literal in an isolated fixture fails at
  `CRITIC_FCVT_POSITIVE_HALF_GOLDEN`; restoration passes. `sabotage.json`
  records exits 101/0 and unchanged production fixture SHA-256.
- P6 provenance — HELD. The clean, scrubbed final clone rebuilds exact WASM
  `0ce4a5a304d82574b5f4118725bcffc2547121605e49304d280153eae4a5d312`
  and passes `make verify-E5_5-T03x` normally. Both built/cold guests retire
  3,611/4,000 via JIT, match registers/actual spills/flags and RAM digest
  `5df84ec4c17598ab2c56293bb5b78338947ce9991af18e0ef795aa4a07d2df44`,
  pass 127/127 with zero errors and show the live conversion pip. All six
  browser images were independently viewed; eight fresh public TLS byte
  fetches match the tested bundle. The cold rebuild's four local-vs-R2 boot
  manifest URL rewrites retain identical artifact hashes/sizes.
- P6 physical measurement/gating — HELD; actual desktop response FAILED.
  At the original deadline `2026-09-15T22:40:14.941Z`, 128 trusted accepted
  events yield no nonce; 13 reads return 75 and one is pending. Frames stay
  2→2 and independently viewed initial/failure Foot images are byte-identical
  empty prompts. No serial nonce injection occurs. Report SHA-256 is
  `29b15c67cd97e1d8daac22b4bf449ba7bd35f3ec5fd6703e8a1e8054fdd7f1fa`.
  T03q stays pending; this verdict makes no desktop responsiveness claim.
- COVERAGE/SUITE — Every changed implementation hunk is executed or explicitly
  statically waived in `provisional-review.md`. Seventeen unchanged T03t/u/v/w
  boundaries carry forward. Fourteen inherited CI boundaries are checked
  independently: full CI remains exit 2 on the same five failed targets;
  native ISA 128/128 and 25.3 MIPS smoke pass. Promote the three independent
  fixtures and acceptance target, including the fixed seed and pure-helper,
  direct-chain and growth checks. No unresolved T03x finding remains.

Commands: recorded native/WASM fixture and isolated sabotage commands;
`check-public.py`, `audit-captures.py`, `audit-ci.py`, and `final-audit.py` in the
verifier directory; policy check, queue regeneration and verifier commit.
