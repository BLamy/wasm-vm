---
id: E5.5-T03m
epic: 5.5
title: Test the real Omarchy keyboard path without pre-input diagnostic RPCs
priority: 525.025997
status: verified
depends_on: [E5.5-T03l]
estimate: S
risk: medium
capstone: false
---

## Boundary

T03l removes hidden IDE/agent traffic. Its actual page emits desktop-ready at
211.898 seconds, but the recorder then invokes `hyprctl clients` (and would
invoke `activewindow`) before sending any key. That diagnostic command does
not finish within the remaining71 seconds. A real user can type as soon as
the overlay clears and does not first issue those two commands.

This makes the result startup-qualification-negative, not measured keyboard
latency. Test that distinction with one direct-user-path recording, not a
runtime tuning experiment or a claim that the existing product is fixed.
The substantive success evidence remains physical guest input, independently
verified effects, and actual newly rendered GUI pixels.

## Scope

Change only the browser proof harness/tests. For the bounded input trial,
omit the recorder-only `clients`/`activewindow` pre-input RPCs. Preserve them
where separately required for image capture/renderer attestation. Do not
change the app's real layers-plus-pixels readiness check, UI overlay, guest,
renderer, clocks, JIT policy, agent lifecycle or persistence.

Begin after the actual desktop-ready event, inspect/capture the visible desktop,
click its real canvas and prove DOM focus. Keep the300-second navigation-to-
input-start,60-second typing, Enter-anchored120-second readback,20-second final
capture and bounded owned-browser cleanup limits. No serial injection of the
nonce, readiness synthesis, injected guest result, fake pixels or deadline reset.

## Deterministic acceptance

`node --test tools/verify/omarchy-user-input.test.mjs`

- Exercise the actual recorder startup-selection path. Input trial does not
  call mapped-client/active-window RPCs; the separate capture/attestation path
  retains its checks. Do not test an unused predicate.
- Real application readiness, focus, physical key emission and independent
  nonce lookup remain ordered, and the existing fixed deadlines remain intact.
- Success requires the unpredictable nonce in its independently named guest
  file, actual trusted key/Worker input evidence and a fresh presentation after
  input. The inspected screenshot must show a genuine application response;
  an unchanged prompt plus unrelated clock/cursor activity is not sufficient.
- Errors/timeouts are retained as failures and owned resources close. Test
  harness-only changes deterministically; do not rebuild unchanged native code.

Record exactly one actual R3 user-path attempt after source freeze, default
recyclingOFF/LP1/divider64 and existing JIT policy. Bind served runtime bytes
to T03l's reviewed runtime and preserve the original failure alongside it.
No ordinary boot is needed merely to reconfirm that host painting is not the
sole delay; this run must actually test the distinct physical-input path.

## Adversarial verification

Fresh Daybreak predicts before the run whether omitting diagnostic preconditions
preserves end-to-end input proof. Inspect actual wire bytes: before keys only
the app's legitimate layers readiness query may execute; the nonce itself must
never reach serial input. Falsify with one bounded fixture attack (missing key,
late readback, stale frame, or manufactured nonce). Inspect actual PNGs and
carry unchanged T03l source/CLI/control checks forward. Do not reinterpret a
negative result as a renderer cause or authorization for Epic6.

## Verification log

### 2026-09-14 UTC — coordinator — pending proof-gap plan

Read-only proposal while T03l awaits final independent verdict. Actual evidence:
`evidence/omarchy-profile/desktop-services-r1/` (runtime `ed550aa8`, recorded
head `3f80aa9c`). No implementation or further guest boot has started.

### 2026-09-14 UTC — coordinator — activation and recorded critic predictions

T03l was independently verified for service isolation only in `0ed53c55`.
Daybreak reviewed this pending plan before implementation and found no
plan-critical blocker. Its pre-result predictions, recorded from the critic's
response, are:

- Before typing, raw serial has only bootstrap CR and the app's real layers
  query; no recorder clients/activewindow request or nonce.
- Actual readiness, visible terminal and canvas focus precede physical keys
  within the existing startup limit. Record the ready-to-first-key interval.
- Success needs ordered trusted keys/Worker acknowledgements, independently
  named-file nonce readback within Enter+120s, and visible application response,
  not just clock/cursor frame increments.
- Removing the diagnostic preconditions may permit typing but does not predict
  success. Failed readback still leaves guest execution, focus and serial
  observation as alternatives. Preserve capture/attestation checks elsewhere.

Implementation scope is the two initial probe guards and source-driven
recorder tests. Reuse the existing bounded runner with a fresh output directory;
no new runtime configuration, build, image or ordinary guest boot is authorized
by this task beyond its single recorded direct-user-path attempt.

### 2026-09-14 UTC — worker — IMPLEMENTED, harness correction; desktop negative

Frozen recorder/test commit: `81cab94ea874e2f177d1005887e2adf2435e3777`.
Runtime/dist is unchanged from T03l's `ed550aa8`. The two input-trial guards
remove only the recorder's initial mapped-client/active-window queries;
capture/verify preserve them. Final collection derives the ready-to-first-key
interval from existing same-page events, with no extra guest query.

Commands:

```
node --check tools/verify/omarchy-desktop-live.mjs
node --test tools/verify/omarchy-user-input.test.mjs tools/verify/omarchy-desktop-services.test.mjs tools/verify/omarchy-input-trial.test.mjs tools/verify/omarchy-owned-trial.test.mjs tools/verify/omarchy-desktop-live.test.mjs
node tools/verify/omarchy-desktop-services.mjs evidence/omarchy-profile/user-path-input-r1
node evidence/omarchy-profile/user-path-input-r1/audit.mjs
```

All39 deterministic recorder tests passed; the initial sandbox-only HTTP listen
failure and permitted retry are documented in `user-path-input-gates/PLAN.md`.
Scoped diff check passed. No unchanged native/CLI/runtime gauntlet was repeated.
Fresh Daybreak's pre-run review found no material blocker.

Exactly one actual R3 attempt completed, exit1 with normal owned cleanup and no
watchdog. Evidence is `evidence/omarchy-profile/user-path-input-r1/`: terminal
`run.json`, `desktop.log`, full `desktop/report.json`/`serial.log`, actual PNGs,
`RESULT.md`, and offline `audit.mjs`/`audit.json`. The audit binds95 served bodies
to frozen runtime/source/artifact identities and validates128 trusted DOM events
through256 matched keyboard RPCs. Actual ready occurred at211.499s; first key at
231.458s, within300s. Physical typing took3.537s, with no diagnostic pre-input
RPC except the application's real layers readiness query.

The experiment is negative: seven independent reads reported file missing,
the eighth was unfinished at Enter+120s, and the recorder retained failure at
deadline+2ms. No nonce was supplied over serial. Actual restored input budget256
and before/after pending/drop/reject counters0 are recorded without substituting
the fresh-device default. True RPC acknowledgements are not guest-consumption
proof. Frames remained2→2 with no host backlog. Coordinator personally viewed
the actual desktop and final failure PNGs: the same bar/Foot prompt, no command
or application response. Both hash to
`97fc180d4d35c68ca5941dc591afb315220550165469f3c4ead7827989cc2f3f`.

Claim: the corrected harness now tests real physical input and faithfully
retains its failure; it does not fix or verify responsiveness. T03d remains
unresolved. No production/image/R2 mutation or new runtime optimization follows
from this result. Await independent Daybreak verdict on the bounded harness task.

### 2026-09-14 UTC — independent critic — VERDICT: verified

VERDICT: verified — the bounded harness correction and faithful failed
physical-input observation, not desktop responsiveness. Reviewed the complete
contract before the run, the actual `runLive` diff/tests, frozen recorder
`81cab94ea874e2f177d1005887e2adf2435e3777`, and the worker claim submitted at
`bb37137f67c25e0ccc743e2b2d606ff06e0c9871`. T03l's unchanged service, Channel,
CLI, controls and cleanup HELDs carry forward. No additional boot was run.

- **M1 — HELD: actual user path, not diagnostic preconditions.** The two
  `!inputTrial` guards at `tools/verify/omarchy-desktop-live.mjs:1119` and
  `:1129` remove only the initial mapped-client/active-window RPCs. Independent
  preflight ran all6 source-driven tests successfully; capture/verify retain
  mapped Foot, active-window and selected renderer checks. Independently
  reconstructed every actual serial-input byte: one bootstrap CR, the app's
  real layers query, then eight read-only nonce-file lookups. No clients,
  activewindow, hidden IDE, agent send or serial nonce appears. Layers request
  `mu115fr60` was sent09:17:59.683 UTC and completed09:21:15.342, exit0;
  the first lookup was sent09:21:38.843, after physical Enter. This is a
  distinct exercised input path, not reinterpretation of T03l's no-key run.
- **M2 — HELD: real readiness/focus and unchanged timing.** The real app ready
  event was211499.425ms; first trusted keydown231458.225ms, a19958.800ms
  gap. Navigation began09:17:43.844, so first input was inside the original
 300s cap. The actual pre-input PNG shows unobstructed bar/Foot; DOM events
  target and focus `ide-display-canvas`. All128 trusted, non-repeat down/up
  events match the precise physical strokes for the independently generated
  nonce command plus Enter, including Shift. Each maps in order to its exact
  EV_KEY type/code/value and following sync:128 keyboard calls and128 syncs,
  with unique same-worker IDs and matching true/error-null RPC responses.
  The seven pointer RPC acknowledgements also match their actual calls.
  Keyboard typing took3537ms, below60s. `desktop/report.json:27694` anchors
  Enter/readback at09:21:38.832 and the original deadline09:23:38.832;
  failure is recorded atdeadline+2ms, not a later success. The last nominal
  per-RPC timeout is2815ms although its subsequently recorded timestamp has
  2814ms remaining: source samples remaining before recording the request.
  The unchanged outer absolute-deadline race still controls acceptance. No
  timer reset or extended input window is credited.
- **M3 — HELD: success is not manufactured; actual outcome is negative.**
  Reparsed exact request/response fences independently of the worker audit.
  Seven completed lookups returned exit75 with empty payload, at09:21:48.173,
  09:22:05.479,09:22:22.199,09:22:38.853,09:22:55.982,09:23:15.080 and
  09:23:35.013 UTC. The eighth (`mu11cn9x8`) was sent09:23:36.022 and has
  no complete response before the deadline. In the raw report the last
  completed fence is at`:27031`; the final request echo is at`:27287`.
  None of these serial commands supplies the nonce. The actual report retains
  `verified:false`, `nonce-readback-failed`, `result:failed` and
  `classification:UNPROVEN` (`:27562`, `:27694`). I viewed the final PNG;
  desktop and failure images are identical bar/Foot prompt, with no typed
  command or application response. Actual frames/presents remain2→2 and no
  host presentation backlog appears (`:270`, `:1333`). Neither a hash match,
  GUI presence nor RPC completion is treated as input acceptance.
- **M4 — HELD: attribution remains bounded.** The two actual input-state
  samples (`desktop/report.json:95`, `:1158`) both record budget256,
  pending events/frames0, dropped events/frames0 and rejected events0.
  All263 input-method acknowledgements mean RPC completion only:
  `web/loader.js:1184` returns after invoking the WASM method, while
  `crates/wasm/src/lib.rs:3808` ignores `inject_event`'s Boolean result.
  The counters give no recorded host rejection/drop/backlog; they do not
  identify Linux, compositor or terminal consumption. Snapshot restore assigns
  the saved budget at `crates/core/src/dev/virtio/input/snapshot.rs:250`, so
  fresh-browser budget2048 must not replace the observed256. Empty pending
  state alone is not an end-to-end delivery witness: event completion occurs
  before used-ring publication at `input/mod.rs:1010`, and device reset can
  clear pending state at`:525`. No such failure/reset is asserted here.
  The interval advanced657973737 retired instructions and1.028084 guest
  seconds; prior matched divider1 negatives remain HELD, not grounds for
  another timer sweep. Removing pre-input IPC allowed the trial to type but
  did not produce accepted input within120s. No renderer cause or architecture
  requirement follows.

**Independent bounded novel attack — HELD.** In memory only, reused the actual
`runLive` source fixture and suppressed synthetic KeyP guest effects while
leaving the physical press promises and synthetic DOM records successful.
The independent lookup then failed, `keyboard.verified` stayed false, no
success result or success PNG was published. This exercises the missing-effect
case rather than treating successful host calls as a guest oracle. It launches
no browser/guest and changes no source. Existing permanent tests additionally
reject wrong nonce, late typing/readback, missing readiness/focus and stale
presentation. The fixtures explicitly describe their synthetic guest/pixel
data; actual wire and inspected PNGs, not fixtures, support the recorded
negative. This satisfies the medium-risk novel-attack boundary.

**Identity, coverage and evidence sufficiency — HELD.** Independently checked
95 served responses against their frozen body sizes/hashes/routes,86 browser
requests against that inventory, all seven recorder-helper pins and the exact
R3 kernel/RAM/delta/chunk-manifest bytes. Actual restore is true, the base has
16384 split chunks, and the changed agent helper is genuinely fetched. Source,
dist and Rust are unchanged from reviewed T03l runtime `ed550aa8`; actual
WASM SHA-256 remains
`9405d6c38be9a170ef5a2e5e0bacdcbec6deace7de48c8e002bc3caea76dfb2b`.
Both runtime samples retain divider64, JIT/cache4096/repack-off24,
recyclingOFF/probeOFF and the same Omarchy generation1. LP1 is inherited
from the pinned R3 pair, not freshly GL-attested. The changed input-trial
branches and final timing-collection hunk execute in this actual recording;
the preserved opposite branches and error guards are source-driven fixtures.
The39-test worker gate includes the disclosed EPERM-only listener failure and
permitted retry; no runtime semantics changed between them. The new timing
fields are passive observations, not acceptance generators. Normal owned
cleanup completed; parent and child exit1, watchdog null. No native build,
CLI repeat or portability/cold-clone proof is needed for this harness-only
boundary. The post-run audit is evidence integrity, not an input-success gate.

Full evidence SHA-256 values, independently recomputed:

- `user-path-input-r1/run.json`:
  `4342868fbd2de53ec2ac71e9c49842273d19d4be34c406ef5b660107abe5ef98`.
- `user-path-input-r1/desktop/report.json`:
  `00ee2657553d1e9111f3f4e535a2534267b6fdf6d9724a826ce6690d994438d2`.
- `user-path-input-r1/desktop/serial.log`:
  `afa7714b40c5ee696e837bce1aa90518e333665c5ee4cabdeb7b8bc01865fd48`.
- `user-path-input-r1/desktop/desktop.png` and `desktop/failure.png`:
  `97fc180d4d35c68ca5941dc591afb315220550165469f3c4ead7827989cc2f3f`.

**SUITE / disposition.** Retain the6 actual-source selection/deadline/nonce/
presentation tests, unchanged owned-recorder tests, and the actual negative
wire/PNG bundle. No new implementation test or runtime fix is authored by
this critic. No remaining proof gap for the submitted harness claim. T03d
responsiveness remains unresolved; no deployment, default promotion, extra
boot or Epic6 expansion is authorized. Critic writes are limited to this
task's status/log and the requested regenerated queue; coordinator owns Git.
