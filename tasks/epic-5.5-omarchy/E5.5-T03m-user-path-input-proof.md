---
id: E5.5-T03m
epic: 5.5
title: Test the real Omarchy keyboard path without pre-input diagnostic RPCs
priority: 525.025997
status: implemented
depends_on: [E5.5-T03l]
estimate: S
risk: medium
capstone: false
---

## Boundary

T03l removes hidden IDE/agent traffic. Its actual page emits desktop-ready at
211.898 seconds, but the recorder then invokes `hyprctl clients` (and would
invoke `activewindow`) before sending any key. That diagnostic command does
not finish within the remaining71 seconds. A real user can type as soon as
the overlay clears and does not first issue those two commands.

This makes the result startup-qualification-negative, not measured keyboard
latency. Test that distinction with one direct-user-path recording, not a
runtime tuning experiment or a claim that the existing product is fixed.
The substantive success evidence remains physical guest input, independently
verified effects, and actual newly rendered GUI pixels.

## Scope

Change only the browser proof harness/tests. For the bounded input trial,
omit the recorder-only `clients`/`activewindow` pre-input RPCs. Preserve them
where separately required for image capture/renderer attestation. Do not
change the app's real layers-plus-pixels readiness check, UI overlay, guest,
renderer, clocks, JIT policy, agent lifecycle or persistence.

Begin after the actual desktop-ready event, inspect/capture the visible desktop,
click its real canvas and prove DOM focus. Keep the300-second navigation-to-
input-start,60-second typing, Enter-anchored120-second readback,20-second final
capture and bounded owned-browser cleanup limits. No serial injection of the
nonce, readiness synthesis, injected guest result, fake pixels or deadline reset.

## Deterministic acceptance

`node --test tools/verify/omarchy-user-input.test.mjs`

- Exercise the actual recorder startup-selection path. Input trial does not
  call mapped-client/active-window RPCs; the separate capture/attestation path
  retains its checks. Do not test an unused predicate.
- Real application readiness, focus, physical key emission and independent
  nonce lookup remain ordered, and the existing fixed deadlines remain intact.
- Success requires the unpredictable nonce in its independently named guest
  file, actual trusted key/Worker input evidence and a fresh presentation after
  input. The inspected screenshot must show a genuine application response;
  an unchanged prompt plus unrelated clock/cursor activity is not sufficient.
- Errors/timeouts are retained as failures and owned resources close. Test
  harness-only changes deterministically; do not rebuild unchanged native code.

Record exactly one actual R3 user-path attempt after source freeze, default
recyclingOFF/LP1/divider64 and existing JIT policy. Bind served runtime bytes
to T03l's reviewed runtime and preserve the original failure alongside it.
No ordinary boot is needed merely to reconfirm that host painting is not the
sole delay; this run must actually test the distinct physical-input path.

## Adversarial verification

Fresh Daybreak predicts before the run whether omitting diagnostic preconditions
preserves end-to-end input proof. Inspect actual wire bytes: before keys only
the app's legitimate layers readiness query may execute; the nonce itself must
never reach serial input. Falsify with one bounded fixture attack (missing key,
late readback, stale frame, or manufactured nonce). Inspect actual PNGs and
carry unchanged T03l source/CLI/control checks forward. Do not reinterpret a
negative result as a renderer cause or authorization for Epic6.

## Verification log

### 2026-09-14 UTC — coordinator — pending proof-gap plan

Read-only proposal while T03l awaits final independent verdict. Actual evidence:
`evidence/omarchy-profile/desktop-services-r1/` (runtime `ed550aa8`, recorded
head `3f80aa9c`). No implementation or further guest boot has started.

### 2026-09-14 UTC — coordinator — activation and recorded critic predictions

T03l was independently verified for service isolation only in `0ed53c55`.
Daybreak reviewed this pending plan before implementation and found no
plan-critical blocker. Its pre-result predictions, recorded from the critic's
response, are:

- Before typing, raw serial has only bootstrap CR and the app's real layers
  query; no recorder clients/activewindow request or nonce.
- Actual readiness, visible terminal and canvas focus precede physical keys
  within the existing startup limit. Record the ready-to-first-key interval.
- Success needs ordered trusted keys/Worker acknowledgements, independently
  named-file nonce readback within Enter+120s, and visible application response,
  not just clock/cursor frame increments.
- Removing the diagnostic preconditions may permit typing but does not predict
  success. Failed readback still leaves guest execution, focus and serial
  observation as alternatives. Preserve capture/attestation checks elsewhere.

Implementation scope is the two initial probe guards and source-driven
recorder tests. Reuse the existing bounded runner with a fresh output directory;
no new runtime configuration, build, image or ordinary guest boot is authorized
by this task beyond its single recorded direct-user-path attempt.

### 2026-09-14 UTC — worker — IMPLEMENTED, harness correction; desktop negative

Frozen recorder/test commit: `81cab94ea874e2f177d1005887e2adf2435e3777`.
Runtime/dist is unchanged from T03l's `ed550aa8`. The two input-trial guards
remove only the recorder's initial mapped-client/active-window queries;
capture/verify preserve them. Final collection derives the ready-to-first-key
interval from existing same-page events, with no extra guest query.

Commands:

```
node --check tools/verify/omarchy-desktop-live.mjs
node --test tools/verify/omarchy-user-input.test.mjs tools/verify/omarchy-desktop-services.test.mjs tools/verify/omarchy-input-trial.test.mjs tools/verify/omarchy-owned-trial.test.mjs tools/verify/omarchy-desktop-live.test.mjs
node tools/verify/omarchy-desktop-services.mjs evidence/omarchy-profile/user-path-input-r1
node evidence/omarchy-profile/user-path-input-r1/audit.mjs
```

All39 deterministic recorder tests passed; the initial sandbox-only HTTP listen
failure and permitted retry are documented in `user-path-input-gates/PLAN.md`.
Scoped diff check passed. No unchanged native/CLI/runtime gauntlet was repeated.
Fresh Daybreak's pre-run review found no material blocker.

Exactly one actual R3 attempt completed, exit1 with normal owned cleanup and no
watchdog. Evidence is `evidence/omarchy-profile/user-path-input-r1/`: terminal
`run.json`, `desktop.log`, full `desktop/report.json`/`serial.log`, actual PNGs,
`RESULT.md`, and offline `audit.mjs`/`audit.json`. The audit binds95 served bodies
to frozen runtime/source/artifact identities and validates128 trusted DOM events
through256 matched keyboard RPCs. Actual ready occurred at211.499s; first key at
231.458s, within300s. Physical typing took3.537s, with no diagnostic pre-input
RPC except the application's real layers readiness query.

The experiment is negative: seven independent reads reported file missing,
the eighth was unfinished at Enter+120s, and the recorder retained failure at
deadline+2ms. No nonce was supplied over serial. Actual restored input budget256
and before/after pending/drop/reject counters0 are recorded without substituting
the fresh-device default. True RPC acknowledgements are not guest-consumption
proof. Frames remained2→2 with no host backlog. Coordinator personally viewed
the actual desktop and final failure PNGs: the same bar/Foot prompt, no command
or application response. Both hash to
`97fc180d4d35c68ca5941dc591afb315220550165469f3c4ead7827989cc2f3f`.

Claim: the corrected harness now tests real physical input and faithfully
retains its failure; it does not fix or verify responsiveness. T03d remains
unresolved. No production/image/R2 mutation or new runtime optimization follows
from this result. Await independent Daybreak verdict on the bounded harness task.
