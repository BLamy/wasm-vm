---
id: E5.5-T03g
epic: 5.5
title: Measure the isolated Omarchy renderer-thread setting
priority: 525.0258
status: cancelled
depends_on: [E5.5-T03f]
estimate: S
risk: high
capstone: false
---

## Boundary

Diagnostic prerequisite to T03d, not an asserted remedy. Test Mesa's existing
`LP_NUM_THREADS=0` setting on a fresh graphical session of an isolated copy of
the same verified SDR image. Do not change the emulator, packages, permissions,
GPU architecture, production artifacts, or input deadline. No Epic 6 work.

## Acceptance criteria

- Change only the two `LP_NUM_THREADS=1` values to `LP_NUM_THREADS=0` in
  `/etc/environment.d/60-omarchy-browser.conf` and
  `/home/omarchy/.config/uwsm/env`. Bind the original image, two changed bytes,
  resulting image/chunk manifest and unchanged inode metadata.
- Use a fresh real-emulator session. Bind kernel/core/image identities and
  the actual compositor PID's environment. A startup timeout is unproven,
  not a negative compatibility result. Preserve the 90-minute cold-start
  budget separately from input measurement.
- If startup succeeds, capture a paired RAM/disk snapshot and run the actual
  built browser's physical nonce/readback test with screenshots. Keep the
  existing 120-second deadline and use no serial writes of the nonce.
- Compare against the corrected, unchanged LP1 baseline r2 from T03f. A
  failed input run establishes only that the exact LP0 configuration did
  not fix responsiveness; do not invent a renderer label or mechanism.
  A crash requires a PID-bound terminal record. Missing evidence is unproven.
- A successful or materially improved LP0 result is provisional: before
  attributing improvement or publishing, repeat matched clean LP1/LP0 arms
  with startup DEBUG logging identically enabled, capture each measured
  compositor's strict GL labels, then identically disable logging before
  timing physical input. An auxiliary EGL client cannot identify the
  compositor's active renderer.
- A fresh critic audits the scoped measurement before this prerequisite
  can become verified. A verified negative diagnostic is not a desktop fix.

## Verification command

The worker will add a bounded deterministic evidence gate and reproducible
recording recipe before submission, following T03f's frozen measurement
infrastructure. Do not activate this task before T03f is verified.

## Adversarial verification

Reject edits beyond the two declared bytes, inherited warm processes that
never read the changed environment, changed logging between timed arms,
missing image/pair identity, environment-only renderer attribution, fabricated
pixels, serial-written nonces, or an expanded physical-input deadline.

## Verification log

### 2026-09-14 UTC — coordinator — experiment retired by user direction

After the full-budget run and independent `needs-evidence` verdict, the user
answered “continue fix this” to the explicit proposal to retire this
inconclusive experiment and move to desktop responsiveness within Epic 5.5.
Cancel this experiment; do not promote it to verified or a negative
compatibility finding. All raw evidence and the critic's proof gaps remain
intact below. E5.5-T03h resumes bounded diagnosis on the unchanged shipped
LP1 pair; T03d retains responsibility for the eventual measured remedy.
No repeat LP0 boot, production mutation, or Epic 6 expansion is authorized by
this planning change.

### 2026-09-10 — worker — startup attempt UNPROVEN; no submission

Prepared the exact two-byte LP0 candidate and launched the native capture at
frozen head `0d3e609629e45021357b0e07f09ab70f9765edf8`. The executable and
unchanged kernel/WASM plus harness closures are recorded in
`evidence/omarchy-profile/thread-r1/runtime-identities.json`. The run exited 1
on the five-minute individual probe timeout before the 90-minute outer budget.
`native-capture.log:1007` has `capture_52_begin` without its END; line 1008 is
the host timeout. Prior clients probe status is 6, with the last actual
Hyprland instance reporting PID 407. No mapped desktop/pair was produced, so
no LP0 built-input measurement or screenshot exists. This is not a crash or
negative compatibility finding. See `thread-r1/native-result.json` for exact
log sizes/digests and `thread-r1/RESULT.md` for the remaining proof gaps.

Focused evidence-gate tests are recorded in `thread-r1/gate-tests.log`; they
validate the real preparation receipt and HELD baseline, reject this actual
incomplete native recording, and exercise synthetic RPC/header mutations.
They do not establish a desktop fix or end-to-end seal coverage. The last
actual screenshot inspected is the corrected LP1 baseline's `failure.png`,
not an LP0 screenshot. Daybreak Blue's fresh audit request returned HTTP 401
(model access unauthorized), so there is no new critic verdict. Status stays
`in-progress`; no production publish, merge, or Epic 6 work occurred.

### 2026-09-10 — coordinator — staged diagnostic plan only

The fresh critic accepted the conditional two-arm scope above. It avoids
unnecessary extra logging-enabled boots for a negative input result but
requires them before a positive renderer/mechanism attribution. No image
has been prepared, no test started, and no product improvement is claimed.

Mesa documents the existing setting at
<https://docs.mesa3d.org/envvars.html>; it is not evidence that it will help
this VM. The measured 4.6% integer-division profile component cannot by itself
account for the minutes-long input delay, so no small CPU microbenchmark is
being substituted for actual desktop acceptance.

Before this task starts, the critic accepted a native capture followed by the
bound pair on the actual built page. Pin the native executable's SHA-256 as
well as the unchanged core source boundary; the package-version snapshot ID
alone is not an exact-head executable binding. Validate the actual compositor
PID's unique `LP_NUM_THREADS=0` before creating the pair.

For the unlogged first LP0 arm, the built harness may report only
`lp0-configuration-observed`, with `activeRendererValidated:false`, when that
same current PID has the unique exact llvmpipe/LP0 environment, a blank GL-label
stream, and no exact `llvmpipe-N` worker. Present labels remain subject to the
strict parser. Softpipe, ambiguous variables and worker-present observations
must fail this fallback. This enables a measurement, not renderer attribution
or acceptance of a positive result without the matched labelled arms above.

### 2026-09-10 — verifier — pre-result predictions

These predictions were recorded after the frozen protocol launched but before
inspection of any startup or input result:

- P1 image confinement: complete source/candidate comparison will find exactly
  two `0x31` to `0x30` byte changes at the declared LP assignments, with the
  pinned source hash and unchanged inode metadata. Candidate and chunk hashes
  will reassemble to the same bound image.
- P2 fresh execution: before pair creation, one current compositor PID will
  have unique exact `GALLIUM_DRIVER=llvmpipe`,
  `LIBGL_ALWAYS_SOFTWARE=1`, and `LP_NUM_THREADS=0` values and no exact
  `llvmpipe-N` worker. Kernel, core, image, native executable, and capture
  scripts will match their frozen identities. Timeout alone will be UNPROVEN.
- P3 classification: a blank GL-label stream may produce only
  `lp0-configuration-observed` with `activeRendererValidated:false`. Any
  present label must pass the strict compositor parser; requested environment
  values alone cannot establish the active renderer.
- P4 input authority: if startup succeeds, the bound pair will reach a mapped
  desktop before an exact 120-second physical nonce/readback arm. The nonce
  will appear only in browser keyboard events, never serial writes, and the
  report will bind the before/after screenshots and independent readback.
- P5 conclusion scope: failed readback means only that this exact LP0
  configuration did not fix responsiveness. A crash needs same-PID terminal
  evidence. Improvement remains provisional until the predeclared matched,
  identically logged LP1/LP0 arms; no production, mechanism, GPU-architecture,
  or Epic 6 claim follows from this run.

### 2026-09-13 — verifier — cold built-page plan and pre-result predictions

Model access is restored. Read-only review at `d36647b8` covered this task,
`thread-r1/RESULT.md`, and the existing built-page cold boot, readiness, stored
restore observation, and paired export paths. The native R1 timeout remains
UNPROVEN. T03f and unchanged prior HELD evidence remain carried forward; this
entry makes no new verdict. These predictions precede the proposed cold
built-page launch and inspection of its results.

Use one local-only cold-pair capture on the actual built page, taking only the
unchanged pinned LP0 candidate chunks and kernel, with no input RAM/disk pair.
Use a fresh browser context, record the origin's empty IndexedDB before the
app starts, and request `noSnapshot=1&persist=1`. Persistence supports paired
export after startup. Keep the 90-minute startup budget; prove the actual
mapped desktop and current compositor settings before exporting the pair,
without prewarm/physical keys. Then run the existing separate built-page
paired-restore verification with its unchanged 120-second physical-input
deadline and actual screenshots. Native capture is not a required producer
of the pair under the acceptance criteria. Freeze the revised recording
helper and plan before launch; emulator, kernel and guest settings stay at
their declared boundaries.

- P6 cold identity: the recording will bind the same candidate, chunk manifest,
  kernel and core identities, empty pre-app storage and the cold URL flags.
  After controller initialization, the actual controller's
  `await window.__linuxCtl.storedSnapshotRestoreEvidence()` will report
  `attempted:true, decision:"missing"`, with its observed overlay generation
  retained, and `restoredFromBootSnapshot()` will be false. Missing APIs or a
  failed observation cannot stand in for this negative restore evidence.
- P7 startup and classification: within the 90-minute startup budget, a real
  mapped desktop and one current compositor PID will satisfy P2/P3's exact
  unique llvmpipe/software/LP0 environment and no exact `llvmpipe-N` worker
  checks. Blank GL labels permit only `lp0-configuration-observed` with
  `activeRendererValidated:false`; present labels remain strict. Startup or
  observation timeout alone remains UNPROVEN, with no compatibility finding.
- P8 pair authority: successful startup will produce a coherent RAM/disk pair
  bound to the actual candidate loader base and overlay generation before any
  physical input or prewarm sequence. A separate fresh built-page run will
  restore that exact pair, report restored state and prove a mapped desktop
  before the existing 120-second physical nonce/readback measurement.
- P9 result scope: P4/P5 still apply. Independent readback and actual GUI
  screenshots will support the input result; no serial write may create the
  nonce. A failed completed input arm establishes only that this exact LP0
  configuration did not fix responsiveness. Improvement remains provisional
  pending the already-declared matched labelled arms; incomplete startup,
  pair export or observation provides no input-performance conclusion.

Concrete recording risks: `noSnapshot` disables the shipped pair but the
persistent loader still attempts a stored snapshot (`web/loader.js:766`), so
use its saved load-time observation, not the later live `snapshotDecision()`.
The real readiness watcher still sends 300-second quiet `hyprctl layers` RPCs
(`web/main.js:1928`); its caught timeout does not terminate boot or prove the
guest command ended. Record those implicit commands, avoid another startup
polling loop, and require completed fencing for subsequent probe evidence.
The old capture branch performs physical nonce/calendar prewarm before export
(`tools/verify/omarchy-desktop-live.mjs:980`); the new cold-pair branch must not
inherit that extra input prerequisite or extend the separate input deadline.

### 2026-09-14 UTC — verifier — VERDICT: needs-evidence

Bounded audit of the completed `thread-r2/cold-built-r2` recording at
`30b76b4fe886db040aabf584bf79b82bc484412b`, including the screenshot-only diff
from `af36874149d3a8c53f878e8a5d42e507028c24bc` and the preflight rerun receipt.
No new guest boot, implementation change, or product verification was performed.
All report line numbers below refer to the immutable 9,824,954-byte
`evidence/omarchy-profile/thread-r2/cold-built-r2/report.json`, SHA-256
`766f19c3dad6f59a2bec769899fe60bdabd38514be953b3f7cc2916f8e94303f`.

- P6 identity/fresh-origin portion — HELD. Revalidated all 305 preflight runtime
  records against local bytes and their frozen identities, including the exact
  295-file tracked dist inventory. Only `omarchy-desktop-live.mjs` changed
  between the first and rerun preflight runtime records. Rehashed the prior
  receipt, preparation receipt, corrected LP1 report and candidate manifest;
  their bindings are unchanged. Checked all 814 recorded HTTP 200 bodies:
  82 runtime resources, 727 content-addressed chunk responses and 5 generated
  blank/manifest responses. Every browser request has a matching recorded 200;
  there are no snapshot/delta requests. Report line 417314 binds candidate
  manifest SHA-256
  `38e8d3144e2e0be26290763a8fd1a712e93b70fc39f93131d80ed3937cbb09fc`;
  line 417319 binds kernel
  `af7c4e471ed4dabdbe5a2717d81cc034b511d2b0f7706de66ad9e84e078c7cce`;
  line 417414 binds WASM
  `c48e9c2d9ec550c7daf4875716fef1dc729072fdfc91b805394d379bee4b9305`.
  Empty pre-app storage is recorded at line 417353, before the startup clock;
  the single worker boot begins at line 13100.
- P6 explicit controller restore observation — NEEDS EVIDENCE. Predicted
  `storedSnapshotRestoreEvidence()` with `attempted:true, decision:"missing"`
  and restore=false. Neither `restoreOutcomes` nor `restored` exists in the
  report. Frozen `omarchy-desktop-live.mjs:1046` reads them only after desktop
  readiness; execution instead exits at line 1039, as report line 417426
  records. Cold flags and empty storage are useful evidence but do not replace
  the predeclared controller observation. Obtain that observation in a future
  authorized measurement; do not label this portion HELD.
- P7 startup budget — HELD; compositor/startup proof — NEEDS EVIDENCE. Report
  lines 417366–417370 give 2026-09-13T22:43:42.402Z through
  2026-09-14T00:13:42.402Z, exactly 5,400,000 ms. The deadline exception at
  line 417426, failure screenshot at line 920 (00:13:42.546Z), and report
  finish at line 417459 (00:13:47.307Z) show deadline expiry followed by
  evidence finalization. `progressCaptureErrors` is empty at line 417304;
  this attempt was not cut short by the previous screenshot/probe timers.
  The complete lifecycle event array at line 417428 has guest-ready at
  page elapsed 3,405,555.735 ms (line 417455), but no desktop-ready. No current
  compositor PID/environment, strict GL labels, or mapped desktop was proven.
  Preserve UNPROVEN; further startup/PID evidence is needed for that claim.
- P7 raw IPC interpretation — HELD as an observation, not a crash finding.
  Independently decoded all 74 serial-input records: 3 cursor-position replies
  and 71 exact framed commands. One `ls -la '/root'` completed with exit 2;
  69 `hyprctl -i 0 -j layers` commands completed with `no such instance` and
  exit 1; the last such command was incomplete. First layers command
  `mu0gj88g1` is sent at report line 290404 and closes at 292642. Last
  completed command `mu0hodsd69` is sent at 413962 and closes at 416465;
  final command `mu0hpcld70` is sent at 416774 with no completed fence.
  These IPC results do not establish PID death, an ABRT, or renderer failure.
- P8 paired restore/input authority — NEEDS EVIDENCE. The report has no pair,
  keyboard, renderer or coldDesktop result, and the output directory contains
  only `report.json`, `serial.log`, `latest.png` and `failure.png`. I viewed
  `failure.png`: black display with the actual "Starting Omarchy desktop"
  dialog, no Foot/bar desktop. Its binding is report lines 920–922; its bytes
  equal the final `latest.png`. The conditional pair/input branch was never
  reached, so there is no 120-second LP0 input result to compare with LP1.
  A bound pair plus the separate physical-input arm remains necessary if
  startup succeeds; this timeout does not discharge that measurement.
- P9 conclusion scope — HELD. Report lines 417425–417427 say failed/UNPROVEN.
  `inputEvents` is empty (line 13098), and all 31,088 worker records contain
  zero keyboard/mouse/tablet send or sync calls. This means zero physical
  input, not zero guest traffic: normal serial/readiness and agent traffic
  are recorded. No LP0 usability, comparative performance, active-renderer,
  crash-causality, production, or Epic 6 conclusion follows.
- COVERAGE — startup routing, empty-origin observation, actual boot, progress
  screenshots and deadline cleanup executed. Post-readiness restore/PID/GL
  probes, mandatory desktop screenshot, coherent pair export and restored
  physical-input execution remain NEEDS EVIDENCE. The new progress-timeout
  catch was not exercised in this actual attempt; its already-reviewed
  synthetic regression coverage remains HELD as harness coverage only.
  Carry forward the exact-overlay selector/blocks-store and screenshot
  preflight results, T03f, and unchanged P1/preparation/LP1 evidence. Do not
  rerun their unrelated gates or count synthetic tests as guest proof.
- Bounded attacks — reparsed the last completed real RPC after removing its
  END fence in memory: it becomes incomplete, not a successful result.
  Passing the actual missing stored-restore observation to `assertColdRestore`
  is rejected. Neither attack changes the retained artifacts.
- Lifecycle — `evidence-needed`, not `verified` or `refuted`. The full bounded
  attempt is auditable, while the required measurement remains incomplete.
  T03d remains dependency-gated. No automatic repeat boot or wider gate is
  requested by this verdict; changing the experiment/dependency plan requires
  an explicit planning decision. The coordinator froze the raw evidence at
  `7c254283` and authorized a subsequent commit of only this critic entry,
  task status and generated queue change.

Additional full SHA-256 bindings (paths relative to
`evidence/omarchy-profile/thread-r2/`):

- `preflight-rerun.json` (98,277 bytes):
  `f91f036c9df252a88b3a4f79dca84d5418edc44027dc6045e9b0acba8aae4a0f`.
- `cold-built-r2/serial.log` (38,409 bytes):
  `9ae939e178e9f09ee19c2b79eb78121491a92ceb01da6bd3caf42c04c2e4358b`.
- `cold-built-r2/failure.png` and `cold-built-r2/latest.png` (21,977 bytes each):
  `8d5e1fcb91c1c5aa44899488e589b42a0cb4914c5238f1a2763054f7267edfa6`.
- `cold-built-r2.log` (194,311 bytes; deadline exception at line 360):
  `bdeb93d4bc5301d9a4ee64391af9ebb941ae4c7b434ee1afc89bc1a733f4a690`.
- `harness-rerun-tests.log` (3,001 bytes; retained 30-test worker result):
  `7aab1b84315d78a0bb2b5abf88db887727b5502357c213868b518b72eb43749d`.

Checks: scoped frozen diff; `validateRuntimeIdentities` with the preflight
policy and an independent exact dist-file inventory comparison; reference and
served-body SHA-256 checks; `createFencedRpc` replay of actual worker traffic;
in-memory missing-fence/missing-restore attacks; direct final PNG inspection.
`python3 tools/check_task_policy.py` passed with T03g as the sole active task;
`python3 tools/build_queue.py` regenerated 479 tasks / 305 verified, changing
only T03g's queue marker. Scoped `git diff --check` passed.
