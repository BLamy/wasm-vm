---
id: E5.5-T03g
epic: 5.5
title: Measure the isolated Omarchy renderer-thread setting
priority: 525.0258
status: pending
depends_on: [E5.5-T03f]
estimate: S
risk: high
capstone: false
---

## Boundary

Diagnostic prerequisite to T03d, not an asserted remedy. Test Mesa's existing
`LP_NUM_THREADS=0` setting on a fresh graphical session of an isolated copy of
the same verified SDR image. Do not change the emulator, packages, permissions,
GPU architecture, production artifacts, or input deadline. No Epic 6 work.

## Acceptance criteria

- Change only the two `LP_NUM_THREADS=1` values to `LP_NUM_THREADS=0` in
  `/etc/environment.d/60-omarchy-browser.conf` and
  `/home/omarchy/.config/uwsm/env`. Bind the original image, two changed bytes,
  resulting image/chunk manifest and unchanged inode metadata.
- Use a fresh real-emulator session. Bind kernel/core/image identities and
  the actual compositor PID's environment. A startup timeout is unproven,
  not a negative compatibility result. Preserve the 90-minute cold-start
  budget separately from input measurement.
- If startup succeeds, capture a paired RAM/disk snapshot and run the actual
  built browser's physical nonce/readback test with screenshots. Keep the
  existing 120-second deadline and use no serial writes of the nonce.
- Compare against the corrected, unchanged LP1 baseline r2 from T03f. A
  failed input run establishes only that the exact LP0 configuration did
  not fix responsiveness; do not invent a renderer label or mechanism.
  A crash requires a PID-bound terminal record. Missing evidence is unproven.
- A successful or materially improved LP0 result is provisional: before
  attributing improvement or publishing, repeat matched clean LP1/LP0 arms
  with startup DEBUG logging identically enabled, capture each measured
  compositor's strict GL labels, then identically disable logging before
  timing physical input. An auxiliary EGL client cannot identify the
  compositor's active renderer.
- A fresh critic audits the scoped measurement before this prerequisite
  can become verified. A verified negative diagnostic is not a desktop fix.

## Verification command

The worker will add a bounded deterministic evidence gate and reproducible
recording recipe before submission, following T03f's frozen measurement
infrastructure. Do not activate this task before T03f is verified.

## Adversarial verification

Reject edits beyond the two declared bytes, inherited warm processes that
never read the changed environment, changed logging between timed arms,
missing image/pair identity, environment-only renderer attribution, fabricated
pixels, serial-written nonces, or an expanded physical-input deadline.

## Verification log

### 2026-09-10 — coordinator — staged diagnostic plan only

The fresh critic accepted the conditional two-arm scope above. It avoids
unnecessary extra logging-enabled boots for a negative input result but
requires them before a positive renderer/mechanism attribution. No image
has been prepared, no test started, and no product improvement is claimed.

Mesa documents the existing setting at
<https://docs.mesa3d.org/envvars.html>; it is not evidence that it will help
this VM. The measured 4.6% integer-division profile component cannot by itself
account for the minutes-long input delay, so no small CPU microbenchmark is
being substituted for actual desktop acceptance.

Before this task starts, the critic accepted a native capture followed by the
bound pair on the actual built page. Pin the native executable's SHA-256 as
well as the unchanged core source boundary; the package-version snapshot ID
alone is not an exact-head executable binding. Validate the actual compositor
PID's unique `LP_NUM_THREADS=0` before creating the pair.

For the unlogged first LP0 arm, the built harness may report only
`lp0-configuration-observed`, with `activeRendererValidated:false`, when that
same current PID has the unique exact llvmpipe/LP0 environment, a blank GL-label
stream, and no exact `llvmpipe-N` worker. Present labels remain subject to the
strict parser. Softpipe, ambiguous variables and worker-present observations
must fail this fallback. This enables a measurement, not renderer attribution
or acceptance of a positive result without the matched labelled arms above.
