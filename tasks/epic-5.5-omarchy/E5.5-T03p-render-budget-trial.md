---
id: E5.5-T03p
epic: 5.5
title: Measure Omarchy physical input with a smaller guest scanout
priority: 525.026
status: in-progress
depends_on: [E5.5-T03o]
estimate: S
risk: medium
capstone: false
---

## Boundary

One local tooling-only trial of the existing display-mode API on the exact R3
snapshot and unchanged production WASM/image. Request 640×400 once after the
real desktop becomes available. This quarter-area mode tests the rendering-work
hypothesis exposed by T03o; no renderer, timing, JIT or architecture change.
The browser canvas is not a substitute for an actual smaller guest resource.

## Deterministic acceptance

`node tools/verify/omarchy-render-budget.mjs evidence/omarchy-profile/render-budget-r1`

- Bind the existing R3 artifacts, actual served sources and frozen helpers.
- Record the original guest scanout, one actual setDisplay(640,400) RPC and its
  acknowledgement. Require a new guest frame and bound 640-pixel-wide scanout
  before calling the mode adopted; account explicitly for stride/height padding.
- Modeset observation uses the original absolute 300-second startup deadline.
  A modeset timeout remains input-not-tested, never a negative latency result.
- Once the mode is adopted, use unchanged physical typing, the independent
  filename/nonce, 120-second readback, fresh presentation and actual screenshot.
  No nonce bytes via serial, no fabricated frame or readiness, no timeout tuning.
- Record an honest bounded outcome: mode-not-observed, physical-input-failed,
  or physical-input-and-presentation-passed. Only the final outcome can support
  product promotion in T03q. Tooling acceptance does not mean desktop acceptance.
- Preserve owned-process cleanup and record no browser errors. Relevant helper
  tests reject an acknowledged mode without actual guest scanout/frame evidence,
  expired deadlines and wrong input bytes. No production build/deploy is claimed.

## Adversarial verification

A fresh critic predicts mode adoption and input result before inspecting the
actual recording, audits the single modeset RPC, original input deadline and
nonce wire bytes, and checks actual screenshots. Invent one bounded false-mode
receipt attack. Carry unchanged T03o runtime, R3 identity and input-harness
checks forward. Do not infer continuous waiting or renderer-fence ownership
from the two checkpoint observations alone.

## Verification log

### 2026-09-15 — worker — bounded rendering experiment activated

T03o completed independent verification at `2a165c3c`. Its actual input trial
failed; no production responsiveness claim has been made. This ordered S slice
uses one reversible existing guest modeset to distinguish reduced rendering
work from CSS-only scaling. T03q owns product changes and final live proof.

### 2026-09-15 — verifier — predictions before implementation recording

Fresh session `/root/render_budget_critic`; task read at `88a045a0`. No T03p run
receipt or screenshot has been inspected. The predictions below distinguish
experimental hypotheses from mandatory harness invariants.

- P1 mode hypothesis: the sole requested `setDisplay(640,400)` returns `true`,
  and a later guest frame reports a 640-pixel resource with a 400-pixel visible
  scanout (any taller storage padding is explicit) before the original
  navigation-start-plus-300000-ms deadline. An RPC acknowledgement, canvas
  dimensions or a repaint of the old resource alone cannot satisfy adoption.
- P2 latency hypothesis: after adopted mode and actual physical typing, the
  independent nonce file is still absent at Enter-plus-120000 ms. This prediction
  may fail without refuting tooling; only recorded successful readback plus fresh
  presentation and the actual screenshot can instead support a positive outcome.
- P3 invariants: the original R3 identities, production WASM and runtime settings
  match the held T03o evidence; only one explicit 640×400 modeset is sent; no nonce
  bytes enter the serial wire; DOM physical strokes and worker keyboard frames
  encode the exact independently generated command and Enter.
- P4 deadline and outcome: mode observation never receives a fresh 300-second
  budget; a missing/late new scanout yields `mode-not-observed` with physical input
  untested. An input result never obtains a longer readback budget. Cleanup closes
  the owned recorder/browser and the run records zero browser errors.
- P5 bounded novel attack: preserve an acknowledged 640×400 request and fresh
  frame/presentation counters, but replace the claimed adopted guest resource with
  the original 1280-pixel resource while leaving the canvas at 640×400. The mode
  receipt validator must reject this CSS-only false positive before promotion.

Coverage will be audited against the frozen diff, including timeout and
false-adoption branches through deterministic helper tests. The actual trial's
honest negative outcome is sufficient only for this diagnostic task, never for
desktop responsiveness or T03q promotion.
