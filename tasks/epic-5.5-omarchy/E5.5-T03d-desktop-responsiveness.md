---
id: E5.5-T03d
epic: 5.5
title: Resolve measured Omarchy post-restore desktop input latency
priority: 525.026
status: cancelled
depends_on: [E5.5-T02a, E5.5-T03c, E5.5-T03f, E5.5-T03h]
estimate: M
risk: high
capstone: false
---

## Planning container — decompose before implementation

The paired SDR desktop renders, but real physical input can take minutes to
appear. Preserve the recorded failure in
`evidence/omarchy-profile/mixed-renderer-candidate-r1/` and the fresh Daybreak
review. The discarded mixed-region partition, larger caches, eager admission,
and an isolated f32-multiply microbenchmark did not demonstrate a useful remedy.

Identify a measured cause and split the smallest remedy into ordered S tasks
with deterministic acceptance before activating anything. Keep the existing
120-second physical nonce acceptance in `omarchy-desktop-live.mjs`; do not
replace it with serial typing, a longer timeout, fake pixels or mere boot status.
Any Epic 6 GPU/architecture expansion requires the user's direction because they
explicitly asked to stop before that epic. Do not infer that one such expansion
is necessary solely from the current failed experiments.

## Acceptance criteria

- Recorded actual browser physical keyboard input produces an independently
  checked result within the unchanged existing acceptance limit.
- Real post-input presentations show the actual application response.
- Runtime/image changes are scoped, deterministic and independently reviewed.

## Verification log

### 2026-09-14 UTC — coordinator — authorized diagnostic replanning

The user approved retiring the inconclusive T03g renderer-setting experiment
and continuing the responsiveness fix. T03g is cancelled, not verified.
Replace that dependency with T03h's bounded investigation of the unchanged
shipped LP1 desktop. This container stays pending until a measured cause
supports decomposition into an atomic remedy and real-browser acceptance.
The existing 120-second physical-input gate and Epic 5.5 scope are unchanged.

Unresolved follow-up, not implemented or verified. E5.5-T03c only repairs the
separate visible-frame and host-side fitting failures.

### 2026-09-14 UTC — coordinator — direct input negative; no speculative remedy selected

T03m is independently verified at `6491d944` for its harness correction only.
Its one actual R3 attempt (`user-path-input-r1/`) removes diagnostic window
queries before input, reaches128 trusted key transitions, records256 matched
keyboard RPCs and no observed input queue drops/rejections, but never creates
the independently checked file within Enter+120s. Actual screenshots remain
the same prompt and frames2→2. This does not close responsiveness.

Read-only follow-up reused the authenticated historical c48 CPU analysis in
`evidence/omarchy-profile/softpipe-r1/symbols/profile-analysis.md`. It attributes
substantial sampled work to emulator execution; its `__udivti3` share4.556%
does not by itself support a minutes-to-responsive remedy. `advance_clock_by`
still has exact overflow-safe u128 arithmetic; no semantic bug was found.
No unbound historical profile was relabeled with current WASM function indices,
no named companion was generated, and no further boot or optimization ran.

Fresh Daybreak's bounded conclusion: no existing HELD evidence identifies
another substantive small fix. The remaining gap is a causal link from stalled
compositor input processing to a specific blocking operation or dominant path
that a bounded change can remove. This does not establish that a small fix is
impossible or that Epic6/GPU acceleration is necessary. This planning container
remains pending; the explicit stop-before-Epic6 boundary remains in force.

T03f is the isolated clean-session software-renderer measurement prerequisite.
This planning container remains unexecuted and must still be decomposed into
the smallest measured remedy before implementation.

T03g is a second bounded configuration measurement, gated on T03f. Its
conditional proof plan must distinguish a failed LP0 configuration from a
verified active-renderer/mechanism claim. This remains a planning container,
not authorization for a larger GPU or JIT architecture change.

### 2026-09-15 — coordinator — concrete checkpoint wait; later-interval probe remains

T03n adds an offline reader of the existing R3 checkpoint. Its frozen worker
run at `f3e0bba9` records exact task-list membership, page-table translations,
kernel frame chains and saved trap registers in
`evidence/omarchy-profile/checkpoint-wait-r1/checkpoint.json` (SHA-256
`1278b8bdd81471590bbb37cccdf6c392c09132fd0bfc24f274b26981cfb1b49a`).
T03n's own log owns its independent verification verdict.

At that earlier checkpoint, Hyprland PID417 is in a futex call on user word
`0x55555efbb948`, whose physical value is zero. Its llvmpipe TID462 is runnable
with a saved user interrupt context; Bash PID174 is the actual current task.
This is more specific than a wchan string, but does not bind a userspace caller,
wake dependency, or persistence of this wait into T03m's later failed input.
No guest run or runtime/image change accompanied the offline inspection.

The next diagnostic boundary is one coherent guest-state capture at the end of
the unchanged failed physical-input window, bound to the actual served runtime
and R3 restore. Compare PID/starttime, saved wait address and kernel/user context
with T03n before attributing the failure. Capturing after the fixed deadline
must preserve the negative verdict, not extend the input acceptance time.
Only a measured caller/work dependency supports selecting a remedy. This
container stays pending and the stop-before-Epic6 boundary remains unchanged.

### 2026-09-15 — coordinator — split into bounded rendering experiment and promotion

Replaced by ordered S slices E5.5-T03p and E5.5-T03q. T03o is independently
verified at `2a165c3c`: the actual failed-input endpoint preserves PID417's
saved futex wait while renderer TID462 is the only on-CPU task. The physical
nonce is still absent at exactly 120 seconds. Rendering workload is a testable
hypothesis, not an established complete explanation.

T03p requests the existing 640×400 guest display mode once, observes an actual
smaller guest scanout within the unchanged startup budget, then uses the same
physical-input acceptance. T03q promotes a mode only after it demonstrates
independent nonce readback and visible application response. This decomposition
does not claim that a lower resolution will solve the problem. Negative or
unsupported results must remain negative and trigger concrete replanning.
