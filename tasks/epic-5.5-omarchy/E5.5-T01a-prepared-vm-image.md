---
id: E5.5-T01a
epic: 5.5
title: Prepare a clean Omarchy image candidate from the existing RISC-V VM
priority: 525.01
status: verified
depends_on: [E5-T05c]
estimate: S
risk: high
capstone: false
---

## Goal

Turn the user's prepared Omarchy RISC-V VM into a separate, inspectable ext4
release candidate. Preserve the original live VM and remove personal state from
the candidate. This is the user's requested image-preparation priority, not an
Epic 6 task and not a claim that Omarchy already runs in the browser.

## Boundary

Own the consistent private source capture, copy-only preparation tools, clean
filesystem construction, package/image identities and local image checks.
Do not publish an unsanitized source image, change the live VM's accounts, carry
unallocated source disk blocks into the release, or bypass artifact integrity.
Browser/kernel integration and production publication follow this boundary.
Use the prepared VM rather than restarting the alternative checkout's proposed
base-from-scratch bootstrap. Do not claim bit-reproducible upstream provisioning.

## Acceptance criteria

- Capture is bound to the exact intended QMP disk, has no backing chain or
  concurrent block job, and restores the VM's prior run state even after a
  failed clone. Record consistency level and hashes of the immutable copy.
- A fresh filesystem contains the installed Omarchy/Hyprland/Quickshell software
  and pacman package database; it does not contain personal homes, bootstrap
  users, private host keys, tailnet identity, logs/history, caches or old free
  disk blocks. New demo account and machine identity policy are explicit.
- Preparation refuses unsafe targets and symlink escapes before mutation;
  deterministic tests cover these guards and replay/idempotence.
- Offline filesystem checks pass; exact size, image hash, package versions and
  content-addressed chunk identity are recorded. A second chunk verification
  reconstructs the exact image identity.
- Fresh Daybreak review interrogates the source/copy and public-state boundary.
  Anything not proven is retained as a release blocker, not called verified.

## Verification command

`python3 tools/image/verify-omarchy-image.py --image <candidate> --manifest <chunks/manifest.json>`

## Adversarial verification

Predict then test: wrong QMP disk, already-paused VM, clone failure, source
backing file, running block job; path traversal and symlinks into a host sentinel;
personal account/password/SSH/machine-id fixtures; repeated preparation; a
tampered chunk. Confirm the output is freshly constructed rather than copied
over a formerly personal ext4. Guest boot is a separate acceptance boundary,
not established by package names or a QEMU screenshot.

## Verification log

### 2026-09-09 — coordinator — started at merged main

Base `88457d4fc660ea4d64737799f02bbe97157da46c`. Source is the existing
`/Users/blamy/Virtual Machines/Omarchy-riscv/archriscv.qcow2`, confirmed by QMP
on QEMU 11.1.1 as an active 32 GiB qcow2 without a backing chain. No source
mutation has occurred. Separate Luna worker and Daybreak critic sessions are
assigned. E5-T26f is pending, not verified, to keep one active task.

### 2026-09-09 — worker — clean image candidate recorded

Implementation commit `7f453b8dfed8c49598cf6e6a89f9cf1d91aaf476`; final
builder, sanitizer and configurator identities are recorded in
`evidence/omarchy-preparation/build-receipt-r3.json`. The accepted private
crash-consistent capture was independently checked by Daybreak; its immutable
qcow2 SHA-256 is `6d292ee907d806f8914d6aa92f9797d925e8d5b9ac198ab8b6448edaf0303113`.
The original VM was resumed after a 9.4 ms pause. Recovery occurred only on a
separate copy, then read-only fsck passed; frozen source hashes were unchanged.

Final image: `releases/rootfs/omarchy.ext4`, 4,294,967,296 bytes,
SHA-256 `bfcde69e5eeb2b5baba96a451f0671092b5973e8cc3a7f581722adf513651200`.
The 389-package, 2,619,608,941-byte installed selection includes Omarchy 4.0.2-1,
Hyprland 0.56.2-3 and Quickshell 0.3.1-1. No source personal filesystem blocks
were used to populate this new ext4. Package mtree identity is not claimed as
independent upstream authentication; six source deviations are explicitly reviewed.

Recorded commands:

- `docker exec wasm-vm-omarchy-prep-20260909-v2 python3 /tools/build-omarchy-image.py /work/assembled-r3 /work/candidate-r3 --selection /work/package-selection.json --overlays /tools/omarchy-reviewed-source-overlays.json`
  — succeeds, including `pacman -Dk`, fresh `systemd-sysusers`, `grpck -r`,
  fresh theme/cache generation, unmount, `mke2fs -d`, and `e2fsck -fn`.
  Full commands/results: `evidence/omarchy-preparation/build-commands-r3.log`.
- `target/release/wasm-vm chunk releases/rootfs/omarchy.ext4 --out releases/chunked-omarchy --chunk-size 131072 --layout split`
  — 32,768 chunks.
- `python3 tools/image/verify-omarchy-image.py --image releases/rootfs/omarchy.ext4 --manifest releases/chunked-omarchy/manifest.json --receipt`
  — exact whole-image match; manifest SHA-256
  `372ea3d0d5611fb2fa26d9d3079b669131f94cde551ec52cb614314119688acb`.
  Receipt: `evidence/omarchy-preparation/chunk-receipt-r3.json`.
- `python3 -m unittest discover -s tools/image -p 'test_*.py'` — image-tool
  regressions run on macOS and as root in the network-disabled Linux container.
  Daybreak independently recorded 88 Linux tests, only the Mac APFS fixture skipped.
- `make ci` — attempted; stops on pre-existing macOS-incompatible Linux seccomp
  libc references and existing all-features dead-code warnings. No Rust runtime
  source changed in this image task. Full failure: `evidence/omarchy-preparation/local-ci-r1.log`.

Daybreak's incremental image verdict and all superseded findings are retained in
`evidence/omarchy-preparation/critic-export-plan.md`; the candidate-r3 verdict
accepts fresh-filesystem/privacy, ownership/capability, account coherence and
frozen-run receipt binding. Earlier r1/r2 candidates are not release artifacts.
Tooling must remain frozen/read-only during a build, with private staging and
no concurrent writers. A pristine-clone harness check follows the frozen head.

**Explicit downstream blockers, not waived:** the image reaches a demo-user
login in local QEMU; with modern virtio, Hyprland/Quickshell and a 1280×800 DRM
monitor start. First-run user provisioning is incomplete, optional fcitx5
restarts and the document portal fails. This is not browser/desktop acceptance.
Our native interpreter and JIT both fault systemd on a page-end misaligned load;
`native-interpreter-r1.log` and `native-interpreter-r1-digest.txt` retain the
failure (1,499,903,053 retired instructions; architectural digest
`a077c0ced8eed71fa5e715f66713bee5c13b99bf2062efc9481a8bf096c89c68`).
No Omarchy deployment, production-integrity or performance claim is made here.

### 2026-09-09 — fresh Daybreak verifier — VERDICT: verified

- **Private export consistency — HELD.** The exact QEMU 11.1.1 disk, backing-free
  qcow2 and empty block-job state were bound before pause; the APFS clone occurred
  between paused observations and the VM returned to running. The immutable private
  capture hash is `6d292ee907d806f8914d6aa92f9797d925e8d5b9ac198ab8b6448edaf0303113`.
  Failure, signal, reconnect, wrong-disk, version, inode-swap and native APFS COW
  attacks pass in the frozen helper suite. The consistency claim remains correctly
  limited to crash-consistent guest storage.
- **Fresh public filesystem/privacy — HELD.** Candidate-r3 was populated into a new
  zero-origin ext4 from the accepted package assembly and reviewed overlays, not by
  deleting from the personal filesystem. Carried and final inspections show locked
  identities, empty machine identity and runtime/state trees, no imported personal
  homes, histories, host keys, tailnet state or old deleted inodes, and preserved
  ownership/modes plus the reviewed `gst-ptp-helper` capability. Final group and
  gshadow sets both contain 59 names, passwd/shadow sets match, every password field
  is locked, and `grpck -r` exits zero.
- **Image/chunk identity — HELD.** The release is exactly 4,294,967,296 bytes with
  SHA-256 `bfcde69e5eeb2b5baba96a451f0671092b5973e8cc3a7f581722adf513651200`;
  read-only `e2fsck -fn` exits zero. A second verifier reconstructs all 32,768
  128-KiB chunks and records manifest SHA-256
  `372ea3d0d5611fb2fa26d9d3079b669131f94cde551ec52cb614314119688acb`
  in `evidence/omarchy-preparation/chunk-receipt-r3.json`.
- **Guards and coverage — HELD.** The network-disabled Linux-root verifier passes
  88 image-tool tests with only the macOS APFS test skipped; the macOS run passes
  the same 88 tests with nine platform skips. This covers wrong QMP state, cleanup
  and receipt failures, source/destination escapes, personal-state fixtures,
  package/overlay admission, input drift, Linux metadata/capabilities, repeated
  preparation and tampered chunks. Builder test SHA-256
  `95c84cca17e500d3e5f31eff5a944d6f31937c5e0921c9d01ff4c40726f8145f`
  is the test-only commit `58eadc97fbbadcb3a00a3d1180feaa08345a986c`;
  implementation hashes are unchanged from the accepted candidate.
- **Pristine clone — HELD.** At frozen worker commit
  `ae16b4a3da07baa3a00a23f5a33930d4695365a4`, a scrubbed macOS run in
  `target/omarchy-cold.Mdavmf/repo` passes 88 tests with nine skips, and a
  `--rm --network none` Linux-root container with the clone's `tools/image`
  mounted read-only passes 88 tests with only the APFS fixture skipped.
- **Suite:** retain all image-tool regressions, the final build/chunk receipts and
  `evidence/omarchy-preparation/critic-export-plan.md`. Browser desktop capture,
  publication, production integrity and the native cross-page core fault remain
  explicitly outside this task and are not implied by this verdict.
