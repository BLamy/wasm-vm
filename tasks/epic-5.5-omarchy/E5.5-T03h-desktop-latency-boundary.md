---
id: E5.5-T03h
epic: 5.5
title: Locate the stalled Omarchy desktop input boundary
priority: 525.0259
status: in-progress
depends_on: [E5.5-T02a, E5.5-T03c, E5.5-T03f]
estimate: S
risk: medium
capstone: false
---

## Boundary

Diagnostic only, using the unchanged shipped SDR/LP1 RAM-disk pair, kernel,
core and built page. Preserve existing failures and do not repeat the LP0
cold boot, software-renderer swaps, cache-size tuning, or failed mixed-region
partition. No emulator semantics, production publication or Epic 6 work.

## Acceptance criteria

- Bind the actual runtime, image manifest and restored pair. Distinguish
  existing evidence from the newly recorded session.
- Record physical keyboard input, screenshots and controller counters before
  and after a bounded observation, including host input delivery, virtual
  input queue state, guest instruction/clock progress and frame presentation.
- Collect completed, read-only, PID-bound guest process/thread state outside
  the timed input window where possible. Missing or timed-out observations
  remain explicitly unproven; never infer a crash from an IPC timeout.
- Identify the narrowest boundary supported by the measurements and one
  falsifiable next remedy hypothesis, or identify the precise missing probe.
  No claim of responsiveness follows from a diagnostic result.
- A fresh critic independently checks attribution and evidence sufficiency.
  The eventual remedy must separately pass the unchanged 120-second physical
  nonce/readback test and personally inspected actual GUI screenshots.

## Verification command

Use `tools/verify/omarchy-input-diagnostic.mjs` with the explicit unchanged
R3 pair and chunk directory. Record the exact invocation and command stream
in the evidence result. Any diagnostic-helper change requires focused tests
and a frozen head before its final recording; no runtime rebuild is required
for an unchanged built page. A deterministic receipt checker will bind the
collected result before submission.

## Adversarial verification

Reject serial-written nonces, clock/config changes, input acceptance inferred
from host events alone, guest consumption inferred from an empty host queue,
CPU attribution based only on an unbound PID or interpreted-PC samples,
and missing presentations passed off as a host rendering failure. Carry
forward unchanged HELD cases rather than repeating unrelated gates.

## Verification log

### 2026-09-14 UTC — coordinator — diagnostic activated

User direction: “continue fix this,” in response to the explicit request to
retire T03g and proceed within Epic 5.5. T03g's full-budget LP0 run remains
inconclusive; the shipped LP1 pair remains the rendered-desktop baseline
to diagnose, not a claim that input works. T03d is still a planning container.

### 2026-09-14 UTC — verifier — pre-result predictions

Read this task's diagnostic plan at `e372fe8e` before inspecting any new
session observations. The coordinator reports warm session 49803 using
`omarchy-input-diagnostic.mjs`, pair `target/omarchy-sdr-r3-snapshot`, chunks
`target/omarchy-profile-chunks-sdr-r3-256k`, divider 64 and JIT enabled. Those
are declared inputs to check, not newly verified facts. No verdict or status
change is made here; unchanged prior HELD evidence remains carried forward.

- P1 identity: the new recording will bind the actual loaded built runtime,
  kernel, chunk manifest and exact R3 RAM/disk pair, and report a restore.
  Its timestamps and output paths will distinguish it from the prior LP1
  failure. No configuration, clock, renderer or cache change will occur in
  this observation; requested arguments alone cannot establish these facts.
- P2 input/progress: observations bracketing the physical input will retain
  actual host delivery evidence, pending/drop/rejection counters, retired
  instructions, guest-clock progress and presentation counters. Record actual
  observation times, including delayed replies. Empty pending input means the
  emulator queue drained, not that Hyprland consumed a key; successful host
  delivery alone cannot establish a working desktop.
- P3 process evidence: completed guest reads will bind the current Hyprland
  PID, its renderer-worker TID where present, and Foot PID rather than assume
  old identifiers still apply. State/wchan samples and, where available,
  CPU/runqueue counter differences will describe the sampled interval.
  Missing, denied, disabled or timed-out counters remain missing evidence,
  not zero activity. Prefer these reads outside the timed input window; do
  not treat a serial request's issue time as its guest sample time.
- P4 attribution: prompt host delivery and continuing VM execution with
  absent frame progress will argue against a long host execution stall.
  Substantial main-thread runnable waiting supports a guest-scheduling
  hypothesis. Main-thread futex sleep with renderer-worker execution and
  little runnable waiting supports investigating a compositor wait dependency,
  but does not identify a specific LLVM fence or prove its ownership. Normal
  main-thread progress without a visible response leaves input consumption/
  forwarding as a candidate. Unbound PC samples or cumulative percent CPU
  alone cannot select among these explanations.
- P5 presentation: distinguish no new frame arriving from frames arriving but
  not being presented. The actual screenshots must agree with the reported
  stage; boot pixels or an unchanged desktop cannot stand in for application
  response. No inferred crash follows from a failed compositor IPC request.
- P6 diagnostic conclusion: the submission will name the narrowest supported
  boundary and one falsifiable remedy hypothesis, or the precise missing
  probe if attribution remains unresolved. This task does not require a
  successful input remedy to justify an honest diagnostic conclusion. Any
  eventual fix still requires its separate unchanged 120-second physical
  nonce/readback acceptance and inspected GUI screenshots.
