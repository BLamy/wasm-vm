---
id: E5.5-T03v
epic: 5.5
title: Compile measured single-precision comparisons with exact accrued flags
priority: 525.0269
status: pending
depends_on: [E5.5-T03u]
estimate: S
risk: high
capstone: false
---

## Boundary

Compile FEQ.S, FLT.S and FLE.S, including their integer result and accrued
invalid-operation flag, through the existing FPR/FS handoff. Keep rounded
arithmetic and double-precision comparisons outside this slice. This is one
comparison/state boundary, not a blanket FP implementation.

The verified renderer recording contains 1,713,705 single-precision comparison
instructions (OP-FP funct7 0x50) in 99,998,678 retirements. The retained live
renderer page contains FLE.S alongside the now translated single-precision
moves and FP memory transfers. T03u's actual keyboard retry still failed at
120 seconds with 128 accepted events, no nonce and no new frame. Remove this
next measured interpreter boundary, then repeat the actual application gate.

## Deterministic acceptance

`make verify-E5_5-T03v`

- Real generated native, private browser and shared browser modules agree with
  the interpreter and independent bit-pattern goldens for FEQ.S/FLT.S/FLE.S:
  both signed zeros, infinities, subnormals, opposite signs, ordinary positive
  and negative ordering, quiet/signaling NaNs and malformed single NaN boxes.
  Integer x0 remains zero; all source FPR bits remain unchanged.
- Preserve fflags as sticky state: FEQ's signaling-NaN invalid flag differs
  from FLT/FLE's any-NaN invalid behavior. Never erase an existing flag or alter
  frm. Successful comparison follows the interpreter's FS dirty transition,
  even if it writes x0. Reserved frm values do not trap a non-rounding compare.
- FS-Off returns the exact raw instruction and virtual fault PC after any
  completed prefix. Flag/FPR handoff stays coherent across interpreter changes,
  same-/cross-module successors, budget exits, subsequent memory faults and
  browser memory growth. Newly accrued flags survive the existing precise exit.
- Record the high-risk local submission and unchanged platform limitations,
  full live browser ISA plus a compiled comparison guest, exact source/artifact
  and guest-state hashes, and one final pristine clone. Update web/dist and the
  live capability through Cloudflare; no Actions or merges.
- Repeat the actual Omarchy physical-keyboard nonce test at the unchanged
  120-second deadline and inspect the real application screenshot. Keep T03q
  pending until independent readback and visible response both succeed.

## Adversarial verification

A fresh verifier predicts the zero/sign/NaN ordering and sticky invalid flag
before inspecting evidence. Independently vary operand seeds, signs, register
aliases, FS and frm. Test comparison flags immediately followed by another
compiled block, an interpreted CSR operation, a precise fault, and a memory
view refresh. Check every changed hunk, test a deliberate wrong golden value,
and inspect the pristine clone. Carry unchanged T03t/T03u proofs forward.

## Verification log

### 2026-09-15 — worker — plan the next measured boundary

T03u runtime `7048847fb542d66618b836b965b94c8c9db771d5`, WASM
`1f989758c67c9dfdf514d2e03c0e4fa2cadb15a2dc0a465f5cb538661446473b`,
passes the built FP memory guest and 127/127 live ISA suite. Its physical
keyboard run entered at `2026-09-15T19:31:51.262Z` and failed at the unchanged
`2026-09-15T19:33:51.262Z` deadline (callback recorded one millisecond later).
The 128 accepted input events produced no nonce or typed text, and presentation
remained at two frames. See
`evidence/omarchy-profile/fp-memory-r1/physical-input/desktop/report.json` and
`failure.png`. This task remains pending until T03u is independently verified.
The original histogram is
`evidence/omarchy-profile/renderer-opcodes-r1/report.json`, `recount.opFpFunct7`.
