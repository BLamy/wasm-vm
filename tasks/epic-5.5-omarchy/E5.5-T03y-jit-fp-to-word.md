---
id: E5.5-T03y
epic: 5.5
title: Keep measured float-to-word conversions in compiled renderer blocks
priority: 525.02693
status: pending
depends_on: [E5.5-T03x]
estimate: S
risk: high
capstone: false
---

## Boundary

Compile FCVT.W.S and FCVT.WU.S through the existing integer-only
`softfloat::f32_to_int` backend. A pure helper returns the low 32 result bits
and new flags in the existing packed u64 convention. Generated code sign-extends
the word result into an integer destination, including the architectural
sign-extension rule for WU, and accrues flags/FS state without changing any FPR.
Reuse precise FS/rm checks and source NaN-box handling. L/LU destinations,
double precision and remaining arithmetic keep their established paths.

T03s measured 426,264 retirements in the single-precision float-to-integer
family. That aggregate does not distinguish integer destination widths. A
static scan of the saved renderer page identifies eight FCVT.W.S instructions,
all with RTZ, among the previously measured FP operations. These still exclude
their whole blocks from JIT admission. W/WU share the one 32-bit result boundary;
this task does not introduce a 64-bit result/flags ABI. No Epic 6 work.

T03x's actual desktop trial again failed at the original 120-second deadline,
with no independently read nonce or typed terminal response. Start only after
T03x is independently verified.

## Deterministic acceptance

`make verify-E5_5-T03y`

- Actual native, private-WASM and shared-WASM generated modules match independent
  literal results/flags and the interpreter for W/WU, exact/fractional values,
  ties, signed zeros, subnormals, signed/unsigned boundaries, overflow clipping,
  infinities, quiet/signaling NaNs and malformed source boxes. Exercise all five
  static modes, dynamic frm and reserved modes, x0 destinations, source f0/f31,
  aliases, sticky flags and FS transitions even for an exact result discarded
  into x0. Preserve every source FPR and unrelated integer register.
- FS-Off and invalid rounding make zero helper calls and retain original parcel,
  virtual fault PC and completed prefix. Prove integer result consumption across
  same/cross-module successors, budgets, genuine interpreted CSR changes, later
  faults and actual browser memory growth. Instantiate actual mixed modules
  containing integer, arithmetic, from-integer and to-word operations, and audit
  every optional import/export/call index. Integer-only behavior stays unchanged.
- Record the prescribed high-risk local submission and existing platform
  limitations, a production built conversion guest, all 127 live ISA tests with
  zero browser errors, exact source/artifact/guest hashes and one final pristine
  clone. Surface the capability and generated task data, commit web/dist and
  publish the checked bundle through Cloudflare.
- Repeat the real physical-keyboard trial once at the unchanged 120-second
  readback deadline. Inspect the nonce and actual terminal image separately.
  Keep T03q gated unless both pass; instruction support is not desktop acceptance.

## Adversarial verification

A fresh critic predicts literal clipping, rounding and flag outcomes before
reading evidence. Independently vary operand bits, malformed boxes, rounding,
destination aliases and prior flags; attack zero-helper illegal paths, x0/FS
behavior, negative fractional WU inputs and sign extension above bit 31. Audit
mixed helper indices, changed register masks and fault publication; sabotage
one literal in an isolated test copy. Carry unchanged HELD evidence forward
and independently inspect cold/public identities and the physical result.

## Verification log

### 2026-09-15 — coordinator — remaining measured conversion boundary

T03x's source `b587faf6f1bee5db45c5c9368d0a748f9badaff4` and WASM
`0ce4a5a304d82574b5f4118725bcffc2547121605e49304d280153eae4a5d312`
pass the focused native/private/shared and actual built-browser acceptance;
the browser guest retires 3,611/4,000 instructions via JIT and the live suite
passes 127/127. Its physical trial accepts 128 trusted events but fails at
`2026-09-15T22:40:14.941Z`: no nonce, frames 2→2 and a byte-identical Foot image.
The physical report SHA-256 is
`29b15c67cd97e1d8daac22b4bf449ba7bd35f3ec5fd6703e8a1e8054fdd7f1fa`.
Evidence is under `evidence/omarchy-profile/fp-from-integer-r1/`; the static
instruction scan, its source hash and its limitations are recorded in
`saved-word-conversion-encodings.json`. The family count comes from the verified
`renderer-opcodes-r1/report.json`. These observations justify this bounded
next slice; they do not establish that it will solve responsiveness.
