---
id: E5.5-T03r
epic: 5.5
title: Request the smaller Omarchy mode explicitly in Hyprland
priority: 525.0265
status: in-progress
depends_on: [E5.5-T03p]
estimate: S
risk: medium
capstone: false
---

## Boundary

One isolated local trial on the unchanged R3 snapshot, image and production WASM.
T03p showed that changing the advertised display mode did not make Hyprland adopt
it. After that same single 640×400 display request, issue one fixed ephemeral
`hyprctl eval` monitor configuration command. No image/config-file writes,
compositor restart, timing change or renderer/JIT change. This is a bounded
measurement; a complete negative receipt is not a responsiveness fix.

## Deterministic acceptance

`node tools/verify/omarchy-compositor-mode.mjs evidence/omarchy-profile/compositor-mode-r1`

- Preserve the held R3/served-runtime identity and the original absolute
  300-second startup deadline. Issue exactly one display advertisement and one
  fixed Hyprland command requesting 640×400 at scale 1; bind its actual serial
  bytes, response/exit and timestamps. An error or timeout remains unproven mode.
- Reuse T03p's actual bound GPU scanout and fresh guest-frame checks; a successful
  command, preferred mode or CSS size alone never proves adoption.
- Only after actual adoption, run the unchanged physical-keyboard command with
  an independently generated nonce, 120-second independent file readback and
  fresh visible application presentation. No nonce injection through serial.
- Record command-failed/input-not-tested, mode-not-observed/input-not-tested,
  physical-input-failed, or physical-input-and-presentation-passed honestly.
  Only the last result permits T03q product work. Record real screenshots,
  browser errors and owned-process closure. No production deployment claim.

## Adversarial verification

A fresh independent critic predicts outcomes before inspecting the recording.
Carry unchanged T03p identity, frame, deadline and physical-input checks forward.
Audit the exact one-command wire and response; invent one bounded forged-success
attack with a rejected or missing compositor response. Conditional branches may
use explicitly synthetic helper tests; product success requires the real run.

## Verification log

### 2026-09-15 — worker — follow the measured compositor boundary

T03p was independently verified at `e251dc30` with actual mode adoption absent.
This new S slice tests the compositor's explicit mode selection before T03q can
publish any policy. The official Hyprland Lua monitor and `hyprctl eval` APIs
describe this dynamic configuration path; the actual guest response and resource
remain the authority for this trial.
