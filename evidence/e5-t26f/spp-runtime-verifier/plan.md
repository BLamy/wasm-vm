# E5-T26f after N — pre-recording critic plan

Date: 2026-09-08. Independent F critic, Daybreak Blue assignment.
State: planning only; no new browser evidence inspected. Main will commit this plan before launch.

## Prerequisite and scope

N is implemented and its fresh verifier is closing; it is not assumed verified. F remains blocked
until N's independent verdict permits Main to reactivate it. Main owns activation, the final F
head, build, one new cold checkpoint and one unprofiled reuse, and retention of every outcome.
This turn owns only this file. No execution, task/status/metadata/HEAD changes, or N re-verification
is authorized here.

Main's supplied N provenance: runtime freeze
`84fd5190832924a0f609b4a10d1bfaee2704dc0e`, test-only promotion
`b50491ceafc5be9029ee96dc9229085efd354e22`, submission `78b2a60a`.
The stated sole production addition is saved SPP bit 8 in cached context equality; its guest
safety verdict belongs to N's verifier. Expected served WASM SHA-256:
`84b2c17c9b6ab9d86c85912f82bd0b4b4533178fc27a0724d47565cb40974b4d`.
The final F activation head and N's final verdict remain pending handoff.

The existing proper runner and wrapper were read and rehashed during this planning pass:

- `tools/verify/e5-t26f-browser-roundtrip.mjs`:
  `7f8b58f7e3f6e0f16b7b6cb91b8593e625ed058e7aa764df428e9990f85100a1`.
- `tools/verify/e5-t26f-browser-single-process-observer.mjs`:
  `3be35665f7d403f9c291b5b81b1c07449ef526d171a53f5e7a69b087e79c416d`.

Carry the existing observer/image/helper, harness, and historical functional HELD conclusions
within their unchanged source, dependency, and evidence boundaries. Do not repeat their tests,
images, clones, or old browser runs. F's `657fb5a2` unprofiled endpoint
`3701.9950000047684 ms` remains FAILED; its separate latency record remains a sampled diagnostic.
All older runtime seals are invalid for this new served runtime and must never be rebound.

## Predictions registered before the next record

Each prediction awaits evidence. Later review will report HELD, FAILED, or NEEDS EVIDENCE with
actual file/line or JSON-field citations and SHA-256 digests. No timing outcome is assumed.

1. **P1 — prerequisite and frozen provenance.** N's independent verified verdict will precede
   F activation and launch. Invocation, cold record, owner binding, reuse record, and derived
   observation will identify Main's final F head, fixed throughout both children. Actual served
   WASM will match the N digest above. Both children will bind the same served runtime tree
   using the proper runner's exact filter, kernel, image, manifest, fixture, and origin. All
   recorded source/helper/C/binary/build-info pins must authenticate actual bytes, including
   the unchanged runner/wrapper digests above. The held image is
   `d2fc4eab9bc1b5fe528a2956b58faefb20fcf18e2505c499ecafa8824d390f72`.
   Missing or mismatched bindings cannot be inferred from a commit name alone.

2. **P2 — new cold seal and isolated reuse.** Cold creation will begin with fresh empty output
   and profile paths, visibly prepare the two-window desktop, terminal text, custom cursor, and
   completed initial playback, then close the paused checkpoint successfully. Actual envelope
   bytes/length/digest, saved CRC/generation, creation provenance, and closed profile-tree digest
   must agree with the checkpoint and cold record. Reuse will authenticate the actual Chromium
   identity (name/version/headless mode), same origin, runtime, and fixture, then launch a newly
   copied iteration profile while preserving the sealed baseline. No historical snapshot/profile
   transplant or rewritten owner binding is permitted. Freshness follows the creation evidence,
   not an assumption that a deterministic digest must differ.

3. **P3 — same actual prepared player.** Read PID/starttime and descriptor identities from the
   new cold evidence; do not seed them from a prior run. Two pre-save observations must agree on
   actual player/parent identity, executable inode, `pipe_read`, a read-only child FIFO descriptor,
   the supervising shell's correct writer, no extra child writer, and an owned PREPARED PCM
   descriptor with zero hardware/application pointers. Independently decoded sound bytes must
   have the held finite stereo S16/48-kHz parameters and no pending playback/XRUN/reset/release.
   After restore, the actual guest post-observation must match that same fresh process/FIFO/PCM
   identity. Under the held helper guards, finite feed, writer close, and successful wait for that
   same child must precede the visible conditional success marker. Inspect the retained PNGs;
   a copied summary alone is insufficient.

4. **P4 — unchanged physical input and policies.** The selected fixture will physically type
   `play` with the existing 5-ms edge pacing after the delayed user gesture. Actual before/after
   observations will retain JIT enabled, decoded capacity 4096, `repack-off` cap 24, compile-queue
   capacity 256, and disabled entry-cost timers. The run will have no CPU/guest-PC/latency sampler,
   command/pacing/clock/divider/budget override, or COMPLETE mode. The existing read-only endpoint
   statistics remain part of this wrapper. Invocation/environment and source pins must show the
   unchanged policy; N correctness is carried from its verdict without retesting it here.

5. **P5 — restored display and fresh guest interaction.** First-present CRC will equal this
   new checkpoint's saved front-buffer CRC; whole-machine resume, full repair, and fresh HELLO
   will occur without a `booting` state. Two pre-gesture samples must show locked/suspended audio
   and zero PCM. The physical gesture/input must produce a fresh mapped tablet frame, guest-rendered
   cursor at the requested point, accepted and guest-used focus, matching keyboard/DOM transitions,
   newly visible guest completion text, and released buttons. Playback must produce positive fresh
   non-silent PCM, attached/running output, and rendered-frame progress. Report actual counters,
   including written versus inspected/non-silent frames; **no expected 1440 count or historical
   PID/starttime is imposed**. This is a freshness/completion claim, not bit-exact audio length or
   an exact first-arrival timestamp.

6. **P6 — canonical timing criterion.** T0 must equal the normal restore's original `completedAt`;
   end must be the original frozen `postRestoreEnd`. Independently compute end minus T0, including
   the delayed gesture and every intervening interaction step. The unchanged criterion is
   `elapsed <= 2000 ms`; any larger value is FAILED. A child exit 1 admitted by the collector
   must cite the exact cap assertion after preceding functional checks. An earlier functional or
   provenance failure is reported at its own point. Later sampling, marker/first-PCM times, and
   outer exit status cannot replace either endpoint or the child's result. Preserve all failures.

7. **P7 — coverage follows reached phases.** If timing passes and the normal coherence audit
   executes, inspect its actual loader admission/generation evidence. If timing fails first,
   retain its deferred status. This non-COMPLETE wrapper exits after normal diagnostic evidence;
   it does not exercise later drag saves, second reload, or guest no-stuck hover. Claim those only
   from a separately authorized record that actually reaches them. Missing full reuse error arrays
   remain a reporting limitation; empty presentation-local errors cannot substitute for them.

8. **P8 — bounded diagnostic disposition.** Cold/reuse and derived output remain
   `acceptance:false` and `fVerified:false`, even if timing passes. Counter conservation, when
   present, will be checked against the raw sequential RPC endpoints and described as aggregate
   jobs with their actual observation limits. Neither this one run nor N's semantic proof supports
   a causal explanation or stable speedup claim. Full F acceptance still requires a complete
   normal, non-diagnostic `make verify-E5-T26f` run and independent review at its frozen head.

## Handoff

Await N's verdict, Main's final F head, exact invocation, and immutable closed cold/reuse paths.
Do not inspect partial new evidence or revise these predictions after seeing it. No additional
task, runtime, image, harness, clone, or browser work is started by this plan.
