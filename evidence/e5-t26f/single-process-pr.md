## Scope

Add a separate read-only static RISC-V observer for the resident aplay fixture.
It gathers bounded proc identity/FD/PCM evidence in one process. The new shell
adapter preserves the existing arming, finite audio feed, same-child wait and
three printed observations. The buffered helper remains intact.

No emulator scheduling, JIT, cache, clock, pacing, restore timestamp or
two-second deadline changes. F remains in-progress. No production release,
merge, Omarchy mutation or Epic6 work is included here.

## Current evidence

- Frozen source head: `05b82bc688a34e6a1abef7b6c761c01bf4a3f7c6`.
- Sanitized C fixtures, actual getppid/FD3 topology and wrong-parent refusal,
  zero-pointer sabotage, and81 focused Node tests pass.
- Actual Zig0.16.0 static RV64GC-compatible cross-build:30960-byte binary,
  SHA256 `cdb72cc93a20ec360f3fb63a23911b77059feaa4230ad84d6963a92e25809350`.
- Native ARM/BusyBox real shell/observer transport passes with controlled
  proc/device fixtures. This is not real aplay, guest or timing proof.
- Fresh1-GiB image contains only the pinned helper and observer additions;
  both extracted readbacks match, fixed ownership/modes/timestamps hold and
  read-only fsck passes. No acceptance claim from image construction.
- Authenticated Chromium cold/reuse restores the same prepared player and
  produces1440 fresh non-silent PCM frames, but the original endpoint is
  **4362.199999928474 ms: FAILED**, not F acceptance.
- The original wrapper additionally failed old collector-kind admission.
  Commit001e8086 repairs only postprocessing admission, with an actual raw-record
  regression. The original wrapper exit and raw bytes are unchanged.
- Daybreak holds scoped source/provenance, browser functionality, incremental
  collector arithmetic and the one exact001e8086 clean-clone proof:10,389 C checks,
  82 observer-gate Node tests and18 overlapping collector tests pass.
- Separate latency and CPU diagnostics also fail the unchanged deadline.
  Authenticated CPU symbolization shows distributed execution costs; it does
  not establish a causal fix or speedup. No additional cold boot is claimed.

Evidence index: `evidence/e5-t26f/single-process-observer.md`.
PR367's11.471545-second negative result is preserved, not superseded into a pass.
