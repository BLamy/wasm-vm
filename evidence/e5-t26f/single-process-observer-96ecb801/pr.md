## Summary

Retain the fresh post-SPP desktop cold/reuse evidence and independent Daybreak
review. Restore/input/audio hold; the original 4104.175ms endpoint fails the
unchanged2000ms requirement. F remains unfinished.

One separate CPU diagnostic fails at4023.200ms. Luna's offline named companion
matches all11 noncustom sections of the served WASM, independently checked by
Daybreak. sync_plic is6.4066% of sampled self weight, not a causal diagnosis or
speedup promise. Retain both prelaunch preprocessing failures and their limits.

Source-only Daybreak preflight permits a narrowly scoped PLIC sparse selector;
this PR does not implement or verify it. No Epic5 completion or deployment.

## Validation

- New authenticated cold child0 and unprofiled reuse child1 at producer96ecb801;
  outer0 records the expected failure, never F acceptance.
- Same prepared player identity, CRC168fc7fa, physical input and1440 fresh
  non-silent PCM frames; actual screenshots reviewed independently.
- Separate CPU profile2608 samples,3260775us denominator, all11 sections equal,
 1890 names and independent summary recomputation; original profile preserved.
- Task policy, generated roadmap metadata and local deploy-bundle rebuild.
- Full commands, digests and limits are in the sibling README.md and
  evidence/e5-t26f/spp-runtime-verifier/{browser-results,cpu-results,cpu-symbols-results}.md.

Later coherence/drag/reload/full-acceptance coverage is still required. The
metadata closure invalidates the old checkpoint seal; it must not be rebound.
