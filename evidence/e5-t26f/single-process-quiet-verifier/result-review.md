VERDICT: **closed-failure — the one print-suppression screen preserves the scoped functional proof but fails the unchanged 2,000 ms cap at 4,101.870000004768 ms.** This is not E5-T26f acceptance, and the print-only hypothesis must not be repeated.

## Reproduction and bindings

`node evidence/e5-t26f/single-process-quiet-verifier/verify-result.mjs` exits 0 and reproduces
`verify-result.json`. It rehashes the closed raw record as
`0d56d45f82d6982a52368ca5dc63499d26ce07409712d460b0cb178ab60363bf`, checks every finite
invocation source binding, independently re-derives the 17-substitution driver, and requires the
actual generated bytes to hash to
`58b735d6a2225cd6614c192af898d7b4878ae1124f39d2244867274684b3fa73`.

The record is explicitly nonacceptance evidence and binds the closed 05b profile/snapshot/runtime/
image seal to its actual producer HEAD `001e80864911863145f2127192bae5df8186e68b`; the **closed recorded
binding's** runtime and image hashes equal the sealed values, and helper/C/binary hashes match the invocation
(`record/failure-post-restore-interaction-checks.json:107-162`). The original sealed snapshot
`87477f...` restores with CRC `9c53f290`, coherence status `passed`, generation 652 and no guest
`booting` state before the RAM-only change (`:244-323`). The separately derived snapshot is
`569f46...`, remains paused at generation 652, then restores that exact SHA and first-present CRC
`af15247a`, again with no `booting` state (`:164-173,460-587`). This sentence makes no claim about
later HEAD `0c9abdac` or its refreshed task/web metadata; those bytes are not the quiet producer.

## Falsifiable result checks

- **Same prepared child / C guards — HELD.** Visual inspection of `quiet-prepared.png` shows both
  pre-save records with these exact repeated values: PID `1000`, start `31468`, `fifoFD=3`, FIFO
  flags `0100000`, `readonly=1`, `parentFD=3`, parent flags `0100002`, `child-writers=0`,
  `pcmFD=4`, owner `1000`, state `PREPARED`, `hw_ptr=0`, and `appl_ptr=0`. They are followed only
  by the RAM `e5_print_observation(){ :; }` definition. The pinned helper still gates feed on a successful
  post-restore C observation exactly equal to the prepared record and emits green success only after
  `wait "$e5_pid"` succeeds (`tools/guest/e5-t26f-resident-observer.sh:112-135`). The pinned C
  observer rejects PID reuse by checking start time and executable inode before/after, then checks
  exact FIFO ownership/modes and prepared PCM ownership/state
  (`tools/guest/e5-t26f-observer.c:219-284`). The final fresh green raster therefore follows those
  gates and the same-child wait; it is not a replacement post-identity print claim.
- **Stale output / physical interaction — HELD.** Two pre-gesture samples are locked with zero
  written and non-silent PCM (`failure-post-restore-interaction-checks.json:589-620`). The exact
  72x13 raster is absent at baseline, then appears once at `(557,195)` with accepted focus and the
  exact ten physical `P/L/A/Y/Enter` press/release edges (`:664-929`). Cursor pixels are freshly
  observed at 824.6200000047684 ms (`:650-657`).
- **Fresh audio — HELD.** The command result is accepted with no red marker and uses the
  `fresh-exact-reviewed-raster` oracle (`:959-979`). PCM is first retained with 1,440 written and
  1,440 non-silent frames, `maxAbs=0.999969482421875`, output attached, running context and advanced
  rendered frames (`:981-1018`). These values falsify stale/prequeued or silent PCM for this run.
- **Original timing boundary — FAILED.** T0 is exactly the derived restore's `completedAt`
  `1050.1349999904633` (`:467-471,589`); the frozen end is `5152.004999995232` (`:1020`). Their
  difference is **4,101.870000004768 ms**, and the child exits 1 at the exact original error
  `post-restore interaction exceeded 2 seconds` (`:1033-1037`). No endpoint or waiver is substituted.

## PNG review

- `quiet-prepared.png` — SHA-256 `9538a1c58c531ed7315b2e68c9204149b50e9dc739c4b44642d8cf7f68238431`:
  two windows and cursor are visible; top terminal retains both PID/start/FD pre-records and shows
  the quiet RAM definition/ready marker; no red failure marker is visible.
- `post-restore.png` and `failure-post-restore-interaction-checks.png` — byte-identical SHA-256
  `80c5b4057a6c89ec7bd33f41995c390e0577ae1d90bd376c0756db77f60cd478`:
  the restored two-window/cursor desktop shows physically typed `play`, fresh green
  `e5t26f-aplay`, the child job `Done`, no verbose post-observation block and no red guest failure.

## Bounded conclusion

Suppressing only the three post-observation `printf`s is insufficient for F. It leaves cursor/focus
ready by 824.62 ms, while the fresh exact raster and fresh PCM arrive about 4.070/4.073 seconds after
T0 (`:921,981-995`). That supports a next, targeted execution-cost investigation of the guarded
post-focus guest command path; it does **not** identify which guarded step is causal, authorize guard
removal, establish a speedup, or justify another print-only run. Later acceptance phases are not
claimed because the unchanged cap failed first.
