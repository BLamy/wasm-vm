# E5-T26f JIT latency extension — pre-evidence predictions

Recorded after reviewing frozen diff `b13c6ce0..43f4cac1ca87af3645a24d2eed12246e84c6593a` and before inspecting `evidence/e5-t26f/jit-latency-43f4cac1`. The prior seven latency-probe predictions/results remain HELD unchanged.

1. **Isolation remains HELD.** JIT sampling executes only under explicit diagnostic-latency reuse. Default acceptance and ordinary diagnostic reuse neither call `jitStats()` nor create latency milestones; the original two-second assertion and completion/PCM predicates are unchanged.
2. **Single-slot ordering.** Each logical sample completes `schedulerStats()` and the paired local `workerRpcStats()` read before issuing `jitStats()`. The sample retains the sole probe-RPC slot through JIT settlement; timer ticks, rejection, deadline, failure, and stop cannot overlap a second scheduler/JIT request or accept late mutation.
3. **Bounded payload.** JIT evidence contains only the named finite scalar counters plus four bounded `entryCost` scalars. Missing APIs are explicitly unavailable; objects, infinities, unknown fields, and large nested data become null or are ignored.
4. **Primary runtime prediction: high JIT share with short entries.** I predict `hasExecutor=true`, positive `executedBlocks`/`retiredViaJit`, and an interval delta `retiredViaJit / guestRetired >= 0.8`, while `retiredViaJit / executedBlocks` and/or `retiredViaJit / entryCost.hostEntries` remains only tens of guest instructions per host entry. That would localize the slow wall time to frequent short translated entries/boundary returns, not absent JIT.
5. **Falsifying alternatives.** If `jitStats()` is missing or `hasExecutor=false`, the replay cannot diagnose JIT participation. If cumulative deltas show `retiredViaJit / guestRetired < 0.5`, the dominant boundary is interpreter fallback/low JIT coverage instead. A 0.5–0.8 share or inconsistent/resetting counters is mixed/inconclusive and must not be relabeled as either outcome.
6. **Use deltas, not cumulative totals.** Share, entry length, cache churn, and host-entry cost must be computed between completed samples bracketing the interaction; absolute counters include work before restore/probe installation and cannot attribute this command's latency.
7. **Diagnostic-only conclusion.** Even a high JIT share and short entries do not waive E5-T26f timing, establish uninstrumented performance, or change T19a/H. The probe itself adds serialized worker RPCs, so it answers the runtime-boundary question only.
