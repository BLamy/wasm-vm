# E5-T26f owned-worker CPU profile — pre-evidence predictions

Recorded at frozen candidate `560c67431d0dcbae24fef7fda4af84df887d0751`, before inspecting `evidence/e5-t26f/cpu-560c6743`. Scoped Make/runner/real-adapter-test diff SHA-256: `01a22ecb58560010b3c990febe85c0fc0761af65f793a792aa3d0efb8b7e11b9`. Prior latency and JIT diagnostic results remain HELD unchanged.

1. **Diagnostic isolation.** `E5_T26F_DIAGNOSTIC_CPU=1` is exact-`1` and reuse-only, independent of the latency flag. This replay has CPU enabled and latency omitted, so no periodic scheduler/JIT probe or `interactionLatency` milestone exists. Default acceptance and ordinary reuse never attach a profiler.
2. **Exact worker ownership.** CDP target discovery finds exactly one worker whose URL is the resolved owned `linux-worker.js`; zero/multiple/wrong targets refuse. The persistent-context adapter's real 300 ms test records the expected synthetic worker function and rejects a missing URL.
3. **Original command boundary.** The CPU profile starts after restored focus/audio unlock but before the first physical key of `sh /tmp/a`. It stops only after conditional command completion, immediate fresh-PCM sampling, output/render checks, and frozen `postRestoreEnd`; profiler collection occurs before the unchanged two-second assertion and cannot rewrite that timestamp.
4. **Failure durability.** The expected timing failure still leaves `interaction-cpu.json` with exact head, runtime binding, owned-worker URL, browser identity, original `restoredAt`, profile start, frozen `postRestoreEnd`, reason `interaction-observed`, raw profile, and a recomputable file digest in the failure milestones. An earlier failure uses `cleanup-after-failure` and remains diagnostic-only.
5. **Name-map sufficiency.** Offline output must bind the raw profile to release WASM SHA-256 `551206882e7e3ec605dfe04571e53046a81678ebd542fdccafc2c08d8188c229`, named WASM SHA-256 `54b350e9b9f628313ad2784886ed8749709a41162e6e3aa7663b703d30b37232`, all eleven identical non-custom sections, and the 1,884-name map. Unresolved/idle/JS shares remain explicit rather than assigned to a guessed Rust function.
6. **Primary CPU prediction.** Weighted leaf/self samples during the profiled command interval will put interpreter/MMU/device execution above 50% and expose one named interpreter, memory-translation, or device-dispatch function as the dominant actual leaf. JIT engine-entry/state-copy/dispatch overhead will be material but secondary; compiler/translator/cache-install work will remain below 10%.
7. **Alternative outcomes.** If named JIT entry/state-copy/exit functions instead have the largest weighted share, the short-compiled-entry boundary wins. If compiler/translator functions exceed 25% and include the dominant leaf, compilation wins. If no category exceeds 50%, the result is mixed; if unresolved/idle/unnamed samples exceed 20% or the profile is sparse, it is inconclusive.
8. **Aggregation rule.** Use profile `timeDeltas` to weight sample-node leaf/self time, report total and category shares, and name the highest-share actual function with its exact sample/time share. Node `hitCount`, JIT counters, cumulative scheduler totals, or inclusive ancestry alone cannot establish dominance.

Whatever the profile shows, it is diagnostic localization only: it does not verify E5-T26f, waive the all-success two-second deadline, alter runtime code, or reopen T19a/H.
