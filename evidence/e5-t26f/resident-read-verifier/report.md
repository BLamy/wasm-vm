# Bounded native read-probe critic

VERDICT: HELD — only for the stated non-acceptance native read-primitive claim.

- P1 provenance and isolation — HELD. Predicted one real aarch64 Linux run in a new cached Alpine 3.20 container with network disabled, CPU/memory/PID limits, a stopped source container before and after, an unchanged Git head, and exit zero. `versions.txt:1-11` identifies BusyBox 1.36.1, strace 6.9, aarch64 Linux, and the two binary hashes. `transcript.log:2-13,122-140` records the stopped source, exact constrained `docker create`, successful start/exit, unchanged source state, and unchanged head. `provenance.json:2-15` records `acceptance: false`, image/container identity, limits, 2026-09-08T14:39:42.973Z–14:39:43.146Z, and exit 0. Commit `2ace135363cf0fa3cf7ba978fe6810e564eecc03` is only the repository/runtime base observed during execution: the new probe `.sh` and parser `.mjs` were uncommitted and are not claimed to be contained in that commit. Their exact executed/reviewed bytes are instead bound by SHA-256 (`ff640c09cf2ef4fb4ef3713b0ab03403ff9d82ff5b48f93d94610df3779ad80b` and `78eed6e63871af82ce9e1299a203c6bb17c9355adbfa103636575d42729d21d9`) and by the byte-identical retained `run.sh`. The recorder performs only evidence/container setup and read-only copies from the stopped source (`record.mjs:26-57`); no runtime, helper, image, served-tree, task, queue, build, browser, or commit action is part of this probe.

- P2 real read behavior and reconstruction — HELD, with the `stat` EOF nuance explicit. Predicted the builtin shell itself would issue one-byte target reads totaling 302/42/102 for stat/fdinfo/io, while three real BusyBox `cat` children would issue two 65536-byte reads per file, one positive and one EOF, and every concatenated returned payload would equal its corresponding output. The probe opens its own `/proc/$$/{stat,fdinfo/3,io}` and uses builtin `read -r` or real `cat` (`tools/verify/e5-t26f-ash-read-probe.sh:24-46`). Raw points are `builtin.strace:3-304` (stat), `:305-346` (fdinfo), `:347-448` (io), and `whole.strace:3-13` (cat PIDs 21/22/23 and all six reads). `summary.json:14-20,1842-1848,2111-2117,2740-2824` records totals 302/42/102 and 2/2/2, plus the output and trace digests. The parser decodes a restricted C-escape grammar without evaluation, checks decoded length against the kernel return, authenticates caller PID/real cat exec, and byte-compares concatenated chunks to output (`tools/verify/e5-t26f-ash-read-probe.mjs:26-85`). Important precision: stat has 302 positive reads and **no EOF read** because single-line `read` stops at newline; fdinfo and io totals include one EOF each. Every whole-file total includes one EOF.

- P3 pins, retained failures, and finite parser checks — HELD. The probe/parser hashes remained `ff640c09cf2ef4fb4ef3713b0ab03403ff9d82ff5b48f93d94610df3779ad80b` and `78eed6e63871af82ce9e1299a203c6bb17c9355adbfa103636575d42729d21d9` before and after review; canonical evidence hashes are in `input-hashes.log`. `setup-path-failure.log:1-9` fails before container creation; `setup-library-failure.log:73-75` exits 127 before tracing; `setup-versions-failure.txt:1-22` identifies the missing SONAME/library failure. `parser-checks.json:2-25` marks the valid copy accepted and three finite mutations rejected while preserving raw inputs. None is relabeled as native success.

- P4 bounded novel parser attack — HELD. Predicted a target line retaining parser shape but containing an unsupported C escape would fail closed and emit no summary. The isolated fixture changes only `attack-unknown-c-escape/builtin.strace:3` to `"\\q"`; parser exit is 1 at `decodeCString` line 42, and no summary exists (`attack.log`). Canonical inputs retained their pre-attack hashes.

Limits: This review does not run or validate full `e5_observe`, prepared `aplay`, RISC-V or browser behavior, the two-second F criterion, timing/performance causality, helper promotion, Rawls' compiler-queue browser recording, or F acceptance/status. The supplied browser-close timestamp (14:34:26 UTC) was deliberately not re-reviewed; only the native record's later timestamps are authenticated here. No claim is made that fewer `cat` reads imply a net speedup.
