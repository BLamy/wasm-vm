# Frozen-source attack registration

Registered after reading the implementation and tests at `05b82bc688a34e6a1abef7b6c761c01bf4a3f7c6`, before executing this attack. C SHA-256: `4ce161e7c1d82c19d2bd924c5f93d3d097c93a1ab6c53cec397e8ec1d2ee30e9`.

P10's selected variant is a late duplicate **ignored PCM metadata key** after all four mandatory fields, a delimiter, and 80 unique metadata fields. The second `tstamp` starts at offset 6 modulo 7, so its key crosses a seven-byte read boundary. The valid control changes only that key's first byte to `xstamp`, retaining the length, fragmentation, required values, and two injected EINTR reads. Predictions: actual `observer_main` accepts the valid control with exactly the independently specified record; rejects the duplicate with exit 30 and zero record bytes. These are controlled proc/device data, not real PCM or browser evidence.

The worker already tests short reads and duplicate ignored keys separately, but not this combined late, split-key input. A verifier-only mutant removes only `remember_key` from `parse_pcm`; it must still accept the positive control, but the duplicate must then be accepted, killing our refusal oracle. Separately, the worker's existing `parser_tests()` rejection oracle must fail on this mutant (not compilation failure or sanitizer termination). Only that parser function is executed, not its whole observation/CLI suite or the Make gate.

The driver mechanically copies the frozen C/test sources and replaces the test program's `main` only. Its filesystem mapping and controlled getppid remain explicit test seams; the production observer implementation runs unchanged in the original copy. Every run has a new retained tree under this verifier directory. No source/helper/build/served or old evidence files are changed. No Docker, cross-build, browser, clone or full gate is run. Compile/run stdout, stderr, exits, source pins and all attempts are retained. Before/after pins must agree.
