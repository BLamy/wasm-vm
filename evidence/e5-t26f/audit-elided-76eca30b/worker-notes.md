# Audit-elided 76eca30b handoff

Build-only diagnostic factory, focused tests, and deferred launcher added under this directory:

- `factory.mjs` derives from the current browser runner and held exact 72×13 raster oracle, pins the current 76eca30b seal and observer helper, and emits a fresh target driver.
- `factory.test.mjs` checks source drift, strict admission, exact RAM-only removal shape, retained guards/oracle/timing, syntax, absolute imports, and fresh-output refusal.
- `run.mjs` creates a fresh target/evidence attempt, records invocation, command/log/raw exit, original timing interval, and generated-source digest. It is not invoked here; Main owns the browser launch after review.

The generated guest command evaluates only the helper’s current `e5_play` body from the sealed image and removes the exact 156-byte post-identity mismatch/failure block plus `e5_print_observation post`; the resulting 535-byte body is SHA-256 `50fa3e0e8264ebdb6f31d2816c06a5275e8ba4c9ddb892a8cc989ef068f83c58`. Armed/PID guard, finite PCM, SIGPIPE, FD close, same-child wait, and green completion remain. Setup additionally clears display and homes the cursor before the green readiness marker; this is outside T0 and is recorded. No helper/image/profile writes occur.

Expected checks: `node --test evidence/e5-t26f/audit-elided-76eca30b/factory.test.mjs` and `node --check evidence/e5-t26f/audit-elided-76eca30b/run.mjs`. No browser, long gate, or acceptance claim was run.

Build-only check results: all 5 Node tests passed; factory, test, and launcher `node --check` passed. Digests: `factory.mjs` `d4c346ccca10fe842e20c249770c2752d746d4c208c90944f22db063f9c1c43a`; `factory.test.mjs` `54c09e2b00212e175aadf9c7fcbe12b149caefadbb98e7bfa643763665e33360`; `run.mjs` `c81e7fd7dd54468dcc05b0896e710e14eb916b88d14af207ea9610a56aeedcb3`. WASM pin is `20f58e0d44cc94f9d0629478789680e4a87345ba800162aa0737ddc763bfd238`.

All emitted records are labeled `audit-elided counterfactual`, `acceptance:false`, `fVerified:false`, and `postIdentityValidated:false`; the result is limited to combined validation/reporting-path materiality.

## Main integration corrections (supersedes draft hashes and five-test handoff)

The original handoff above is retained as historical worker output. Main corrected the
physical setup to one printable base64-decoding command (the draft contained literal
newlines unsupported by the physical-key helper), aligned its readiness waiter, made
launcher imports inert, strengthened source pins and raw-result/cap validation, and added
the recorded six-test check in `main-tests.log`. These edits do not change the proper
runner, installed helper, runtime, image, HEAD, or original sealed profile.

Daybreak's prelaunch review caught launcher metadata occupying the child output directory.
Main separated the fresh run root from its empty `record/` child, used that child consistently
for generation, execution and raw reads, and added a no-browser layout regression test.
All seven tests pass in `main-tests-r2.log`; launcher syntax also passes. Current launcher
SHA256 is `cbe55bafb7ba5132664795c92afa28a1dbc0260bbb4667f5d06e6abb6af5a60b`;
tests SHA256 is `f438f342761e550ec9f1a3994b845a88cee0edaf4b7cecb3096391d914d89b00`.
Factory remains `651a75c95d6cf0d016c192b9b5ae7487a1e78085fffec6784577b37f901f366f`;
generated driver remains `6679ef040cdce8d4a3a67a4ce02f08c87188ed049fb8f1e0835292020ffae133`.

Read-only Docker/debugfs inspection of the sealed image found `/bin/base64` as a symlink
to `/bin/busybox` (inode16, mode0777). `/usr/bin/base64` was absent. This was an availability
check, not a guest execution claim. No browser has run for this diagnostic at this entry.

## Second generated revision: setup margin only

The first launch is retained in `run-1788906189330-12945/` with its exact three source
files archived under `source/`. It failed before physical play because the readiness
banner contaminated the unchanged geometry detector's first32 sampled body rows.
Daybreak's offline PNG analysis and closed verdict are in
`../plic-runtime-verifier/audit-elided-results.md`; no timing result came from that run.

Main added four blank setup lines after clear/home and before readiness. The actual
driver delta in `setup-margin-r2.diff` contains only that one command change. The helper
function, detector, raster region, physical play and timing boundaries are unchanged.
Seven focused tests pass in `main-tests-r3.log`, including exact native printf output
and byte-identical inherited geometry detector. Daybreak approved one corrected run.
Driver SHA256 `c847ff1b91428b66579ad11b54efe8351d9a4915ec49a816473bf80b1e6b89bf`;
factory `367aff58510129a94fd11b93c79e0d8f5470d57d358a1781fdeffd5af1477faa`;
launcher `b8a8d30c5028ed7d70e8aaea776ad89abeac6b72ec9c144a504e0a57204aeb45`;
tests `c7b4a0bec3d0df3ad8faf872f70b8e69461d8b2fe5a5a4498c5a049fabe64082`.
Four lines—not a claimed64 pixels—is the setup specification.
