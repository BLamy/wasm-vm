# E5-T26f inline-context runtime screen — critic predictions

Date: 2026-09-08  
Role: fresh Daybreak Blue critic  
State: predictions registered before Main's new browser evidence; awaiting the closed result.

## Fixed scope and carried results

This review initially owns only this plan. Main owns F reactivation, the frozen head, build,
browser launch, raw logs, and later task/status work. I will not run a browser, build, clone,
WebKit, rr, `ssh dev`, an independent-machine check, or change runtime/harness/task status.

E5-T26m is carried **HELD**, not re-litigated: cached equality ignores exactly `mstatus` bits
1/3/5/7 (SIE/MIE/SPIE/MPIE), retains every other authority input, and preserves the complete
architectural `mstatus`. Its verified runtime WASM is
`18e53caa2e160819d16a6e0bf376530d45234e28f315c89b5042c48b1d791cc4`; Main identifies the
M final-closure commit as `fff16dadf26b5e1596c8524dfa6d6572d1d14da2`; the new frozen F head remains
pending Main's handoff. M made no live-CSR,
interrupt-sampler, clock, execution-budget, or performance claim.

The prior observer source/image and reached-functionality findings remain **HELD only within
their old digests and scope**. They are not current browser evidence. The old unprofiled
`4362.199999928474 ms` and print-only `4101.870000004768 ms` endpoints remain failures and
must not be rebound, averaged away, or relabeled as baselines proving a speedup.

## Falsifiable predictions for the new closed screen

1. **P1 — fresh exact bindings.** `invocation.json`, cold checkpoint, reuse raw record, and outer
   observation will identify one Main-supplied frozen head. Independently read served WASM will
   equal the verified M digest above. Cold and reuse will agree on the proper runner's full served
   runtime-tree digest, kernel, observer image and chunk manifest, helper/source/binary/build-info
   pins, local origin, and Chromium name/version/headless identity. The unchanged observer image
   may retain its held bytes; the binding and browser seal must be newly recorded for this runtime.

2. **P2 — no historical seal rebind.** The wrapper will create a new empty output and a new
   task-owned persistent profile, close it after a successful cold preparation, and run reuse only
   on a copied iteration profile. Its profile digest, checkpoint creation time, snapshot digest,
   and retained path will be new and will not cite or mutate any prior `05b82bc6`, `001e8086`, or
   other F seal. Any old profile/snapshot transplanted under the new head is a refutation even if
   CRC or timing passes.

3. **P3 — actual same prepared process.** Cold evidence will show two matching observations of
   one real prepared player: PID/starttime/executable inode, `pipe_read`, child FIFO FD3 read-only,
   parent FD3 read/write, sole writer, PCM FD4 owned by that PID, PREPARED state, and zero
   hw/appl pointers. Decoded saved sound bytes will have the finite stereo S16/48-kHz parameters
   and no pending TX work/XRUN/reset/release. After restore, physical `play` at 5-ms pacing will
   re-observe that same PID/start/FIFO/PCM identity, feed exactly the finite fixture, close the
   writer, wait for that same child, and only then show conditional success and child completion.

4. **P4 — unchanged execution policy.** Reuse will report actual executor state with JIT enabled,
   decoded-cache entries `4096`, residency `repack-off` with cap `24`, compile-queue capacity `256`,
   and entry-cost timing disabled. There will be no CPU, latency, guest-PC profiler, command,
   guest-clock, ICount-divider, decoded-capacity, key-pacing, live-CSR, sampler, quantum, or budget
   override. The explicit wrapper settings must resolve to these existing defaults, not a tuned
   policy hidden behind the phrase “unchanged.”

5. **P5 — original timing boundary.** T0 will be exactly the normal restore's `completedAt` and
   end will be the proper runner's frozen `postRestoreEnd`, before after-sampling/collector work.
   `elapsed = end - T0`; `<= 2000 ms` is HELD and `> 2000 ms` is FAILED. Exit 1 is admissible only
   for that exact cap assertion after all preceding functional checks; any earlier failure is
   reported at its own predicate. Outer wrapper/collector exit 0 and `fVerified:false` never turn
   a failed child into acceptance. One faster result is not a causal or stable speedup claim.

6. **P6 — fresh interaction, not stale ledgers.** The first-present CRC will equal the new saved
   CRC, full repair and fresh HELLO will complete, and no `booting` state will appear. Two
   post-restore pre-gesture samples will remain locked/suspended with zero PCM. The delayed
   physical gesture will produce a new guest tablet frame and rendered cursor at the requested
   point, accepted focus, matching physical keyboard/DOM transitions, newly visible terminal
   completion text, released buttons, attached/running audio, rendered-frame progress, and fresh
   positive non-silent PCM (expected finite fixture: 1440 frames). Host input counters or old PCM
   alone cannot satisfy this prediction.

7. **P7 — coherence and unreachable later phases.** If the diagnostic timing assertion passes,
   the normal restore-coherence audit will also complete against the same saved generation without
   a reboot/re-probe. If timing fails, the non-COMPLETE diagnostic is expected to stop before that
   audit and must say so. By source construction this wrapper returns after normal diagnostic
   evidence and cannot reach drag-phase saves, second reload, or the guest no-stuck hover; any claim
   that this screen freshly proved them is false. Old drag/no-stuck findings may only remain carried
   at their original unchanged boundary.

8. **P8 — M and acceptance limits remain explicit.** The record may test whether M's already
   verified runtime happens to improve this one endpoint, but it cannot re-prove M semantics or
   infer a speedup cause. The wrapper's cold/create and reuse artifacts must remain
   `acceptance:false`, with outer `fVerified:false`, regardless of timing.

## Required follow-on for F acceptance

Even if every prediction above holds and elapsed time is at most 2000 ms, this bounded
create/reuse observer screen is **never full E5-T26f acceptance**. A complete, non-diagnostic
`make verify-E5-T26f` run at the final exact head is still required. That normal run must satisfy
the original two-window/cursor/text/completed-aplay snapshot, first-present CRC, fresh timed
text/cursor/focus/audio, normal coherence, all before/held/moving/released drag saves, second
reload, and guest-visible no-stuck/no-reboot result under the unchanged supported policy.

No prelaunch source contradiction was found. Review pauses here until Main supplies the immutable
closed result and exact frozen head.
