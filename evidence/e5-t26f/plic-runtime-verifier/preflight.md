# Source preflight: audit-elided counterfactual

GO for **ONE explicitly labeled diagnostic screen**, not acceptance or a production guard waiver.
The original4002.944999933243ms failure remains authoritative. No implementation or run performed here.

The proposed edit is bounded: in `tools/guest/e5-t26f-resident-observer.sh:112`, remove only
the post-restore `e5_observe && [ "$e5_seen" = "$e5_expected" ]` failure block and
`e5_print_observation post`. Preserve the armed/PID guard, unchanged finite payload/feed,
SIGPIPE handling, writer FD close, same-child wait and conditional green completion.
Do not forge `e5_seen`, return synthetic observer success, rerun preparation, or alter the
installed helper. Saved preparation is carried evidence, **not fresh post-restore identity proof**.

Use a fresh copy of the current76eca30b/yj0SBu seal and a separately identified RAM-derived
snapshot; authenticate baseline and derived CRC/sound/coherence using existing checks. Keep
original profile, runtime, image and HEAD untouched. Label every result `audit-elided
counterfactual`, `acceptance:false`, `fVerified:false`, post-identity validation absent.

Reuse the held exact72×13 raster oracle (`e5-t26f-quiet-text-probe.mjs:1302`): empty baseline
in the actual focused upper region, exactly one fresh token, matching physical key edges and
unchanged titlebar. Preserve play/5-ms pacing, audio freshness, cursor/focus and original
restore-completedAt→frozen-end2000ms criterion. Do not retain the incompatible2000-pixel heuristic.

Concrete source constraint: `e5-t26f-single-process-quiet-probe.mjs` hard-pins the historical05b
seal. It cannot run as-is. Derive the fresh diagnostic with current bindings; never rebind
an old owner/seal or modify the held factory to hide that distinction.

Narrow the inference: this screens **combined validation/reporting-path materiality**, not
validation-fork/proc-read dominance alone. Printing, raster observation and derived-snapshot
preparation also differ; the historical print-only4.1s run is not a current control. No extra
arm is required for this bounded question. A fast result can motivate guard-preserving work,
such as a resident observer, but establishes neither causation nor permission to omit guards.
