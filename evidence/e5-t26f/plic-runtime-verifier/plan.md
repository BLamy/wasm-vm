# E5-T26f after O — prelaunch critic predictions

Planning only, 2026-09-08. Independent F critic, not O verifier or implementer. Read F's
current boundary/acceptance and existing single-process wrapper. No new evidence inspected.
F remains blocked until O is independently verified; Main owns activation, metadata freeze,
final HEAD and ONE fresh cold checkpoint plus ONE unprofiled default reuse screen.

Supplied O runtime: `02c79f5672c3f1a08a3429f7b32dce4e051a41ec`, reportedly changing only
`PlicState.best_source` to guarded ascending-bit scanning with bit0 masked. Its correctness
belongs to O's independent verifier. Expected WASM:
`20f58e0d44cc94f9d0629478789680e4a87345ba800162aa0737ddc763bfd238`.
Final F HEAD is pending; all historical seals are invalid and must never be rebound.

## Falsifiable predictions

1. **Prerequisite/bindings.** O's verified verdict precedes F launch. Invocation, cold/reuse,
   owner and collector agree on Main's frozen F HEAD and actual runtime/source/browser/origin
   bindings. Recompute recorded digests, including served WASM, runtime tree, kernel, fixture,
   manifest, helper and source pins. Held image remains
   `d2fc4eab9bc1b5fe528a2956b58faefb20fcf18e2505c499ecafa8824d390f72`.
   Missing bindings are not inferred from HEAD alone.

2. **Fresh seal/player.** Cold begins in new empty output/profile paths, visibly prepares two
   windows, terminal text, cursor and completed initial playback, then closes a paused checkpoint.
   Actual snapshot bytes/digest, CRC, generation and closed seed agree. Reuse authenticates
   Chromium identity and launches a fresh copied iteration, never the baseline or an old seal.
   Inspect new PNGs and actual pre-1/pre-2/post player PID/starttime, FIFO and owned PREPARED PCM
   identity under carried helper guards; decode saved sound and require zero pending playback.
   Do not hardcode historical PID, descriptors, starttime or frame counts.

3. **Unchanged policy/functionality.** Physical `play` retains existing 5-ms pacing, default
   JIT/cache/residency policies and clocks/budgets. No command, CPU/latency/guest profiler or
   COMPLETE override. First-present CRC equals this fresh saved CRC; full repair, fresh HELLO
   and machine resume occur without booting/re-probe. Two locked/suspended zero-PCM samples
   precede fresh non-silent PCM, attached/running audio, accepted guest-visible focus, matching
   keyboard/DOM edges, completion text and correctly rendered moved cursor; buttons are released.
   Report actual written/inspected/non-silent counts, not bit-exact duration or first arrival.

4. **Original deadline.** T0 equals normal restore `completedAt`; independently subtract it
   from the original frozen `postRestoreEnd`. Require `elapsed <= 2000 ms`. Any larger value
   is FAILED; cite exact child exit, assertion and raw fields. Neither outer0, later interaction
   readings nor profiler timestamps can replace the endpoint. No speedup is presumed.

5. **Reached coverage only.** Claim later coherence admission/generation, drag-phase saves,
   second reload and no-stuck hover only if actually reached. Preserve deferred phases and
   missing complete browser/HTTP error arrays; presentation-local emptiness is insufficient.

6. **Bounded disposition.** Record HELD/FAILED/NEEDS EVIDENCE with hashes. Carry unchanged
   observer/image/helper and prerequisite correctness boundaries without reruns; prior failures
   remain failures. Aggregate RPC counters are not exact interval attribution or causation.
   Diagnostic `acceptance:false`/`fVerified:false` remains mandatory even if timing passes:
   complete normal `make verify-E5-T26f` and independent review still follow.

Stop after this plan. Await O verification, final freeze and closed immutable evidence.
