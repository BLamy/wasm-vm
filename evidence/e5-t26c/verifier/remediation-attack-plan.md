# E5-T26c remediation verifier predictions

Date: 2026-09-06
Frozen submitted head: `be6d61351b0f98930e4200f9df029876dbcfc8ea`
Runtime/test remediation: `7c2a61aec9fe81c46da37bbcd0db279aaaf86d73`
Review range: `4233b51b3b4f03186561a91df7469300d376bdc9..be6d61351b0f98930e4200f9df029876dbcfc8ea`

These predictions were fixed after reading the task, prior provisional report, prior attack plan,
the complete remediation diff, and the current source, but before opening the replacement
`native-final.json` or executing the acceptance/evidence run.

1. **Exact-head gate.** At the unchanged submitted head, `make verify-E5-T26c` exits zero and
   runs 9 snapshot tests, 6 keyboard/LED tests, 2 virtio-keyboard integration tests, both clippy
   modes, format, and the no-default-features wasm32 build with no ignored or panicking test.
2. **Evidence binding.** The replacement evidence has SHA-256
   `cf3f1286da0fe022859e0e455bc11c7805ded038f91e28688303978059105d62`, identifies runtime/test
   commit `7c2a61aec9fe81c46da37bbcd0db279aaaf86d73`, records the exact commands and successful
   outcomes, and is corroborative rather than a self-computed semantic oracle.
3. **Total serialized-event cap, including consumed records.** Exactly 65,536 combined staged
   and pending-frame event records encode and decode. Adding record 65,537 fails with
   `TooManyEvents` even when the frame's `next` index makes its events fully consumed and the
   declared pending count is therefore zero. Decode refusal leaves the target's serialized state
   and intentionally unserialized delivered-key ledger unchanged.
4. **Release-growth atomicity.** Restoring a valid payload already at the 65,536 serialized-event
   cap over a target with delivered keys/buttons predicts the additional deterministic key-up plus
   `SYN_REPORT` frame before mutation. Because that frame would make the restored state exceed the
   total serialized-event cap, restore returns `TooManyEvents`; the complete target state,
   delivered/suppressed ledgers, queue ordering, and re-encoded bytes remain unchanged.
5. **Malformed atomicity.** Zero/oversized frame lengths, `next > length`, pending-count mismatch,
   truncation, trailing data, invalid booleans/reserved bytes, duplicate events, and an oversized
   combined serialized count are all rejected before mutation, including preservation of the
   host-held ledger demonstrated by the subsequent expected release frame.
6. **Order, protection, and fresh input.** Multi-frame/staged snapshots preserve event bytes,
   frame order and partial indices byte-for-byte. Multi-key/button release-all is descending by
   code followed by one `SYN_REPORT`, precedes saved work, is idempotent, survives budget and
   suppression pressure, and clears physical ledgers. Empty restore accepts fresh keyboard,
   tablet, and mouse frames immediately.
7. **LED round-trip.** LED state serializes as exactly `[num, caps, scroll]` canonical boolean
   bytes; malformed values fail atomically; a fresh post-restore LED status event updates the
   exact expected byte without reset.
8. **Coverage and stale-evidence audit.** Every runtime hunk in the remediation is executed by an
   acceptance or verifier attack: encode validation/pre-reservation/cap, decode combined cap, and
   restore release-growth preflight. Test-only, task-log, queue, and evidence hunks are classified
   explicitly. No claim relies on stale `4233b51` evidence or on a fixture whose expected value is
   generated by the implementation under test.

Waived by explicit user/repository policy: independent machines, WebKit, and host rr.
