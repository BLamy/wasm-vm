# E5-T26i fresh critic — pre-evidence predictions

Recorded against frozen head `99b8e692fddb7b1e82e4175e152ef5682c6b9373` after reading the complete task and `git diff 65e99f2e..99b8e692`, before opening any T26i submission evidence. E4-T24, E5-T26h, and E5-T19a results are carried HELD where their unchanged boundaries apply. No speedup, production-default change, F timing waiver, or F verification is predicted.

1. **Default and selection atomicity.** Omitted and exact `icount` select ICount without reading a host clock. Only exact `wall` is accepted. Invalid types/case/whitespace and unavailable/nonfinite/negative `performance.now()` reject before fetch, machine clock mutation, scheduling, or source construction. A rejected selection while wall mode is active preserves mode, `mtime`, anchor, snapshot bytes, and subsequent progression from the old anchor.
2. **Real monotonic source.** Browser wall mode is backed by the active Window/Worker realm's receiver-correct `performance.now()`, converted from milliseconds to nanoseconds; no `Date.now`, RTC epoch, profiler timer state, or query-string observation alone counts as guest-clock proof.
3. **Busy guest rate and control.** In a non-WFI UART/`rdtime` loop, direct and worker wall modes report a positive guest delta near the real host interval at the DT's 10 MHz rate (declared bound 0.7–1.3 for the 600 ms fixture). ICount reports exact `floor(retiredInstructions / 10)` and remains retirement-derived. Initial machine state is mode-selected with decimal-string `mtime == "0"`, `timebaseHz == 10_000_000`, and `clockDiv == 10`.
4. **Backward jitter.** After wall time reaches 10,000,000 ticks, a host sample 100 ms backward cannot reduce guest `rdtime`; a later host sample 1 ms beyond the prior high-water point yields exactly 10,010,000 ticks, with no duplicate retirement contribution.
5. **Explicit pause/resume.** A 500 ms host advance while explicitly paused leaves the complete guest-clock state unchanged. Resume rebases before unpausing/scheduling; the first resumed `rdtime` is non-regressing and excludes the paused interval (fixture bound below 250 ms). A rebase failure leaves pause/quota/read-only state and scheduling untouched.
6. **Ordinary background gap.** An unpaused ten-minute host gap is not silently rebased: existing E4-T24 clamp/slew/jump policy applies, advances guest time by the policy's documented amount, and exposes a jump notification. A later explicit rebase clears stale jump notification without itself changing `mtime` or deadlines.
7. **Successful restore lifecycle.** Fresh-machine and same-machine restore both commit the saved `mtime` and timer deadline, then establish a new host epoch at that restored value. Advancing either new host source by 1 ms advances exactly 10,000 ticks; the saved deadline becomes pending at its original restored tick. The old pre-restore host epoch/floor cannot drag the restored timeline forward or backward.
8. **Rejected restore atomicity.** Corrupt CPU, RAM, CLINT, and CLOCK sections each reject without reading/rebasing the host clock and without changing policy, anchor, `mtime`, deadline, guest state, or snapshot bytes. The next valid wall sample must progress from the pre-failure anchor. The WASM blob-refusal path has the same property.
9. **Portable wire and ICount oracle.** Saving in wall mode neither samples nor serializes the host epoch; identical guest state produces identical resume bytes across host timestamps. ICount rebase calls are no-ops, repeated ICount selection/restore preserves sub-tick phase, trace hash, snapshot digest, and the historical deterministic result.
10. **Worker/WASM ownership.** The desktop query is only an input: the exact option crosses page → worker boot data → loader validation → WASM `setGuestClock`; a fresh `guestClockState` RPC returns current machine state rather than requested/cached data, preserves u64 `mtime` as a string, and propagates absence/errors. The actual worker busy-guest fixture must execute the same `rdtime` code as direct mode.
11. **Conservative JIT boundary.** Wall mode does not alter residency, tier thresholds, scheduler quantum, snapshot format, or E4-T24 slew policy and does not enable direct chaining. Browser timing comparisons are unprofiled and use the unchanged `sh /tmp/a`, image, sealed checkpoint, input, PCM, marker, and two-second assertion.
12. **Browser comparison sufficiency.** Both modes bind to the same exact head/runtime/image/profile/snapshot, restore without reboot, report positive guest ticks from worker machine state, and retain real terminal marker/visual change plus attached non-silent PCM. Any interaction over two seconds remains an explicit F failure and cannot be relabeled as T26i acceptance of F.
13. **Bounded novel attack.** Repeated wall rebases at a nonzero restored `mtime`, including an unchanged host sample followed by backward host jitter and then +1 ms, must never alter `mtime` at rebase, regress it afterward, consume a deadline, or accumulate paused host time. Switching to ICount afterward must advance only by retirement/divisor and must not retain a wall jump.
14. **Sabotage prediction.** In an isolated copy, freezing the manual wall source after arming must make the busy-rate regression fail its positive 10 MHz expectations; scaling host nanoseconds by 1/10 must make the same exact tick assertions fail. If either sabotage remains green, the test is self-licking or does not prove host-clock dependence.

Each item will be marked `HELD`, `FAILED`, or `NEEDS EVIDENCE` only after independent attacks and authenticated evidence review. Browser-dependent items remain pending until the coordinator supplies the frozen recordings.
