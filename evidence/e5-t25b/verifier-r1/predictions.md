# E5-T25b verifier predictions (frozen before evidence inspection)

Frozen at verifier start against expected head `6bc03107cc72704c5f6e1f939e6147a15625e973`.

1. **P1 baseline completeness:** the retained headed-Chromium JSON will identify the exact commit, browser, viewport, DPR, image, and manifest; contain exactly five accepted real-window runs; and each run will request exactly 300 moves, process at least 300 pointer frames, and record positive wall duration and drawn presents. The recomputed drawn-FPS CV will be below 15%.
2. **P2 drawn-present FPS:** each accepted run's FPS and aggregate percentiles will recompute from drawn presents over measured wall time, not requestAnimationFrame or acknowledged presents. Full present records will support the count.
3. **P3 attribution:** each accepted run will contain finite, nonnegative, internally consistent attribution deltas with nonzero guest instructions and nonzero guest/present duration buckets; regressing or zero required counters will be rejected by the analyzer.
4. **P4 sink/window/damage rejection:** null/undrawn sink, absent Foot window, stationary/no-damage execution, and zero drawn presents will reject rather than emit an accepted plausible FPS result.
5. **P5 malformed analyzer inputs:** 299 and 301 requested moves, duplicate/non-finite pointer points, missing/zero/regressing attribution, null or undrawn sink, and impossible/regressing present counters will all be rejected or fail the exact assertion appropriate to that layer.
6. **P6 release parity:** the audit will prove exact source/dist parity for every runtime projection changed by the task, including perf modules, terminal integration, roadmap query gate, and scheduler sampler install/clear behavior; committed dist will match source at HEAD.
7. **P7 environment isolation:** `make verify-E5-T25b` and its browser runner will scrub inherited `RUSTFLAGS`, every `CARGO_*` variable, and `RUST_LOG` before building/serving/running, with no dependence on the verifier shell's values.
8. **P8 stress semantics:** if a bounded local Chromium run completes, DPR 2 and a busy/throttled guest will retain truthful full records and either meet ordinary validity rules or reject explicitly; their numbers are stress observations and need not satisfy the baseline CV/FPS. If the overloaded local browser cannot complete in the bound, retained baseline evidence proves only DPR 1/unthrottled behavior and any uncovered stress criteria remain a proof gap.
9. **P9 changed-hunk coverage:** runtime branches implementing successful drag collection and null/no-window/no-damage rejection will be exercised by retained or fresh evidence. Declarative exports, source/dist copies, metadata, and cleanup-only hardening may be waived only with exact parity/static reasoning; no task-claimed semantic branch may be silently waived.
