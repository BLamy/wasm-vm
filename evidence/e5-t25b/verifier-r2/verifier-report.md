VERDICT: refuted

- **P6 stationary-window rejection — FAILED.** Predicted that a run with no window movement could not emit a plausible accepted result. A bounded composition attack supplied five runs with exactly 300 requested and processed pointers, positive stationary drawn records, valid attribution, and matching durations; production `summarizeDragRun` plus `aggregateDragRuns` accepted them at mean 15 FPS, CV 0%, `repeatabilityHeld:true` (`analyzer-attacks-output.json:15-18`). The browser runner reads Foot chrome before the run and checks only pointer-frame delta; it never measures after-geometry or rejects stationary damage (`tools/verify/e5-t25b-browser.mjs:135-169`). This contradicts the task's explicit stationary-window failure requirement. Demand: retain and compare before/after window geometry (or an equivalently robust displacement signal), reject insufficient displacement before aggregation, and add a deterministic stationary-window regression test.
- **P1-P5 retained baseline/raw attribution — HELD.** Both artifact hashes, exact producer head, local Chrome version, viewport/DPR, 1 GiB image hash, split-manifest hash, and screenshot dimensions matched. The only producer-to-current difference is task metadata (`artifact-audit-output.json:2-15`). Across all 473 records, sequences/timestamps were globally unique and increasing; booleans, positive bounded damage, exact damage bytes, nonnegative coherent instruction totals/deltas, drawn counts, duration sample counts/sums, scheduler duration deltas, and pointer equations held. Each retained run also showed 286-405 pixels of horizontal damage movement (`artifact-audit-output.json:17-21`). Aggregate p50/p95/mean/CV independently recomputed, with CV 11.321151210004228% and null-sink rejection (`artifact-audit-output.json:23-31`). Scheduler retired-instruction differences from present sums were exactly explained by nonnegative pre/tail sampling boundaries in every run; no regression or unattributed sign error was found.
- **P7 malformed analyzer matrix — HELD where producer invariants apply; stationary exception is the refutation above.** NaN values, missing/zero/negative attribution, missing/regressing scheduler counters, null/all-undrawn records, wrong run counts, and NaN aggregate FPS rejected. The pure analyzer accepts 299/301, duplicate sequence, regressing cumulative totals, duplicate run IDs, and FPS-only objects (`analyzer-attacks-output.json:4-14`), but the fixed browser producer pins 300 moves, `PresentationController` creates monotone sequences/totals, and the retained raw audit proves those producer invariants for this evidence. Those accepted synthetic cases do not independently refute the recorded production path. Stationary drawn records do, because no later producer check measures movement.
- **P8 release/environment — HELD.** Three syntax checks, all five focused tests, updated release audit, direct source/dist comparisons (including roadmap), and diff whitespace audit passed under hostile inherited `RUSTFLAGS`, `RUST_LOG`, and `CARGO_*`. The runner deletes these keys from the child server environment (`focused-checks.log:1-11`; `tools/verify/e5-t25b-browser.mjs:65-72`).
- **P10 DPR-2/busy/4x stress — ENVIRONMENT-LIMITED.** The approved headed DPR-2 attempt timed out after exactly 120000 ms at the desktop-ready wait (`tools/verify/e5-t25b-browser.mjs:111`) and wrote no artifact (`fresh-stress.log:1-7`). No drag ran, so busy/4x were not attempted and no stress behavior is claimed. This is separate from the DPR-1 baseline and is not the refutation.
- **COVERAGE — runtime success covered; stationary acceptance refuted.** Successful exact-head recording exercised raw retention and all baseline producer paths added through `a14bf545`; the fresh timeout exercised failure cleanup. Dist/TS/roadmap/declarations and bounded ring/error guards are precisely waived. The movement loop's missing postcondition is anchored to the changed browser hunk and the passing stationary sabotage (`coverage.md`).
- **SUITE:** retain `artifact-audit.mjs` as the raw-evidence checker and `analyzer-attacks.mjs` as the deterministic sabotage corpus. Promote the stationary attack into the repository test suite after the runtime fix. No implementation or test files were modified by this verifier.

## Orientation and commands

Inspected `AGENTS.md`, the complete task, prior verifier report, producer diff from activation through `a14bf545`, and metadata delta to `6793e0ef`. Frozen predictions are in `predictions.md`.

Commands included: `node artifact-audit.mjs`; `node analyzer-attacks.mjs`; hostile-env syntax checks, focused Node tests, and release audit; direct source/dist `cmp`; `git diff --check`; actual 1 GiB image SHA-256; Chrome version check; and a bounded headed DPR-2 browser attempt. No WebKit, independent-machine, host-rr, merge, implementation, or test mutation was performed.
