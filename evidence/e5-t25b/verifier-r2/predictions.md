# E5-T25b verifier-r2 frozen predictions

Frozen before opening the retained JSON, screenshot, prior detailed verifier report, or test outputs. The task log necessarily disclosed worker summaries; every prediction below is checked independently from raw records and producer code.

1. **Artifact identity/provenance (P1):** SHA-256 will exactly match the two claimed digests; JSON will name exact producer head `a14bf5453a6799a67b5f66e85b4167223580e541`, Chromium `152.0.7977.76`, viewport `1440x1050`, DPR 1, and image/manifest digests that match repository bytes at that head. Current head minus producer head will contain task-log metadata only; any producer/runtime difference is a failure requiring rerecording.
2. **Five-run movement and stability (P2):** exactly five baseline runs will each request 300 moves, satisfy `pointerFramesBefore + pointerFramesDelta = pointerFramesAfter`, have integer delta >=300, and aggregate drawn FPS recomputation will match stored per-run and aggregate mean/p50/p95/CV with CV <15%.
3. **Raw present integrity (P3):** every run's retained present records will have unique strictly increasing sequence numbers and nondecreasing finite timestamps; booleans will be booleans; byte/damage/instruction fields will be finite nonnegative integers; every drawn record will carry nonempty positive damage, positive bytes, and positive guest attribution; undrawn records will not contribute to drawn summaries.
4. **Instruction coherence (P4):** per-record `guestInstructionsTotal` will be monotone, nonnegative and coherent with `guestInstructions`; raw guest sums and scheduler before/after retired-instruction deltas will equal the reported guest attribution buckets without regressions or unattributed positive work.
5. **Duration and scheduler coherence (P5):** present-duration samples will be finite/nonnegative, their count and sum will exactly reproduce reported present count/duration; scheduler `totalSliceMs` and `totalFetchMs` deltas will be nonnegative and exactly reproduce reported guest/transfer buckets.
6. **Drawn-source and null sink (P6):** drawn/undrawn raw counts will exactly match summaries, FPS will derive from drawn presents over measured wall time rather than requested moves or RAF callbacks, and the null sink will reject with zero drawn presents. No-window, no-damage, zero/missing attribution, malformed aggregate, and motion-below-threshold paths will reject.
7. **Analyzer attacks (P7):** 299 processed moves will reject while 301 will accept; duplicate/out-of-order sequence, NaN/nonfinite numbers, missing/zero/regressing attribution, null sink, and malformed aggregate mutations will all reject. Acceptance of any such malformed case is a refutation unless producer invariants are both enforced and retained evidence proves them.
8. **Release/env reproducibility (P8):** focused tests, syntax check, and updated release audit will pass with hostile inherited `RUSTFLAGS`, `RUST_LOG`, and `CARGO_*`; source/dist parity checks will be byte-exact for duplicated source assets, and the runner's child server environment will delete those variables.
9. **Coverage (P9):** retained successful evidence will exercise all behavioral producer/analyzer paths needed for the baseline and successful exact-head write. Pure declarations/static release assertions may be precisely waived. Error-only guards require deterministic analyzer/release attacks; any task-relevant unexercised producer hunk is `needs-evidence`.
10. **Bounded fresh stress (P10):** if local desktop readiness completes, DPR 2 and busy/4x-throttle results will be recorded separately and will either validly reject/complete without contaminating the DPR-1 baseline. If readiness times out, the log will cite the exact timeout point/duration and the stress behavior will remain unproven, not refuted.

Verdict rule: `verified` only if P1-P9 hold and the task-required retained baseline is sufficient; P10 environment limitation alone is recorded separately because the prior attempt established a bounded readiness limitation and the user explicitly permits that classification. Any contradictory runtime value is `refuted`; any missing task-relevant record or unexercised producer behavior is `needs-evidence`.
