# E5.5-T03e fresh critic predictions

Recorded before running tests or inspecting worker evidence. Scope is the current browser-side
immutable boot cache and startup reporting diff only. E5.5-T03c's unchanged runtime/GUI bindings
remain incrementally HELD; physical-input responsiveness is neither inferred from pixels nor
re-verified here.

## Predictions

- **P1 — exact integrity on both sources.** For independently generated byte bodies, a network or
  cache response returns only when its streamed byte count equals the descriptor size and its
  SHA-256 equals the descriptor hash. Truncated, oversized, same-length-corrupt, opaque, non-200,
  missing-body, and non-byte streams fail before bytes reach the loader or a cache write occurs.
- **P2 — release/hash independence.** Two descriptors for different release hashes use distinct
  CacheStorage keys. A lookup for the new hash cannot consume the old hash entry even when URL,
  role, and size are identical; no broad cache deletion occurs.
- **P3 — corrupt-cache recovery.** A corrupt exact-key cache entry is rejected, that exact key is
  deleted once, and a network request with cache bypass/reload semantics is verified before the
  exact key is repopulated. If the refetch is also invalid, boot fails and no corrupt replacement is
  stored.
- **P4 — storage failure is non-fatal and honestly reported.** CacheStorage access/open/match/delete/
  put failures do not weaken network verification. Access/open/match/put failure permits a valid
  network boot, emits an unavailable source status, and never claims successful persistence.
- **P5 — source-phase accuracy.** A clean hit reports `reading cache` while consuming cached bytes;
  a miss reports `downloading`; a corrupt hit visibly transitions from cache to network; and cache
  unavailability remains visible in the network phase. Progress callback failures cannot affect
  integrity or boot.
- **P6 — transfer/restore UI accuracy.** Unpacking and guest lifecycle states hide/reset any prior
  determinate transfer bar. Generic serial output appends diagnostics without replacing the active
  download/cache/unpack/restore/wait status.
- **P7 — late lifecycle events.** Once desktop readiness is accepted, later generic progress,
  guest-state, guest-ready, and serial events cannot revive the overlay, stale bar, or waiting timer.
  A new `wvm:guest-booting` event starts a fresh lifecycle and can report progress again. Error and
  halt events remain terminal for their active boot.
- **P8 — bounded wait honesty.** `restored` or `guest-ready` arms at most one current-generation
  15-second wait notice; desktop-ready/error/halt/new-boot cancels it, including already-queued stale
  callbacks. The notice says the guest is still awaited and does not claim interactivity.
- **P9 — real reload request proof.** With browser HTTP caching disabled by the proof server, the
  initial real Omarchy boot requests each of kernel, manifest, delta, and snapshot exactly once;
  reload requests none of those paths and reads four independently verified content-addressed cache
  entries. Local and production captures retain real guest presents and no browser errors.
- **P10 — harness/source sufficiency.** The final report binds its source hashes and evidence to one
  frozen HEAD/dist, fails if required source files or real cache entries are absent, and cannot pass
  merely from emitted phase labels, GUI screenshots, or browser HTTP cache behavior.
- **P11 — boundary and regressions.** Only immutable compressed release objects enter the shared
  cache; no RAM, writable disk/overlay, credentials, or cross-session guest state is persisted. CLI
  guest layout/boot behavior and the built ISA suite remain unchanged/green.
- **P12 — test independence.** A bounded novel adversarial test using independently computed hashes,
  randomized chunk boundaries, stale-key poisoning, throwing storage methods, and late-event orderings
  will agree with P1–P8. Sabotaging either hash comparison or exact-size enforcement will make the
  promoted integrity tests fail.

## Evidence required before verdict

- Final exact-head targeted tests and `make verify-E5.5-T03e` from the frozen integration.
- Real initial/reload local request log and captures from a no-store server.
- Pristine cold-clone acceptance at the same commit with scrubbed build environment.
- Production initial/reload request/cache/status proof bound to the deployed source hashes.
- Built ISA suite result and CLI regression evidence.
- No physical-input claim: screenshots and successful presents prove pixels only.

## 2026-09-10 — interim adversarial results

INTERIM VERDICT: refuted (task remains `in-progress`; integration is not frozen)

- **P1–P5 — HELD on focused and independent attacks.** The worker's 26 focused tests passed.
  Six independent cache tests also passed: randomized stream chunking and same-length tamper,
  stale-release hash separation, throwing `open`/`match`/`put`, failed exact-key deletion followed
  by verified `cache: reload` refetch, malformed progress, and stale queued timer callbacks. A
  disabled SHA comparison and a disabled final exact-size check each broke the rejection oracle.
  Command: `node --test web/tests/e5.5-t03e-critic.test.mjs ...` (green before the terminal-state
  attack was added).
- **P6–P7 (nonterminal traffic) — HELD.** A real headless Chrome fixture loaded the actual `ide.js`,
  then showed that serial output did not replace download status, restore/unpack hid and reset the
  determinate bar, desktop readiness stayed sticky against late progress/state/ready/serial traffic,
  and a new boot reset the overlay. Promoted check:
  `web/tests/e5.5-t03e-critic-ui.test.mjs`.
- **P7–P8 terminal `done` — FAILED.** Predicted that `done` would cancel the pending wait and reject
  late readiness until a new boot. Observed the pure lifecycle invoke `onWaiting`, and the actual IDE
  change `Guest finished; desktop readiness is not confirmed.` to
  `Guest shell ready; waiting for desktop readiness…` after a late `wvm:guest-ready`. Repro:
  `node --test web/tests/e5.5-t03e-critic.test.mjs web/tests/e5.5-t03e-critic-ui.test.mjs` — 6 pass,
  2 fail. Demand: latch `done`/error/halt terminal state, cancel its generation, reject late
  ready/desktop-ready events, and clear only on `booting`; handlers must honor rejection.
- **P9 local request/cache proof — HELD for the unfrozen build.** The no-store server recorded one
  initial GET and zero reload GETs for each of kernel, content-addressed chunk manifest, overlay
  delta, and boot snapshot. Restore/ready were 4624/67597 ms initially and 3124/65497 ms on reload;
  the four cache keys were present, no browser/guest errors were recorded, and progress was hidden at
  readiness. `initial-desktop.png` and `reload-desktop.png` are byte-identical
  (`97fc180d…f3f`); the restored captures are byte-identical (`5f53bd95…a139`). This proves pixels,
  not physical-input responsiveness. Source: `local/report.json` and its four PNGs.
- **P10 exact-head/source binding — NEEDS EVIDENCE.** The local run predates the verifier's fail-fast
  edit and final dist rebuild. Its helper/runtime hashes still match except the subsequently restamped
  `sw.js`, so the behavior is useful but not final exact-head evidence. The production harness hashes
  local `web/dist` at `tools/verify/omarchy-boot-cache.mjs:23-25` but does not hash remotely served
  source files or record a commit/dirty-state binding. Bounded demand: record frozen commit and local
  dist hashes, fetch the same six production URLs with `cache: no-store`/a unique query, hash their
  response bytes, and fail unless every remote hash equals the frozen local hash.
- **P11 — PARTIAL HELD / NEEDS EVIDENCE.** Static diff and unchanged WASM hash
  `c48e9c2d…b9305` keep E5.5-T03c runtime/GUI bindings incrementally HELD, and only the four immutable
  descriptors reach the new cache. The first demo attempt honestly failed because stale `tasks.json`
  lacked E5.5-T03e; the regenerated rerun then passed with `126 passed, 0 failed`, zero console errors,
  and zero HTTP errors (`demo-final/demo-suite.json`). CLI regression evidence remains outstanding;
  do not expand into the unchanged prior trace issue.
- **COVERAGE — NEEDS EVIDENCE.** Unit/critic tests cover integrity, source transitions, storage
  failures, and lifecycle policy; the local run covers both real network and cache source paths.
  Final exact-head local/cold-clone/production runs remain required after the terminal-state fix.

Runtime/browser source under attack: `af206380a8200770a77bf3076313f0e9db58f29b`. The two promoted
critic tests and this report are intentionally uncommitted for coordinator review.

## 2026-09-10 — P7/P8 terminal-fix re-verification

SCOPED VERDICT: HELD (runtime remains unfrozen; no overall task verdict)

- The helper now latches `done`, `error`, and `halted`, invalidates the timer generation, rejects
  state/ready/desktop-ready while terminal, and clears the latch only on `booting`.
- Actual `ide.js` handlers honor lifecycle acceptance for progress, state, guest-ready, and
  desktop-ready. Terminal events clear/reset the transfer bar; `done` after an already-visible
  desktop restores the overlay and removes visible readiness.
- The previously failing predictions now hold: `done` neither fires a stale wait callback nor accepts
  late guest readiness. Worker evidence `terminal-fix-tests.log` reports all 35 focused/promoted tests
  passing.
- The promoted real-IDE test was extended to attack `done -> late desktop-ready/ready/progress/state`,
  `error|halt -> late ready/desktop-ready/progress/state`, terminal stale bars, and new-boot reset.
  Independent command passed 12/12:
  `node --test web/tests/e5.5-t03e-critic.test.mjs web/tests/e5.5-t03e-critic-ui.test.mjs web/tests/omarchy-startup-state.test.mjs`.
- No runtime files were edited by the verifier. Only
  `web/tests/e5.5-t03e-critic-ui.test.mjs` and this critic record changed in this pass.

P10 and final exact-head local/cold-clone/production evidence remain pending and were not re-reviewed.

## 2026-09-10 — frozen-head P10 harness review

SCOPED VERDICT: HELD for harness design; final environment evidence pending

- Frozen source is `408422d4b319f8267071d0bd47d46c90a81e1af3`. The harness records that HEAD,
  asserts an empty scoped status for its verifier, manifest, source, dist, service-worker, and WASM
  inputs, and records the exact scoped path list in the report.
- Loader, IDE, cache helper, lifecycle helper, and WASM source/dist pairs are byte-identical at the
  frozen head. Recorded dist hashes are `26a4c9fa…d6a3`, `afe853ea…e93`, `ec667f89…dfdbe`,
  `fb80a37b…2ac8`, `5e476073…b9db` (stamped SW), and `c48e9c2d…b9305` (unchanged WASM).
- Production mode independently fetches all six deployed URLs from the destination origin using a
  HEAD/time/random unique query, `cache: no-store`, and a 30-second abort signal. HTTP 200 and exact
  SHA-256 equality with frozen local dist are mandatory before Chromium starts.
- Bounded sabotage HELD: a loopback production endpoint returned HTTP 200 with wrong bytes. The
  harness exited 1 before browser launch and wrote a failed report containing frozen HEAD,
  `scopedDirty: ""`, unique request URL, observed digest `51c912ed…d7bb`, expected loader digest
  `26a4c9fa…d6a3`, and the assertion at `tools/verify/omarchy-boot-cache.mjs:84`.
- The harness records both restored and desktop PNG digests for initial/reload (four total), loaded
  module resource URLs, the controlling service-worker URL, cache entries, provenance phases, actual
  requests, and browser/display errors.
- Frozen CLI regression HELD: `cli/report.json` says `PASS`, real VM, usable BusyBox, exact arithmetic/
  serialization/nonzero/spoof/stream/after-stop behavior, and no console/page/request errors.
- Frozen built demo/ISA HELD: `demo-terminal-fix/demo-suite.json` records 126 passed, 0 failed, zero
  console errors, and zero HTTP errors.

Await completed cold-clone and production reports before promoting P10 or issuing an overall verdict.

## 2026-09-10 — pristine-clone acceptance review

SCOPED VERDICT: HELD; final overall verdict still awaits production

- `cold-clone/report.json` records `status: passed`, frozen HEAD
  `408422d4b319f8267071d0bd47d46c90a81e1af3`, and an empty scoped status over the complete P10
  source/dist/manifest set. Its six source hashes exactly match the previously reviewed frozen hashes.
- The pristine-clone command log records 35/35 focused and promoted tests passing before the real
  browser run. Snapshot acquisition was independently SHA-256 verified.
- The no-store server saw exactly one initial GET and zero reload GETs for kernel, content-addressed
  chunk manifest, overlay delta, and boot snapshot. Both phases retained the same four exact
  content-addressed CacheStorage keys.
- Initial/reload phases reported the expected download/cache-read plus unpack phases, reached
  restored and desktop-ready without guest/browser/display errors, hid the transfer bar and overlay,
  and recorded the loaded IDE/loader/cache/lifecycle/WASM URLs plus controlling `sw.js` URL.
- Timings were 4308/67309 ms (initial restore/ready) and 3125/64514 ms (reload restore/ready).
- All four 1280x800 captures were inspected. Initial/reload restored captures are byte-identical at
  `5f53bd95…a139`; initial/reload desktop captures are byte-identical at `97fc180d…f3f`. They preserve
  the graphical pixels and honest waiting overlay. They do not prove physical-input responsiveness.

All earlier HELD results are carried forward unchanged. Production source-binding and initial/reload
behavior remain the sole outstanding acceptance evidence.
