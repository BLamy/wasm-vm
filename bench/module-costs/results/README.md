# results — E4-T19 cost matrix

Generated by the repository's exact `../run.sh` command on 2026-08-30 from this 16-logical-CPU
macOS host. One `<engine>.json` is recorded per browser: `chromium.json`, `firefox.json`, and
`webkit.json` (the Safari substitute). The captured Playwright browser versions are Chromium
151.0.7922.34, Firefox 153.0, and WebKit 26.5.

The run passed all three projects. Each engine held 1,000, 5,000, and 10,000 live instances of
the 64-function module without an instantiation failure, so `10,000` is the observed lower bound
for the instance cliff. The E4-T20 budget of 256 live modules is therefore at least 39.1x below
that observed lower bound. Firefox and WebKit do not expose `performance.memory`; their rows record
`null` for that browser-specific field as designed.

`chromium-system.json` is a supplemental robustness screen, generated with
`E4_T19_CHROME_EXECUTABLE` and the separately installed Google Chrome 152.0.7977.65. It also
reached 10,000 live instances (`failedAt: null`), so the result was stable across the pinned
Chromium 151 and system Chrome 152 browser versions on this host. It is not part of `../run.sh`.
