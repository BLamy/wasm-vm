# results — E4-T19 cost matrix

Populated by `../run.sh` on the Linux dev box (this mac OS-reaps long browser runs).
One `<engine>.json` per browser: chromium.json, firefox.json, webkit.json.
Committed empty on purpose — no fabricated numbers (see the ticket Verification debt).
