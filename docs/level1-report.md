# Level 1 compliance gate report — ✅ THRESHOLD MET

Frozen consolidated output of `tools/level1_gate.sh` (regenerate on demand with `make
level1-gate`; the live copy lands in the gitignored `target/level1-report.md`). This is the
Epic-1 Level-1 capstone (E1-T24) demonstrable threshold: **the complete RV64GC + privileged
machine passes riscv-tests AND a full RISCOF architectural-compliance run against the canonical
RISC-V Sail reference — native and WASM — with ZERO allowlist/exclusion entries.**

## VERDICT: ✅ LEVEL 1 THRESHOLD MET — all legs green, zero deferrals

| leg | what | status | detail |
|---|---|---|---|
| A | native riscv-tests | **PASS** | rv64ui/um/ua/uf/ud/uc + rv64mi green; 0 allowlisted |
| B | RISCOF vs Sail | **PASS** | 395 passed / 0 failed; 0 EXCLUSIONS entries |
| C | native==wasm equality | **PASS** | native and wasm32 builds match the frozen T22 golden fingerprints |
| D | wasm artifact identity | **PASS** | `web/pkg/wasm_vm_wasm_bg.wasm` sha256 recorded below |

## Deferral accounting — the zero-exclusion bar, MET

- riscv-tests allowlist (`tests/riscv-tests-allowlist.txt`): **0**
- RISCOF exclusions (`compliance/EXCLUSIONS.md`): **0**
- **total deferred: 0**

Every exclusion that once lived here was cleared: exception-priority §3.7.1 (E1-T25),
misaligned-access + the switch to the config-honoring **Sail** reference that validates our
declared ISA — which alone retired the Sv57 / 64-region-PMP / misalign exclusions Spike could
not (E1-T26), and debug triggers → `rv64mi-p-breakpoint` (E1-T29).

## Pins (reproducibility)

- reference model: **RISC-V Sail** `sail_riscv_sim` v0.12 (prebuilt; provisioned by
  `compliance/provision.sh`), pinned to our ISA via `compliance/sail/sail_config_override.json`
  (rv64gc + S/U + Sv39/Sv48; misaligned scalar supported; Svnapot/Svpbmt/Sv57/Svrsw60t59b off).
  Spike fallback: `RISCOF_REF=spike` (a reference-capability gap, documented, not a DUT bug).
- wasm artifact sha256: `3d2b86c1aa68466d2d4c5488a0e8189a878fe6e1353229b00a701c25c55808db`
- toolchain image: `wasm-vm-toolchain:local` (gcc + Spike; see `tools/toolchain/versions.env`)
- riscv-arch-test: pinned by `compliance/provision.sh` (`riscof arch-test --clone`)

## Browser leg (the ROADMAP's "in both native and WASM builds", made visible)

The E0-T23 web demo (`web/index.html` + `web/main.js`, served by `make web-serve`) loads the
wasm module and runs the riscv-tests set live with a per-test pass/fail heatmap and a green/red
verdict banner — the WASM leg running in a browser tab. Leg C already proves the wasm build is
the same machine as native (byte-identical determinism fingerprints).

_Generated by `tools/level1_gate.sh` — exit 0 (THRESHOLD MET)._
