#!/bin/sh
# E5-T18d: init may respawn getty, but a failed desktop may not reset its boot-wide budget.
set -eu
if [ -e /home/desktop/.local/state/wasm-vm/desktop.failed ]; then
  exec /sbin/getty -L -f /etc/wasm-vm/desktop-fallback.issue 115200 tty1 linux
fi
exec /bin/login -f desktop
