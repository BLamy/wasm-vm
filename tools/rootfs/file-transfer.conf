# Fixed WVFT guest capability. This file is declarative build/audit metadata; the
# agent compiles these values in and does not accept runtime overrides.
endpoint=10.0.2.2:10021
inbox=/var/lib/wasm-vm/transfer/inbox
outbox=/var/lib/wasm-vm/transfer/outbox
max_concurrent_transfers=2
