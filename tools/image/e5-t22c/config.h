/* Only declarations from the hash-pinned Weston backend headers are compiled.
 * No upstream implementation or optional renderer is built into this module.
 * The structs used here have unconditional layouts in Weston 12.0.4. */
